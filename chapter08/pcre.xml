<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-system-pcre2" role="wrap">
  <?dbhtml filename="pcre2.html"?>

  <sect1info condition="script">
<productname>pcre2</productname>
<productnumber>&pcre2-version;</productnumber> <address>&pcre2-url;</address></sect1info>

  <title>Pcre2-&pcre2-version;</title>

  <indexterm zone="ch-system-pcre2">
    <primary sortas="a-Pcre2">Pcre2</primary>
  </indexterm>

  <sect2 role="package">
    <title/>

    <para>O pacote pcre2 contém uma nova geração das bibliotecas Perl Compatible
Regular Expression.</para>

    <segmentedlist>
      <segtitle>&buildtime;</segtitle>
      <segtitle>&diskspace;</segtitle>

      <seglistitem>
        <seg>&pcre2-fin-sbu;</seg>
        <seg>&pcre2-fin-du;</seg>
      </seglistitem>
    </segmentedlist>

  </sect2>

  <sect2 role="installation">
    <title>Instalação do Pcre2</title>

    <para>Prepare pcre2 para compilação:</para>

<screen><userinput remap="configure">./configure --prefix=/usr                       \
            --docdir=/usr/share/doc/pcre2-&pcre2-version; \
            --enable-unicode                    \
            --enable-jit                        \
            --enable-pcre2-16                   \
            --enable-pcre2-32                   \
            --enable-pcre2grep-libz             \
            --enable-pcre2grep-libbz2           \
            --enable-pcre2test-libreadline      \
            --disable-static</userinput></screen>

    <variablelist>
      <title>O significado das novas opções de configuração:</title>

      <varlistentry>
        <term><parameter>--enable-unicode</parameter></term>
        <listitem>
          <para>
            Essa opção habilita o suporte Unicode e inclui as funções para manusear
sequências de caracteres UTF-8/16/32 na biblioteca. 
          </para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><parameter>--enable-jit</parameter></term>
        <listitem>
          <para>
            Essa opção habilita compilação just-in-time, o que pode acelerar bastante a
correspondência de padrões.  
          </para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><parameter>--enable-pcre2-16</parameter></term>
        <listitem>
          <para>
            Essa opção habilita suporte a caracteres de 16 bits. 
          </para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><parameter>--enable-pcre2-32</parameter></term>
        <listitem>
          <para>
            Essa opção habilita suporte a caracteres de 32 bits. 
          </para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><parameter>--enable-pcre2grep-libz</parameter></term>
        <listitem>
          <para>
            Essa opção adiciona suporte para leitura de arquivos comprimidos .gz no
pcre2grep.
          </para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><parameter>--enable-pcre2grep-libbz2</parameter></term>
        <listitem>
          <para>
            Essa opção adiciona suporte para leitura de arquivos comprimidos .bz2 ao
pcre2grep.
          </para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><parameter>--enable-pcre2test-libreadline</parameter></term>
        <listitem>
          <para>
            Essa opção adiciona recursos de edição de linha e de histórico ao programa
pcre2test.
          </para>
        </listitem>
      </varlistentry>

    </variablelist>

    <para>Compile o pacote:</para>

<screen><userinput remap="make">make</userinput></screen>

    <para>Para testar os resultados, emita:</para>

<screen><userinput remap="test">make check</userinput></screen>

    <para>Instale o pacote:</para>

<screen><userinput remap="install">make install</userinput></screen>

  </sect2>


  <sect2 id="contents-pcre2" role="content">
    <title>Conteúdo do Pcre2</title>

    <segmentedlist>
      <segtitle>Programas instalados</segtitle>
      <segtitle>Biblioteca instalada</segtitle>

      <seglistitem>
        <seg>pcre2grep e pcre2test</seg>
        <seg>libpcre2-8.so, libpcre2-16.so, libpcre2-32.so e libpcre2-posix.so</seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descrições Curtas</bridgehead>
      <?dbfo list-presentation="list"?> <?dbhtml list-presentation="table"?>

      <varlistentry id="pcre2grep">
        <term><command>pcre2grep</command></term>
        <listitem>
          <para>é uma versão do grep que entende expressões regulares compatíveis com Perl</para>
          <indexterm zone="ch-system-pcre2 pcre2grep">
            <primary sortas="b-pcre2grep">pcre2grep</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="pcre2test">
        <term><command>pcre2test</command></term>
        <listitem>
          <para>consegue testar uma expressão regular compatível com Perl</para>
          <indexterm zone="ch-system-pcre2 pcre2test">
            <primary sortas="b-pcre2test">pcre2test</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
