Révision 15612

fichiers modifiés :
   multimedia/libdriv/v4l-utils.xml
   lxde/desktop/lxsession.xml

Log :
Typos

Le robot a traité 59 % du commit anglais
Index: lxde/desktop/lxsession.xml
===================================================================
--- lxde/desktop/lxsession.xml	(révision 15611)
+++ lxde/desktop/lxsession.xml	(révision 15612)
@@ -77,8 +77,8 @@
       <xref linkend="dbus-glib"/>,
       <xref linkend="gtk2"/>,
       <xref linkend="libunique"/> (used by lxpolkit and lxclipboard),
-<xref linkend="lsb-release"/> (runtime for lxde-logout do not crash),
-<xref linkend="polkit"/> and
+      <xref linkend="lsb-release"/> (runtime for lxde-logout so it does not crash),
+      <xref linkend="polkit"/>, and
       <xref linkend="vala"/>
     </para>
 

