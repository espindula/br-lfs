Révision 15783

fichiers modifiés :
   general/prog/openjdk.xml
   introduction/welcome/changelog.xml

Log :
Fix tests for OpenJDK, due to the new version of jtreg

Le robot a traité 0 % du commit anglais
Index: general/prog/openjdk.xml
===================================================================
--- general/prog/openjdk.xml	(révision 15782)
+++ general/prog/openjdk.xml	(révision 15783)
@@ -388,7 +388,8 @@
 xhost +</userinput></screen>
 
     <para>
-Then, it is necessary to modify some files:
+      Then, it is necessary to modify some files (replace &lt;N&gt; with the
+      number of JAVA VM allowed to run concurrently):
     </para>
 
 <screen role='nodump'><userinput>echo -e "
@@ -403,8 +404,12 @@
           javax/print         \\
           sun/pisces          \\
           com/sun/java/swing" >> jdk/test/TEST.groups &amp;&amp;
-sed -e 's/all:.*jck.*/all: jtreg/' \
--i langtools/test/Makefile</userinput></screen>
+sed -e 's/all:.*jck.*/all: jtreg/'      \
+    -i langtools/test/Makefile          &amp;&amp;
+sed -i 's@/win32@@' jdk/test/Makefile   &amp;&amp;
+sed -e 's@JTREG_HOME@JT_PLATFORM="" &amp;@' \
+    -e 's@TEST="@CONCURRENCY=&lt;N&gt; &amp;@'    \
+    -i test/Makefile</userinput></screen>
 
     <para>
       Some variables have to be set:
@@ -423,10 +428,11 @@
             JT_HOME=${JT_HOME}              \
             JT_JAVA=${JT_JAVA}              \
             PRODUCT_HOME=${PRODUCT_HOME} all
-LANG=C ${JT_HOME}/linux/bin/jtreg -a -v:fail,error \
--dir:$(pwd)/hotspot/test    \
--k:\!ignore                 \
--jdk:${PRODUCT_HOME}        \
+LANG=C ${JT_HOME}/bin/jtreg -a -v:fail,error \
+                -dir:$(pwd)/hotspot/test     \
+                -k:\!ignore                  \
+                -jdk:${PRODUCT_HOME}         \
+                -agentvm -conc:&lt;N&gt;           \
                 :jdk</userinput></screen>
 
     <para>

Index: introduction/welcome/changelog.xml
===================================================================
--- introduction/welcome/changelog.xml	(révision 15782)
+++ introduction/welcome/changelog.xml	(révision 15783)
@@ -48,6 +48,10 @@
       <para>April 4th, 2015</para>
       <itemizedlist>
         <listitem>
+          <para>[pierre] - Change the test instructions in OpenJDK
+          to account for the new version of the jtreg test harness.</para>
+        </listitem>
+        <listitem>
           <para>[pierre] - Update to gnupg2-2.0.27. Fixes
           <ulink url="&blfs-ticket-root;6363">#6363</ulink>.</para>
         </listitem>

