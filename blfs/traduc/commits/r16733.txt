Révision 16733

fichiers modifiés :
M       scripts/blfs-chapter36.php
M       scripts/blfs-chapter40.php
M       scripts/blfs-chapter25.php

Log :
Ignore unstable versions for gdk-pixbuf, tigervnc, gimp and amarok.

Le robot a traité 1 % du commit anglais
Index: scripts/blfs-chapter25.php
===================================================================
--- scripts/blfs-chapter25.php	(révision 16732)
+++ scripts/blfs-chapter25.php	(révision 16733)
@@ -122,11 +122,7 @@
       $dirpath  = substr ( $dirpath, 0, $position );
       $dirlines = http_get_file( "$dirpath/" );
 
-if ( $book_index == "gdk-pixbuf" )
-$dir      = find_max( $dirlines, '/\d$/', '/^.* ([\d\.]+)$/' );
-else
-$dir      = find_even_max( $dirlines, '/\d$/', '/^.* ([\d\.]+)$/' );
+      $dir      = find_even_max( $dirlines, '/\d$/', '/^.* ([\d\.]+)$/' );
       $dirpath .= "/$dir/";
     }
  

Index: scripts/blfs-chapter36.php
===================================================================
--- scripts/blfs-chapter36.php	(révision 16732)
+++ scripts/blfs-chapter36.php	(révision 16733)
@@ -22,7 +22,7 @@
 $regex[ 'pidgen'       ] = "/^.*Download pidgin-(\d[\d\.]+\d).*$/";
 $regex[ 'fontforge'    ] = "/^.*fontforge-(20\d+).tar.*$/";
 $regex[ 'xscreensaver' ] = "/^.*xscreensaver-(\d[\d\.]+\d).tar.*$/";
-$regex[ 'tigervnc'     ] = "/^.*TigerVNC (\d[\d\.]+\d).*$/";
+$regex[ 'tigervnc'     ] = "/^.*TigerVNC (\d[\d\.]+\d)$/";
 $regex[ 'transmission' ] = "/^.*release version.*(\d[\d\.]+\d).*$/";
 $regex[ 'xarchiver'    ] = "/^.*Download xarchiver-(\d[\d\.]+\d).tar.*$/";
 
@@ -211,7 +211,7 @@
      {
          $dirpath  = rtrim  ( $dirpath, "/" );    // Trim any trailing slash
          $dirs     = http_get_file( "$dirpath/" );
-$dir      = find_max( $dirs, "/v\d\./", "/^.*(v\d\.[\d\.]+).*$/" );
+         $dir      = find_even_max( $dirs, "/v\d\./", "/^.*(v\d\.[\d\.]+).*$/" );
          $dirpath .= "/$dir/";
      }
 

Index: scripts/blfs-chapter40.php
===================================================================
--- scripts/blfs-chapter40.php	(révision 16732)
+++ scripts/blfs-chapter40.php	(révision 16733)
@@ -147,7 +147,7 @@
                                "/^.*cdparanoia-III-([\d\.]+).src.tgz.*$/" );
 
   if ( $package == "amarok" )
-return find_max( $lines, "/\d\./", "/^.*;([\d\.]+)\/.*$/" );
+      return find_max( $lines, "/\d\./", "/^.*;([\d\.]+)\/.*$/", TRUE );
 
   if ( $package == "xine-ui" )
       return find_max( $lines, "/\d\./", "/^\s*([\d\.]+)$/" );

