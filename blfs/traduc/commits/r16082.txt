Révision 16082

fichiers modifiés :
M       scripts/blfs-chapter43.php

Log :
Add 'pending' to the texlive output when nothing is found.

Le robot a traité 0 % du commit anglais
Index: scripts/blfs-chapter43.php
===================================================================
--- scripts/blfs-chapter43.php	(révision 16081)
+++ scripts/blfs-chapter43.php	(révision 16082)
@@ -135,7 +135,8 @@
          $dirs     = http_get_file( "$dirpath/" );
          $dir      = find_max( $dirs, "/20\d\d/", "/^.*(20\d\d).*$/" );
          $lines    = http_get_file( "$dirpath/$dir/" );
-return find_max( $lines, "/texlive-/", "/^.*texlive-(\d+).*$/" );
+         $ver      = find_max( $lines, "/texlive-/", "/^.*texlive-(\d+).*$/" );
+         return ( $ver == "0" ) ? "pending" : $ver;
      }
 
     // Get listing

