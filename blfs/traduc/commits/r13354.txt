Révision 13354

fichiers modifiés :
   general/prog/gcc-java.xml
   general/prog/gcc-ada.xml
   general/prog/gcc.xml

Log :
Try to improve English in GCC pages

Le robot a traité 0 % du commit anglais
Index: general/prog/gcc-ada.xml
===================================================================
--- general/prog/gcc-ada.xml	(révision 13353)
+++ general/prog/gcc-ada.xml	(révision 13354)
@@ -260,7 +260,7 @@
     that files are created for the specific architecture of your computer.</para>
 
     <para>
-<parameter>--with-system-zlib</parameter>: allows to use the system
+      <parameter>--with-system-zlib</parameter>: Uses the system
       <application>zlib</application> instead of the bundled one.
       <application>zlib</application> is used for compressing
       and uncompressing <application>GCC</application>'s intermediate
@@ -268,9 +268,10 @@
     </para>
 
     <para>
-<parameter>--enable-languages=ada</parameter>: instructs
-the build system to build the Ada tools and compiler. Note that
-the C and C++ compilers are also built.
+      <parameter>--enable-languages=ada</parameter>: Instructs the build system
+      to build the Ada tools and compiler. It is unavoidable that the
+      <application>C</application> and <application>C++</application>
+      compilers be built too.
     </para>
 
     <para><command>ulimit -s 32768</command>: This command prevents several

Index: general/prog/gcc-java.xml
===================================================================
--- general/prog/gcc-java.xml	(révision 13353)
+++ general/prog/gcc-java.xml	(révision 13354)
@@ -220,7 +220,7 @@
     that files are created for the specific architecture of your computer.</para>
 
     <para>
-<parameter>--with-system-zlib</parameter>: Allows to use the system
+      <parameter>--with-system-zlib</parameter>: Uses the system
       <application>zlib</application> instead of the bundled one.
     </para>
 
@@ -232,12 +232,12 @@
     </para>
 
     <para>
-<parameter>--enable-java-home</parameter>: Allows to create a directory
+      <parameter>--enable-java-home</parameter>: Creates a directory
       layout similar to that of a JVM.
     </para>
 
     <para>
-<parameter>--with-jvm-root-dir=/opt/gcj</parameter>: Allows to install
+      <parameter>--with-jvm-root-dir=/opt/gcj</parameter>: Installs
       the JVM in the specified location.
     </para>
 

Index: general/prog/gcc.xml
===================================================================
--- general/prog/gcc.xml	(révision 13353)
+++ general/prog/gcc.xml	(révision 13354)
@@ -216,7 +216,7 @@
     </para>
 
     <para>
-<parameter>--with-system-zlib</parameter>: allows to use the system
+      <parameter>--with-system-zlib</parameter>: Uses the system
       <application>zlib</application> instead of the bundled one.
       <application>zlib</application> is used for compressing
       and uncompressing <application>GCC</application>'s intermediate

