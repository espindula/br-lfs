Révision 15910

fichiers modifiés :
   postlfs/security/gnutls.xml
   general.ent

Log :
GnuTLS-3.4.0: move sed between configure and make; s/doc/manpages in explanation for the command.

Le robot a traité 39 % du commit anglais
Index: postlfs/security/gnutls.xml
===================================================================
--- postlfs/security/gnutls.xml	(révision 15909)
+++ postlfs/security/gnutls.xml	(révision 15910)
@@ -135,14 +135,15 @@
       following commands:
     </para>
 
-<screen><userinput>sed -e '/FUNCS=/ s/-d/&amp;|uniq/'    \
+<screen><userinput>./configure --prefix=/usr \
+            --with-default-trust-store-file=/etc/ssl/ca-bundle.crt &amp;&amp;
+sed -e '/FUNCS=/ s/-d/&amp;|uniq/'    \
     -e '/get_x509_crt/ {n;d}'     \
     -e '/get_x509_key/ {n;d}'     \
     -e '/get_verify_flags/ {n;d}' \
--i doc/manpages/Makefile.in   &amp;&amp;
+    -i doc/manpages/Makefile      &amp;&amp;
 
-./configure --prefix=/usr \
---with-default-trust-store-file=/etc/ssl/ca-bundle.crt &amp;&amp;
 make</userinput></screen>
 
     <para>
@@ -174,7 +175,7 @@
 
     <para>
       <command>sed -e ... -i doc/manpages/Makefile</command>:
-This command removes some duplicate lines and fixes the doc Makefile.
+      This command removes some duplicate lines and fixes the manpages Makefile.
     </para>
 
     <para>

