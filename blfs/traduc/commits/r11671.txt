Révision 11671

fichiers modifiés :
   xsoft/graphweb/firefox.xml
   x/lib/xulrunner.xml

Log :
note about gstreamer dep for firefox.

Le robot a traité 0 % du commit anglais
Index: x/lib/xulrunner.xml
===================================================================
--- x/lib/xulrunner.xml	(révision 11670)
+++ x/lib/xulrunner.xml	(révision 11671)
@@ -110,6 +110,7 @@
       <xref linkend="curl"/>,
       <xref linkend="dbus-glib"/>,
       <xref linkend="doxygen"/>,
+      <xref linkend="gst-plugins-base"/>,
       <ulink url="http://hunspell.sourceforge.net/">Hunspell</ulink>,
       <xref linkend="libnotify"/>,
       <xref linkend="startup-notification"/>,
@@ -151,6 +152,8 @@
 ac_add_options --disable-libnotify
 
 # Uncomment these lines if you have installed optional dependencies:
+# GStreamer is necesary for H.264 video playback in HTML5 Video Player
+#ac_add_options --enable-gstreamer
 #ac_add_options --enable-system-hunspell
 #ac_add_options --enable-startup-notification
 

Index: xsoft/graphweb/firefox.xml
===================================================================
--- xsoft/graphweb/firefox.xml	(révision 11670)
+++ xsoft/graphweb/firefox.xml	(révision 11671)
@@ -115,6 +115,7 @@
       <xref linkend="curl"/>,
       <xref linkend="dbus-glib"/>,
       <xref linkend="doxygen"/>,
+      <xref linkend="gst-plugins-base"/>,
       <ulink url="http://hunspell.sourceforge.net/">Hunspell</ulink>,
       <xref linkend="libnotify"/>,
       <xref linkend="startup-notification"/>,
@@ -170,6 +171,8 @@
 ac_add_options --disable-libnotify
 
 # Uncomment these lines if you have installed optional dependencies:
+# GStreamer is necesary for H.264 video playback in HTML5 Video Player
+#ac_add_options --enable-gstreamer
 #ac_add_options --enable-system-hunspell
 #ac_add_options --enable-startup-notification
 

