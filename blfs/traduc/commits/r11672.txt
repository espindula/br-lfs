Révision 11672

fichiers modifiés :
   postlfs/security/attr.xml
   postlfs/security/acl.xml

Log :
minor acl fixes.

Le robot a traité 0 % du commit anglais
Index: postlfs/security/acl.xml
===================================================================
--- postlfs/security/acl.xml	(révision 11671)
+++ postlfs/security/acl.xml	(révision 11672)
@@ -78,7 +78,7 @@
 
 INSTALL_USER=root  \
 INSTALL_GROUP=root \
-./configure --prefix=/usr --libdir=/lib --libexecdir=/usr/lib &amp;&amp;
+./configure --prefix=/usr --libexecdir=/usr/lib --disable-static &amp;&amp;
 make</userinput></screen>
 
     <para>For meaningful results, the tests need to be carried out on a file
@@ -90,11 +90,10 @@
     <para>Now, as the <systemitem class="username">root</systemitem> user:</para>
 
 <!-- Package uses DIST_ROOT instead of DESTDIR -->
-<screen role="root"><userinput>make install install-dev install-lib                           &amp;&amp;
-chmod -v 0755 /lib/libacl.so.1.1.0                             &amp;&amp;
-rm -v /lib/libacl.{a,la,so}                                    &amp;&amp;
-ln -sfv ../../lib/libacl.so.1 /usr/lib/libacl.so               &amp;&amp;
-sed -i "s|libdir='/lib'|libdir='/usr/lib'|" /usr/lib/libacl.la &amp;&amp;
+<screen role="root"><userinput>make install install-dev install-lib             &amp;&amp;
+chmod -v 755 /usr/lib/libacl.so                  &amp;&amp;
+mv -v /usr/lib/libacl.so.* /lib                  &amp;&amp;
+ln -sfv ../../lib/libacl.so.1 /usr/lib/libacl.so &amp;&amp;
 install -v -m644 doc/*.txt /usr/share/doc/acl-&acl-version;</userinput></screen>
 
     <para>You should now re-install <application><ulink
@@ -129,9 +128,8 @@
     modifies the documentation directory so that it is a versioned
     directory.</para>
 
-<para><command>sed -i ... /usr/lib/libacl.la</command>: This command
-corrects the location of the installed static library as referenced
-in the libtool archive.</para>
+    <xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
+      href="../../xincludes/static-libraries.xml"/>
 
   </sect2>
 

Index: postlfs/security/attr.xml
===================================================================
--- postlfs/security/attr.xml	(révision 11671)
+++ postlfs/security/attr.xml	(révision 11672)
@@ -84,7 +84,7 @@
 
 <!-- Package uses DIST_ROOT instead of DESTDIR -->
 <screen role="root"><userinput>make install install-dev install-lib &amp;&amp;
-chmod -v 755 /usr/lib/libattr.so.1.1.0 &amp;&amp;
+chmod -v 755 /usr/lib/libattr.so &amp;&amp;
 mv -v /usr/lib/libattr.so.* /lib &amp;&amp;
 ln -sfv ../../lib/libattr.so.1 /usr/lib/libattr.so</userinput></screen>
 

