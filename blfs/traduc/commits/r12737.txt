Révision 12737

fichiers modifiés :
   general/sysutils/dbus.xml

Log :
D-Bus-1.8.0: modify 'Optional' dependencies.

Le robot a traité 3 % du commit anglais
Index: general/sysutils/dbus.xml
===================================================================
--- general/sysutils/dbus.xml	(révision 12736)
+++ general/sysutils/dbus.xml	(révision 12737)
@@ -92,20 +92,16 @@
 
     <bridgehead renderas="sect4">Optional</bridgehead>
     <para role="optional">
-To run tests:
+      For the tests:
       <xref linkend="dbus-glib"/>,
       <xref linkend="dbus-python"/>, and
       <xref linkend="pygobject2"/>;
-and to generate the API documentation:
-<xref linkend="doxygen"/>
+      for the API documentation:
+      <xref linkend="doxygen"/>;
+      for man pages and XML/HTML documentation:
+      <xref linkend="xmlto"/>
     </para>
 
-<para>
-Note that the <command>configure</command> script will look for
-<xref linkend="xmlto"/> but it does nothing as the XML/HTML documentation is
-already shipped in the source tree.
-</para>
     <para condition="html" role="usernotes">User Notes:
       <ulink url="&blfs-wiki;/dbus"/>
     </para>

