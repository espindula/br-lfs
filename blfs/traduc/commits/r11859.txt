Révision 11859

fichiers modifiés :
   xsoft/graphweb/firefox.xml
   x/lib/xulrunner.xml

Log :
firefox 24 enables gstreamer by default

Le robot a traité 5 % du commit anglais
Index: x/lib/xulrunner.xml
===================================================================
--- x/lib/xulrunner.xml	(révision 11858)
+++ x/lib/xulrunner.xml	(révision 11859)
@@ -151,11 +151,12 @@
 # If you have installed libnotify comment out this line:
 ac_add_options --disable-libnotify
 
-# Uncomment these lines if you have installed optional dependencies:
 # GStreamer is necessary for H.264 video playback in HTML5 Video Player;
 # to be enabled, also remember to set "media.gstreamer.enabled" to "true"
-# in about:config
-#ac_add_options --enable-gstreamer
+# in about:config. If you have installed GStreamer comment out this line:
+ac_add_options --disable-gstreamer
+# Uncomment these lines if you have installed optional dependencies:
 #ac_add_options --enable-system-hunspell
 #ac_add_options --enable-startup-notification
 

Index: xsoft/graphweb/firefox.xml
===================================================================
--- xsoft/graphweb/firefox.xml	(révision 11858)
+++ xsoft/graphweb/firefox.xml	(révision 11859)
@@ -170,11 +170,12 @@
 # If you have installed libnotify comment out this line:
 ac_add_options --disable-libnotify
 
-# Uncomment these lines if you have installed optional dependencies:
 # GStreamer is necessary for H.264 video playback in HTML5 Video Player;
 # to be enabled, also remember to set "media.gstreamer.enabled" to "true"
-# in about:config
-#ac_add_options --enable-gstreamer
+# in about:config. If you have installed GStreamer comment out this line:
+ac_add_options --disable-gstreamer
+# Uncomment these lines if you have installed optional dependencies:
 #ac_add_options --enable-system-hunspell
 #ac_add_options --enable-startup-notification
 

