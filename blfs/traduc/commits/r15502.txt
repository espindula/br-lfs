Révision 15502

fichiers modifiés :
   postlfs/security/sudo.xml

Log :
Fix relink of libsudo_util.so.0: the install process links it to the previous version of the library instead of the newly installed one. Thanks to Frenando for report and advice

Le robot a traité 3 % du commit anglais
Index: postlfs/security/sudo.xml
===================================================================
--- postlfs/security/sudo.xml	(révision 15501)
+++ postlfs/security/sudo.xml	(révision 15502)
@@ -121,7 +121,8 @@
       Now, as the <systemitem class="username">root</systemitem> user:
     </para>
 
-<screen role="root"><userinput>make install</userinput></screen>
+<screen role="root"><userinput>make install &amp;&amp;
+ln -sfv libsudo_util.so.0.0.0 /usr/lib/sudo/libsudo_util.so.0</userinput></screen>
 
   </sect2>
 
@@ -172,6 +173,12 @@
       </para>
     </note>
 
+    <para>
+      <command>ln -sfv libsudo_util...</command>: works around a bug in the
+      installation process, which links to the previously installed
+      version (if there is one) instead of the new one.
+    </para>
   </sect2>
 
   <sect2 role="configuration">

