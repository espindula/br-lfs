Révision 10742

fichiers modifiés :
   postlfs/security/openssl.xml
   x/installing/mesalib.xml

Log :
Text fixes

Le robot a traité 0 % du commit anglais
Index: postlfs/security/openssl.xml
===================================================================
--- postlfs/security/openssl.xml	(revision 10741)
+++ postlfs/security/openssl.xml	(revision 10742)
@@ -179,7 +179,7 @@
 
       <para>
         Most users will want to install Certificate Authority Certificates
-        for validataion of downloaded certificates. For example, these
+        for validation of downloaded certificates. For example, these
         certificates can be used by <xref linkend='git'/>,
         <xref linkend='curl'/> or <xref linkend='wget'/> when accessing secure
         (https protocol) sites. To do this, follow the instructions from the

Index: x/installing/mesalib.xml
===================================================================
--- x/installing/mesalib.xml	(revision 10741)
+++ x/installing/mesalib.xml	(revision 10742)
@@ -166,7 +166,7 @@
     </note>
 
     <para>
-      If you have downloaded recommended patch, apply it by running the
+      If you have downloaded the recommended patch, apply it by running the
       following command:
     </para>
 
@@ -195,7 +195,7 @@
 make</userinput></screen>
 
     <para>
-      If you have applied recommended patch, build the demo programs by
+      If you have applied the recommended patch, build the demo programs by
       running the following command:
     </para>
 
@@ -212,7 +212,7 @@
 <screen role="root"><userinput>make install</userinput></screen>
 
     <para>
-      If you have built demo programs, install them by running the
+      If you have built the demo programs, install them by running the
       following command as the
       <systemitem class="username">root</systemitem> user:
     </para>

