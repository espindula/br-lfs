Révision 15761

fichiers modifiés :
   x/lib/webkitgtk2.xml
   networking/netutils/networkmanager.xml
   xincludes/setqt.xml

Log :
Remove one extra line from WebKitGTK+-2.4.8. Remove a left over dependency from NetworkManager-1.0.0. Fix setq4 and setq5, in case one of them is installed with respective Qt but the other version of Qt is not installed.

Le robot a traité 26 % du commit anglais
Index: networking/netutils/networkmanager.xml
===================================================================
--- networking/netutils/networkmanager.xml	(révision 15760)
+++ networking/netutils/networkmanager.xml	(révision 15761)
@@ -82,8 +82,7 @@
       <xref linkend="dbus-glib"/>,
       <xref linkend="libndp"/>,
       <xref linkend="libnl"/>,
-<xref linkend="nss"/>,
-(or <xref linkend="gnutls"/>), and
+      <xref linkend="nss"/>, and
       <xref linkend="udev-extras"/> (for GUdev)
     </para>
 

Index: xincludes/setqt.xml
===================================================================
--- xincludes/setqt.xml	(révision 15760)
+++ xincludes/setqt.xml	(révision 15761)
@@ -17,13 +17,13 @@
     the following scripts:</para>
 
 <screen role="root"><userinput>cat &gt; /usr/bin/setqt4 &lt;&lt; 'EOF'
-<literal>if [ "x$QT5DIR" != "x/usr" ]; then pathremove  $QT5DIR/bin; fi
+<literal>if [ "x$QT5DIR" != "x/usr" ] &amp;&amp; [ "x$QT5DIR" != "x" ]; then pathremove  $QT5DIR/bin; fi
 if [ "x$QT4DIR" != "x/usr" ]; then pathprepend $QT4DIR/bin; fi
 echo $PATH</literal>
 EOF</userinput></screen>
 
 <screen role="root"><userinput>cat &gt; /usr/bin/setqt5 &lt;&lt; 'EOF'
-<literal>if [ "x$QT4DIR" != "x/usr" ]; then pathremove  $QT4DIR/bin; fi
+<literal>if [ "x$QT4DIR" != "x/usr" ] &amp;&amp; [ "x$QT4DIR" != "x" ]; then pathremove  $QT4DIR/bin; fi
 if [ "x$QT5DIR" != "x/usr" ]; then pathprepend $QT5DIR/bin; fi
 echo $PATH</literal>
 EOF</userinput></screen>

