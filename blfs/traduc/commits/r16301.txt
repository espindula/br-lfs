Révision 16301

fichiers modifiés :
   gnome/platform/dconf.xml
   x/lib/webkitgtk2.xml

Log :
Fix some typos

Le robot a traité 0 % du commit anglais
Index: gnome/platform/dconf.xml
===================================================================
--- gnome/platform/dconf.xml	(révision 16300)
+++ gnome/platform/dconf.xml	(révision 16301)
@@ -173,8 +173,8 @@
 
     <para>Now, optionally install the editor:</para>
 
-<screen><userinput>tar -xf ../dconf-editor-3.16.0.tar.xz &amp;&amp;
-cd dconf-editor-3.16.0 &amp;&amp;
+<screen><userinput>tar -xf ../dconf-editor-&dconf-editor-version;.tar.xz &amp;&amp;
+cd dconf-editor-&dconf-editor-version; &amp;&amp;
 
 ./configure --prefix=/usr --sysconfdir=/etc &amp;&amp;
 make</userinput></screen>

Index: x/lib/webkitgtk2.xml
===================================================================
--- x/lib/webkitgtk2.xml	(révision 16300)
+++ x/lib/webkitgtk2.xml	(révision 16301)
@@ -131,9 +131,9 @@
 
     <para>
       Upstream standard now is to link this package against <application>GTK+
-3</application>. However, for backward compatibility, manu users need
+      3</application>. However, for backward compatibility, many users need
       also another instance, linked to  <application>GTK+ 2</application>. Both
-can be installed in the same system, without problem.  However, some
+      can be installed in the same system, without any problems.  However, some
       packages can alternatively be linked to either one. Here, both build
       methods are presented and recommended to be built, but if you know which
       one you need, just jump to the respective part.

