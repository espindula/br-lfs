Révision 16155

fichiers modifiés :
M       scripts/blfs-chapter25.php

Log :
Fetch libepoxy version from github.

Le robot a traité 4 % du commit anglais
Index: scripts/blfs-chapter25.php
===================================================================
--- scripts/blfs-chapter25.php	(révision 16154)
+++ scripts/blfs-chapter25.php	(révision 16155)
@@ -24,6 +24,7 @@
 
 $regex = array();
 $regex[ 'freeglut' ] = "/^.*Download freeglut-(\d[\d\.]+\d).tar.*$/";
+$regex[ 'libepoxy' ] = "/^.*v(\d[\d\.]+\d).*$/";
 
 $url_fix = array (
 
@@ -38,6 +39,10 @@
    array( 'pkg'     => 'imlib2',
           'match'   => '^.*$', 
           'replace' => "http://sourceforge.net/projects/enlightenment/files/imlib2-src" ),
+   array( 'pkg'     => 'libepoxy',
+          'match'   => '^.*$', 
+          'replace' => "https://github.com/anholt/libepoxy/releases" ),
 );
 
 function get_packages( $package, $dirpath )

