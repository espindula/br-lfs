Révision 11364

fichiers modifiés :
   general.ent
   xsoft/graphweb/firefox.xml

Log :
Remove sed which fixed ff-21 build on xulrunner but breaks the ff-22 build.

Le robot a traité 47 % du commit anglais
Index: xsoft/graphweb/firefox.xml
===================================================================
--- xsoft/graphweb/firefox.xml	(révision 11363)
+++ xsoft/graphweb/firefox.xml	(révision 11364)
@@ -222,7 +222,6 @@
     </para>
 
 <screen><userinput>sed -i 's@ ""@@' browser/base/Makefile.in &amp;&amp;
-sed -i "/gre_path/s@DIST'\]@&amp; + '/bin'@" toolkit/mozapps/installer/packager.py &amp;&amp;
 make -f client.mk</userinput></screen>
 
     <para>
@@ -304,11 +303,6 @@
     </para>
 
     <para>
-<command>sed -i "/gre_path/s@DIST'\]@&amp; + '/bin'@" ...</command>: This
-sed fixes build with <application>Xulrunner</application>.
-</para>
-<para>
       <command>make -f client.mk ...</command>: Mozilla products are packaged to
       allow the use of a configuration file which can be used to pass the
       configuration settings to the <command>configure</command> command.

