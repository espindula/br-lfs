Révision 16922

fichiers modifiés :
M       scripts/blfs-chapter09.php
M       scripts/blfs-chapter17.php
M       scripts/blfs-chapter06.php

Log :
Update scripts for tcsh, liblinear, and serf.

Le robot a traité 7 % du commit anglais
Index: scripts/blfs-chapter06.php
===================================================================
--- scripts/blfs-chapter06.php	(révision 16921)
+++ scripts/blfs-chapter06.php	(révision 16922)
@@ -13,10 +13,11 @@
 
 $ignores = array();
 
-//$current="bluefish";   // For debugging
+//$current="tcsh";   // For debugging
 
 $regex = array();
$regex[ 'joe'  ] = "/^.*Download joe-(\d[\d\.]+).tar.*$/";
@@ -32,7 +33,7 @@
 
   array( 'pkg'     => 'tcsh',
          'match'   => '^.*$', 
-'replace' => "http://www.sfr-fresh.com/unix/misc" ),
+         'replace' => "http://www.tcsh.org/MostRecentRelease" ),
 );
 
 function get_packages( $package, $dirpath )
@@ -41,6 +42,7 @@
   global $regex;
   global $book_index;
   global $url_fix;
+  global $current;
 
   if ( isset( $current ) && $book_index != "$current" ) return 0;
 

Index: scripts/blfs-chapter09.php
===================================================================
--- scripts/blfs-chapter09.php	(révision 16921)
+++ scripts/blfs-chapter09.php	(révision 16922)
@@ -9,9 +9,10 @@
 $STOP_PACKAGE  = 'xapian-core';
 
 $renames = array();
+$renames[ 'v' ] = 'liblinear';
 $ignores = array();
 
-//$current="mozjs"; // For debugging
+//$current="v"; // For debugging
 
 $regex = array();
 $regex[ 'clucene-core'  ] = "/^.*Download clucene-core-([\d\.]+).tar.*$/";
@@ -94,6 +95,10 @@
         'match'   => '^.*$',
         'replace' => "http://pkgs.fedoraproject.org/repo/pkgs/libdaemon" ),
 
+ array( 'pkg'     => 'v',  //liblinear
+        'match'   => '^.*$',
+        'replace' => "https://github.com/cjlin1/liblinear/releases" ),
  array( 'pkg'     => 'wv',
         'match'   => '^.*$',
         'replace' => "http://www.abisource.com/downloads/wv" ),
@@ -294,6 +299,9 @@
   if ( $book_index == "libusb" )
     return find_max( $lines, '/^.*libusb-[\d\.]+.*$/', '/^.*libusb-([\d\.]+)$/' );
 
+  if ( $book_index == "v" ) // liblinear
+    return find_max( $lines, '/v\d/', '/^.*v(\d+)$/' );
   if ( $book_index == "libusb-compat" )
     return find_max( $lines, '/^.*compat-[\d\.]+$/', '/^.*compat-([\d\.]+)$/' );
 

Index: scripts/blfs-chapter17.php
===================================================================
--- scripts/blfs-chapter17.php	(révision 16921)
+++ scripts/blfs-chapter17.php	(révision 16922)
@@ -12,12 +12,12 @@
 $ignores = array();
 $ignores[ 'rpcnis-headers' ] = '';
 
-//$current="libnl-doc";
+//$current="serf";
 
 $regex = array();
 $regex[ 're-alpine' ] = "/^.*Download re-alpine-(\d[\d\.]+\d).tar.*$/";
 $regex[ 'w3m'       ] = "/^.*Download w3m-(\d[\d\.]+\d).tar.*$/";
-$regex[ 'serf'      ] = "/^.*serf-([\d\.]*).tar.*$/";
+$regex[ 'serf'      ] = "/^.*Serf is ([\d\.]+\d).*$/";
 $regex[ 'neon'      ] = "/^.*Source code: neon-(\d[\d\.]*).tar.*$/";
 $regex[ 'geoclue'   ] = "/^.*geoclue-(\d[\d\.]+).tar.*$/";
 $regex[ 'libevent'  ] = "/^.*release-(\d[\d\.]*)-stable.*$/";
@@ -42,7 +42,7 @@
 
    array( 'pkg'     => 'serf',
           'match'   => '^.*$',
-'replace' => "https://code.google.com/p/serf" ),
+          'replace' => "http://serf.apache.org/download" ),
 
    array( 'pkg'     => 'libtirpc',
           'match'   => '^.*$',

