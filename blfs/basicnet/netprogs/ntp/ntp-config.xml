<sect2>
<title>Configurer <application><acronym>NTP</acronym></application></title>

<sect3><title>Fichiers de configuration</title>
<para><filename>/etc/ntp.conf</filename></para>
</sect3>

<sect3><title>Informations de configuration</title>

<para>Le fichier de configuration suivant définit différents serveurs
<acronym>NTP</acronym> de strate 2 avec un accès ouvert aux différents
continents. Il crée aussi un fichier drift où <command>ntpd</command> stocke la
différence de temps. Comme la documentation incluse avec le paquetage est légère,
visitez le site web <acronym>NTP</acronym> à
<ulink url="http://www.ntp.org/"/> for more information.</para>

<screen><userinput><command>cat &gt; /etc/ntp.conf &lt;&lt; "EOF"</command>
# Afrique
server tock.nml.csir.co.za

# Asie
server ntp.shim.org

# Australie
server ntp.saard.net
        
# Europe
server ntp.tuxfamily.net

# Amérique du Nord
server clock.psu.edu

driftfile /var/cache/ntp.drift
<command>EOF</command></userinput></screen>
</sect3>

<sect3><title>Synchroniser le temps</title>

<para>Il existe deux options. la première est de lancer <command>ntpd</command>
continuellement et de le permettre de synchroniser le temps de manière gradué.
L'autre option est de lancer <command>ntpd</command> périodiquement (avec cron)
et de mettre à jour l'heure à chaque fois que <command>ntpd</command> est lancé.</para>

<para>Si vous choisissez la première option, alors installez le script
d'initialisation <filename>/etc/rc.d/init.d/ntp</filename> inclus dans le
paquetage <xref linkend="intro-important-bootscripts"/>.</para>
  
<screen><userinput><command>make install-ntp</command></userinput></screen>

<para>Si vous préférez lancer ntpd périodiquement, ajoutez la commande suivante
au <filename>crontab</filename> de root&nbsp;:</para>
<screen><userinput>ntpd -q</userinput></screen>

</sect3>

</sect2>
