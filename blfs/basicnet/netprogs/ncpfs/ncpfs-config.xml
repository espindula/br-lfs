<?xml version="1.0" encoding="ISO-8859-1"?>
<sect2>
<title>Configurer <application>NCPFS</application></title>

<sect3><title>Fichiers de configuration</title>
<para><filename>~/.nwclient</filename></para>
</sect3>

<sect3><title>Informations de configuration</title>

<para>Le fichier de configuration <filename>~/.nwclient</filename> devrait être
placé dans le répertoire personnel de chaque utilisateur qui souhaite utiliser
<application>ncpfs</application>. Les droits sur ce fichier devraient être 600,
pour des raisons de sécurité évidentes. Le fichier de configuration doit contenir
une seule ligne par serveur que l'utilisateur prendra en compte. Chaque ligne
doit contenir le nom du serveur, le nom de l'utilisateur et, en option, le mot
de passe. Ci-dessous se trouve un fichier <filename>.nwclient</filename>
d'exemple.</para>

<screen><userinput># Début exemple pour fichier de configuration <filename>~/.nwclient</filename>

Serveur1/Utilisateur1 MotDePasse
Serveur2/Utilisateur1 
Serveur2/Invité1 -

# Fin exemple pour fichier de configuration .nwclient</userinput></screen>

<para>La syntaxe du fichier <filename>.nwclient</filename> est simple,
<userinput>nom_serveur/nom_utilisateur mot_de_passe</userinput>. Faites
particulièrement attention lors de la création ou de l'édition de ce fichier
car les outils clients sont très pointilleux sur la syntaxe. Il devrait toujours
y avoir un espace immédiatement après le nom de l'utilisateur. Si cet espace est
substitué par une tabulation ou plusieurs espaces, vous n'obtiendrez pas les
résultats attendus lors de la tentative d'utilisation des outils
<application>NCPFS</application>. Si aucun mot de passe n'est fourni, les outils
clients demanderont un mot de passe si nécessaire. Si aucun mot de passe n'est
demandé, par exemple suite à l'utilisation d'un compte invité, un simple '-' 
devrait être placé au lieu du mot de passe.</para>

<para>Il est à noter que ncpmount n'a pas pour but de monter des volumes
individuels parce que chaque point de montage crée une connexion client
séparée vers le serveur Novell. Monter chaque volume individuel séparément ne
devrait pas être conseillé car monter tous les volumes sur un serveur avec un
seul point de montage utilise une seule connexion client.</para>

<para>Si vous avez besoin de configurer le protocole <acronym>IPX</acronym> au
démarrage, vous pouvez installer le script de service réseau
<filename>/etc/sysconfig/network-devices/services/ipx</filename> inclus dans le
paquetage <xref linkend="intro-important-bootscripts"/>.</para>

<screen><userinput><command>make install-service-ipx</command></userinput></screen>

<para>Installez ensuite le script de configuration,
<filename>ifconfig.ipx0</filename>. La configuration suppose que
<acronym>IPX</acronym> sera configuré pour <emphasis role="strong">eth0</emphasis> 
et que le type de frame réseau est 802.2. Vous devez confirmer que ce sont les bons
paramètres et les modifier si nécessaire.</para>

<para><emphasis>Note</emphasis>&nbsp;: ceci va écraser tout fichier existant.</para>

<screen><userinput><command>cat &gt; /etc/sysconfig/network-devices/ifconfig.ipx0 &lt;&lt; "EOF"</command>
ONBOOT=yes
SERVICE=ipx
IPXDEV=eth0
FRAME=802.2
<command>EOF</command></userinput></screen>

</sect3>

</sect2>
