<sect2>
<title>Installation de <application>PostgreSQL</application></title>

<para>Installez <application>PostgreSQL</application> avec les commandes
suivantes: </para>

<screen><userinput><command>./configure --prefix=/usr &amp;&amp;
make &amp;&amp;
make install</command></userinput></screen>

<note><para>Si vous mettez à jour un système existant et que vous allez installer
les nouveaux fichiers sur les anciens, alors vous devriez faire une sauvegarde,
arrêter le serveur et suivre les instructions dans <ulink
url="http://www.postgresql.org/docs/7.4/static/install-upgrading.html">la
documentation officielle de <application>Postgre<acronym>SQL</acronym></application>
</ulink>.</para></note>

<para>Initialisez un groupe de base de données avec les commandes
suivantes&nbsp;:</para>

<screen><userinput><command>mkdir -p /var/pgsql/data &amp;&amp;
useradd -d /var/pgsql/data postgres &amp;&amp;
chown postgres /var/pgsql/data &amp;&amp;
su - postgres -c '/usr/bin/initdb -D /var/pgsql/data'</command></userinput></screen>

<para>Démarrez le serveur de base de données avec la commande
suivante&nbsp;:</para>

<screen><userinput><command>su - postgres -c '/usr/bin/postmaster -D /var/pgsql/data > \
    /var/pgsql/data/logfile 2&gt;&amp;1 &amp;'</command></userinput></screen>

<para>Créez une base de données et vérifiez l'installation&nbsp;:</para>

<screen><userinput><command>su - postgres -c '/usr/bin/createdb test' &amp;&amp;
echo "create table t1 ( name varchar(20), state_province varchar(20) );" \
    | (su - postgres -c '/usr/bin/psql test ') &amp;&amp;
echo "insert into t1 values ('Billy', 'NewYork');" \
    | (su - postgres -c '/usr/bin/psql test ') &amp;&amp;
echo "insert into t1 values ('Evanidus', 'Quebec');" \
    | (su - postgres -c '/usr/bin/psql test ') &amp;&amp;
echo "insert into t1 values ('Jesse', 'Ontario');" \
    | (su - postgres -c '/usr/bin/psql test ') &amp;&amp;
echo "select * from t1;" | (su - postgres -c '/usr/bin/psql test')</command></userinput></screen>

</sect2>
