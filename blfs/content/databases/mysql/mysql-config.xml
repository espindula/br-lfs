<?xml version="1.0" encoding="ISO-8859-1"?>
<sect2>
<title>Configurer MySQL</title>

<sect3>
<title>Fichiers de <application>My<acronym>SQL</acronym></application></title>

<para><filename>/etc/my.cnf</filename>, <filename>~/.my.cnf</filename></para>

</sect3>

<sect3>
<title>Informations de configuration</title>

<para>Il y a de nombreux fichiers de configuration par défaut
disponible dans <filename class="directory">/usr/share/mysql</filename> que
vous pouvez utiliser. Créez <filename>/etc/my.cnf</filename> en utilisant la
commande suivante&nbsp;:</para>

<screen><userinput><command>cp /usr/share/mysql/my-medium.cnf /etc/my.cnf</command></userinput></screen>

<para>Maintenant, vous pouvez installer la base de données et changer
le propriétaire en un utilisateur et groupe non privilégié.</para>

<screen><userinput><command>mysql_install_db &amp;&amp;
chown -R mysql:mysql /var/lib/mysql</command></userinput></screen>

<para>Une configuration plus avancée requiert que le serveur mysql soit
lancé:</para>

<screen><userinput><command>mysqld_safe --user=mysql 2&gt;&amp;1 &gt;/dev/null &amp;</command></userinput></screen>


<para>Une installation par défaut n'attribue pas de mot de passe pour
l'administrateur. Donc, utilisez la commande suivante pour en ajouter un.
Remplacez <replaceable>[nouveau_motdepasse]</replaceable> par le votre.
</para>

<!--

<screen><userinput><command>mysql -uroot mysql</command></userinput>
Welcome to the MySQL monitor.  Commands end with ; or \g.
Your MySQL connexion id is 2 to server version: 3.23.51-log

Type 'help;' or '\h' for help. Type '\c' to clear the buffer.

<prompt>mysql&gt;</prompt> <userinput><command>UPDATE user SET password=password('<replaceable>nouveau_motdepasse</replaceable>') WHERE user='root';</command></userinput>
Query OK, 2 rows affected (0.00 sec)
Rows matched: 2  Changed: 2  Warnings: 0

<prompt>mysql&gt;</prompt> <userinput><command>FLUSH PRIVILEGES;</command></userinput>
Query OK, 0 rows affected (0.00 sec)

<prompt>mysql&gt;</prompt> <userinput><command>EXIT;</command></userinput>
bye
</screen>

-->

<screen><userinput><command>mysqladmin -u root password <replaceable>[nouveau_motdepasse]</replaceable></command></userinput></screen>

<para>La configuration du serveur est maintenant terminée. Arrêtez le serveur
en utilisant la commande suivante&nbsp;:</para>

<screen><userinput><command>mysqladmin -p shutdown</command></userinput></screen>

<para>Installez le script d'initialisation <filename>/etc/rc.d/init.d/mysql</filename>
inclus dans le paquetage <xref linkend="intro-important-bootscripts"/>.</para>

<screen><userinput><command>make install-mysql</command></userinput></screen>

</sect3>

</sect2>

