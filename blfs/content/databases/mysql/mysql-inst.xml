<sect2>
<title>Installation de mysql</title>

<para>Pour des raisons de sécurité, lancer le serveur en tant qu'utilisateur
et groupe non privilégiés est fortement encouragé:</para>

<screen><userinput>groupadd mysql &amp;&amp;
useradd -c mysql -d /dev/null -g mysql -s /bin/false mysql</userinput></screen>

<para>Construire et installer mysql en lançant les commandes suivantes:</para>

<screen><userinput>cp configure configure.old &amp;&amp;
sed -e "s%mysql-test/Makefile%%" -e "s% mysql-test%%" configure.old &gt; configure &amp;&amp;
./configure --prefix=/usr \
            --sysconfdir=/etc \
            --libexecdir=/usr/sbin \
            --localstatedir=/var/lib/mysql \
            --enable-thread-safe-client \
            --without-debug \
            --without-bench &amp;&amp;
make &amp;&amp;
make install</userinput></screen>

</sect2>
