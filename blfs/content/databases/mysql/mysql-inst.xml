<sect2>
<title>Installation de <application>My<acronym>SQL</acronym></application></title>

<para>Pour des raisons de sécurité, lancer le serveur en tant qu'utilisateur
et groupe non privilégiés est fortement encouragé:</para>

<screen><userinput><command>groupadd mysql &amp;&amp;
useradd -c mysql -d /dev/null -g mysql -s /bin/false mysql</command></userinput></screen>

<para>Construire et installer
<application>My<acronym>SQL</acronym></application> en lançant les commandes
suivantes:</para>

<screen><userinput><command>./configure --prefix=/usr --sysconfdir=/etc \
    --libexecdir=/usr/sbin --localstatedir=/var/lib/mysql \
    --enable-thread-safe-client --enable-local-infile \
    --enable-assembler --without-debug --without-bench &amp;&amp;
make testdir=/usr/lib/mysql/mysql-test &amp;&amp;
make testdir=/usr/lib/mysql/mysql-test install</command></userinput></screen>

<para>Les paquetages dépendant de <application>MySQL</application> ont besoin
des bibliothèques partagées à l'exécution&nbsp;:</para>

<screen><userinput><command>cd /usr/lib &amp;&amp;
ln -sf mysql/libmysqlclient{,_r}.so* .</command></userinput></screen>

</sect2>
