<sect2>
<title>Installation de openoffice</title>

<para>Notez qu'openoffice pose problème avec
<application>bison-1.875</application>. Vous pouvez soit télécharger
<application>bison-1.75</application> soit installer
<application>bison-1.75</application> en indiquant un autre préfixe
(c'est-à-dire <filename>/opt/bison-1.75</filename>) et placez
<filename class="directory">$bison_prefix/bin</filename> en tant que première
entrée de <envar>PATH</envar>.</para>

<para>Appliquez les correctifs téléchargés et copiez les fichiers gpc.</para>

<para><screen><userinput>mv ../gpc231/* external/gpc &amp;&amp;
rm -rf ../gpc231 &amp;&amp;
for p in ../openoffice-&openoffice-version;-*.patch
do patch -Np1 -i $p
done</userinput></screen></para>

<para>Si vous voulez optimiser la construction, éditez
<filename>solenv/inc/unxlngi4.mk</filename> et ajoutez les options
d'optimisation à la variable <envar>CFLAGSOPT</envar>. Quelques utilisateurs
ont rapporté avoir des problèmes avec -fomit-frame-pointer. Le meilleur choix
est de ne pas utiliser d'optimisation personnalisé. La commande suivante
supprime une option -mcpu incorrecte dans le fichier ci-dessus.</para>

<para><screen><userinput>cd solenv/inc &amp;&amp;
cp unxlngi3.mk unxlngi3.mk.orig &amp;&amp;
sed -e "s:^CFLAGSOPT=.*:CFLAGSOPT=${CFLAGS}:g" \
unxlngi3.mk.orig &gt; unxlngi3.mk &amp;&amp;
cp unxlngi4.mk unxlngi4.mk.orig &amp;&amp;
sed -e "s:^CFLAGSOPT=.*:CFLAGSOPT=${CFLAGS}:g" \
-e "s:\-mcpu=pentiumpro::" \
unxlngi4.mk.orig &gt; unxlngi4.mk &amp;&amp;
cd ../..</userinput></screen></para>

<para>Configurez openoffice en utilisant les commandes suivantes. Vous pouvez
ajouter des langages suivant vos préférences. Si un composant particulier n'est
pas disponible dans la langue de votre choix, il sera par défaut en anglais.
</para>

<para><screen><userinput>cd config_office/ &amp;&amp;
./configure --with-lang=ENUS --with-stlport4-home=/usr &amp;&amp;
cd .. &amp;&amp;
source LinuxIntelEnv.Set.sh</userinput></screen></para>

<para>Compilez openoffice en utilisant les commandes suivantes.</para>

<para><screen><userinput>./bootstrap &amp;&amp;
dmake</userinput></screen></para>

<para>Si vous avez téléchargé les fichiers compressés d'aide localisé, vous
aurez besoin de les décompresser dans le répertoire approprié comme
mentionné ci-dessous et de recréer l'ensemble d'installation.</para>

<para><screen><userinput>cd solver/641/unxlngi4.pro/pck &amp;&amp;
for i in $(ls ../../../../../helpcontent_*_unix.tgz)
do
tar -xvzf $i
done &amp;&amp;
cd ../../../../instsetoo &amp;&amp;
rm -rf unxlngi4.pro &amp;&amp;
dmake &amp;&amp;
cd ..</userinput></screen></para>

<para>Installez OpenOffice en utilisant les commandes suivantes. Les commandes
suivantes installent le nécessaire pour la langue anglaise. Pour installer une
version localisée, remplacez le 01 par le code international téléphonique de
votre pays. Notez qu'un affichage X est nécessaire lors de l'installation bien
qu'aucune fenêtre X ne sera visible. Vous pouvez utiliser Xvfb si vous compilez
à partir de la console.</para>

<para><screen><userinput>cd instsetoo/unxlngi4.pro/01/normal &amp;&amp;
cp install install.orig &amp;&amp;
sed -e "s:^oo_home=.*:oo_home=openoffice:" install.orig > install &amp;&amp;
./install --prefix=/opt &amp;&amp;
for appl in swriter scalc sdraw simpress smath soffice
do ln -sf /opt/openoffice/program/$appl /usr/bin/$appl
done</userinput></screen></para>

</sect2>

