<sect2>
<title>Explication des commandes</title>

<para><command>--localstatedir=/var/lib</command> : Cette option place les
fichiers de scrollkeeper dans <filename>/var/lib/scrollkeeper</filename> au lieu
de <filename>$GNOME_PREFIX/var/scrollkeeper</filename>.</para>

<para><command>--with-ssl</command> : Modifie la configuration de Balsa qui
ne propose pas par défaut le support de SSL.</para>

<para><command>--sysconfdir=/etc/gnome</command> : Cette option place les
fichiers de configuration dans <filename>/etc/gnome</filename> au lieu de
<filename>$GNOME_PREFIX/etc</filename>.</para>

<para><command>--with-ldap</command> : Change la valeur par défaut pour
activer le support de <acronym>LDAP</acronym> s'il est disponible.</para>

<para><command>--with-gpgme</command> : Change la valeur par défaut pour
activer le support de <acronym>GPG</acronym> si 'GnuPG made easy'
(<acronym>GPGME</acronym>) est installé.</para>

</sect2>

