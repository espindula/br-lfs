<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY firefox-download-http "http://releases.mozilla.org/pub/mozilla.org/firefox/releases/&firefox-version;/source/firefox-&firefox-version;.source.tar.bz2">
  <!ENTITY firefox-download-ftp  "ftp://ftp.mozilla.org/pub/mozilla.org/firefox/releases/&firefox-version;/source/firefox-&firefox-version;.source.tar.bz2">
  <!ENTITY firefox-md5sum        "b7d09b082e88a74860a51a8e04e29333">
  <!ENTITY firefox-size          "67 Mio">
  <!ENTITY firefox-buildsize     "3 Gio">
  <!ENTITY firefox-time          "24 SBU (indépendant) ou 0.3 SBU (sur Xulrunner)">
]>

<sect1 id="firefox" xreflabel="Firefox-&firefox-version;">
  <?dbhtml filename="firefox.html" ?>

  <sect1info>
    <othername>$LastChangedBy: randy $</othername>
    <date>$Date: 2011-01-09 17:51:09 +0100 (dim 09 jan 2011) $</date>
  </sect1info>

  <title>Firefox-&firefox-version;</title>

  <indexterm zone="firefox">
    <primary sortas="a-Firefox">Firefox</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introduction à Firefox</title>

    <para><application>Firefox</application> est un navigateur autonome basé sur le code
    <application>Mozilla</application>.</para>

    &lfs70_checked;

    <para>&lfssvn_checked;20101029&lfssvn_checked2;</para>

    <bridgehead renderas="sect3">Informations sur le paquet</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>Téléchargement (HTTP)&nbsp;: <ulink url="&firefox-download-http;"/></para>
      </listitem>
      <listitem>
        <para>Téléchargement (FTP)&nbsp;: <ulink url="&firefox-download-ftp;"/></para>
      </listitem>
      <listitem>
        <para>Somme de contrôle MD5 du téléchargement&nbsp;: &firefox-md5sum;</para>
      </listitem>
      <listitem>
        <para>Taille du téléchargement&nbsp;: &firefox-size;</para>
      </listitem>
      <listitem>
        <para>Estimation de l'espace disque requis&nbsp;: &firefox-buildsize;</para>
      </listitem>
      <listitem>
        <para>Estimation du temps de construction&nbsp;: &firefox-time;</para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Dépendances de Firefox</bridgehead>

    <bridgehead renderas="sect4">Recommandées</bridgehead>
    <para role="recommended"><xref linkend="xulrunner"/> qui partage toutes ces
    dépendances, ou prur un firefox autonome, les dépendances requises et facultatives suivantes&nbsp;:</para>

    <bridgehead renderas="sect4">Requises</bridgehead>
    <para role="required"><xref linkend="alsa-lib"/>,
    <xref linkend="gtk2"/>,
    <xref linkend="libidl"/> and
    <xref linkend="zip"/></para>

    <para>Remarque&nbsp;: <application>libjpeg</application> et
    <application>libpng</application> (corrigé pour le support apng) devraient
    avoir été installés avant <application>gdk-pixbuf</application> et exister
    sur votre système. Si, pour une quelconque région bizarre, vous ne les aviez
    pas installés, vous devriez supprimer les options
    <option>--with-system-jpeg</option> et <option>--with-system-png</option> de
    du fichier
    <filename>mozconfig</filename> créé ci-dessous.</para>

    <bridgehead renderas="sect4">Facultatives</bridgehead>
    <para role="optional"><xref linkend="dbus-glib"/>,
    <xref linkend="startup-notification"/>, <xref linkend="sqlite"/>,
    <ulink url="http://www.webmproject.org/code/">libvpx</ulink>,
    <ulink url="http://sourceforge.net/projects/hunspell/">Hunspell</ulink>,
    <xref linkend="doxygen"/>,
    <xref linkend="gnome-vfs"/> and <xref linkend="libgnomeui"/> (pour
    l'intégration de gnome), <xref linkend="libnotify"/>, <xref linkend="nss"/>,
    <xref linkend="unzip"/>, <xref linkend="wireless_tools"/>,
    <ulink url="http://www.valgrind.org/">Valgrind</ulink> (seulement pour
    tester le code jemalloc) et <xref linkend="wget"/></para>

    <para condition="html" role="usernotes">Notes utilisateur&nbsp;:
    <ulink url="&blfs-wiki;/firefox"/></para>

  </sect2>

  <sect2 role="installation">
    <title>Installation de Firefox</title>

    <para>La configuration de <application>Firefox</application> se
    fait en créant un fichier <filename>mozconfig</filename> contenant les options de configuration désirées. Un
    <filename>mozconfig</filename> par défaut est créé ci-dessous. Pour voir toute
    la liste des options de configuration disponibles, 
    (et une description abrégée de chacune), faites
    <command>./configure --help</command>. <!--If you are not building against
    <application>Xulrunner</application> you must comment the 'Xulrunner'
    items. --> Il se peut que vous vouliez aussi relire tout le fichier et
    décommenter d'autres options souhaitées. Créez le fichier en lançant la
    commande suivante&nbsp;:</para>

<screen><?dbfo keep-together="auto"?><userinput>cat &gt; mozconfig &lt;&lt; "EOF"
<literal># Si vous avez une machine multic½urs, vous pouvez accélérer la
# construction en lançant plusieurs tâches en même temps, mais vous n'avez qu'un
# coeur, effacez cette ligne :
mk_add_options MOZ_MAKE_FLAGS="-j4"

# Si vous avez installé DBus-Glib vous pouvez effacer cette option :
ac_add_options --disable-dbus

# Si vous avez installé wireless-tools vous pouvez effacer cette option :
ac_add_options --disable-necko-wifi

# Décommentez ceci si vous les avez installé :
# ac_add_options --enable-system-sqlite
# ac_add_options --enable-system-lcms
# ac_add_options --with-system-libvpx
# ac_add_options --enable-startup-notification

mk_add_options MOZ_OBJDIR=@TOPSRCDIR@/firefox-build-dir
ac_add_options --enable-official-branding
ac_add_options --prefix=/usr
ac_add_options --enable-application=browser
ac_add_options --enable-pthreads
ac_add_options --enable-system-ffi
ac_add_options --enable-system-jpeg
ac_add_options --enable-system-png
ac_add_options --enable-system-cairo
ac_add_options --enable-system-zlib
ac_add_options --enable-shared
ac_add_options --disable-static
ac_add_options --disable-debug
ac_add_options --disable-crashreporter
ac_add_options --disable-installer
ac_add_options --disable-updater
ac_add_options --disable-tests</literal>
EOF</userinput></screen>

<!--
# Point to the Xulrunner libraries - comment these for a standalone browser.
ac_add_options - -with-system-libxul
ac_add_options - -with-libxul-sdk=/usr/lib/xulrunner-devel-&xulrunner-version;
-->

    <para>Compile <application>Firefox</application> by issuing the
    following commands:</para>

<screen><userinput>make -f client.mk</userinput></screen>

    <para>Ce paquet n'est pas fourni avec une suite de tests.</para>

    <para>Maintenant, en tant qu'utilisateur <systemitem class="username">root</systemitem> ,
    installez le paquet&nbsp;:</para>

<screen role="root"><userinput>make -C firefox-build-dir install &amp;&amp;
mkdir -p /usr/lib/mozilla/plugins &amp;&amp;
ln -sv ../mozilla/plugins /usr/lib/firefox-&firefox-version; &amp;&amp;
chown -R -v root:root /usr/{include,lib,share/idl}/firefox-&firefox-version; &amp;&amp;
chown -R -v root:root /usr/lib/firefox-devel-&firefox-version;</userinput></screen>
 
  </sect2>

  <sect2 role="commands">
    <title>Explication des commandes</title>

    <para><command>make -f client.mk ...</command>&nbsp;: Les produits Mozilla
    sont enpaquetés pour permettre l'utilisation d'un fichier de configuration qui peut
    être utilisé pour passer les paramètres de configuration à la
    commande <command>configure</command>. <command>make</command> utilise le
    fichier <filename>client.mk</filename> pour avoir la configuration initiale
    et régler les paramètres et la configuration de départ.</para>

    <para><command>make -C firefox-build-dir install</command>&nbsp;: ceci
    lance 
    <command>make install</command> dans le dossier firefox-build-dir.</para>
    that /usr/lib/mozilla/plugins exists (if you installed

    <para><command>mkdir -p /usr/lib/mozilla/plugins</command>&nbsp;: ceci
    vérifie 
    <xref linkend="librsvg"/>, il était probablement déjà là).</para>

    <para><command>ln -sv ... /usr/lib/firefox-&firefox-version;</command>&nbsp;:
    ceci crée un lien symbolique vers /usr/lib/mozilla/plugins.</para>

  </sect2>

  <sect2 role="content">
    <title>Contenu</title>

    <segmentedlist>
      <segtitle>Programmes installés</segtitle>
      <segtitle>Bibliothèques installées</segtitle>
      <segtitle>Répertoires installés</segtitle>

      <seglistitem>
        <seg>firefox</seg>
        <seg>De nombreuses bibliothèques, composants du navigateur, des plugins, des
        extensions et des modules d'aide installés dans 
        <filename class='directory'>/usr/lib/firefox-&firefox-version;</filename></seg>
        <seg>/usr/include/firefox-&firefox-version;,
        /usr/lib/firefox-&firefox-version;,
        /usr/lib/firefox-devel-&firefox-version;, et
        /usr/share/idl/firefox-&firefox-version;</seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descriptions courtes</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="firefox-prog">
        <term><command>firefox</command></term>
        <listitem>
          <para>est un navigateur internet gtk2 qui utilise le moteur
          d'affichage gecko.</para>

          <indexterm zone="firefox firefox-prog">
            <primary sortas="b-firefox">firefox</primary>
          </indexterm>
        </listitem>
      </varlistentry>
   </variablelist>

  </sect2>

</sect1>
