<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY firefox-download-http "http://releases.mozilla.org/pub/mozilla.org/firefox/releases/&firefox-version;/source/firefox-&firefox-version;.source.tar.bz2">
  <!ENTITY firefox-download-ftp  "ftp://ftp.mozilla.org/pub/mozilla.org/firefox/releases/&firefox-version;/source/firefox-&firefox-version;.source.tar.bz2">
  <!ENTITY firefox-md5sum        "7cf2bd379792a9b232267c6a79680566">
  <!ENTITY firefox-size          "70.8 MB">
  <!ENTITY firefox-buildsize     "3 GB (33 MB installé)">
  <!ENTITY firefox-time          "24 SBU (ou 0.3 si vous construisez contre un Xulrunner installé)">
]>

<sect1 id="firefox" xreflabel="Firefox-&firefox-version;">
  <?dbhtml filename="firefox.html" ?>

  <sect1info>
    <othername>$LastChangedBy: andy $</othername>
    <date>$Date: 2012-01-11 15:22:48 +0100 (mer. 11 janv. 2012) $</date>
  </sect1info>

  <title>Firefox-&firefox-version;</title>

  <indexterm zone="firefox">
    <primary sortas="a-Firefox">Firefox</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introduction à Firefox</title>

    <para><application>Firefox</application> est un navigateur autonome basé sur le code
    <application>Mozilla</application>.</para>

    &lfs70_checked;

    <bridgehead renderas="sect3">Informations sur le paquet</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>Téléchargement (HTTP)&nbsp;: <ulink url="&firefox-download-http;"/></para>
      </listitem>
      <listitem>
        <para>Téléchargement (FTP)&nbsp;: <ulink url="&firefox-download-ftp;"/></para>
      </listitem>
      <listitem>
        <para>Somme de contrôle MD5 du téléchargement&nbsp;: &firefox-md5sum;</para>
      </listitem>
      <listitem>
        <para>Taille du téléchargement&nbsp;: &firefox-size;</para>
      </listitem>
      <listitem>
        <para>Estimation de l'espace disque requis&nbsp;: &firefox-buildsize;</para>
      </listitem>
      <listitem>
        <para>Estimation du temps de construction&nbsp;: &firefox-time;</para>
      </listitem>
    </itemizedlist>
    
    <bridgehead renderas="sect3">Chargement supplémentaire</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>Correctif requis si vous avez installé libpng-1.5.6 ou supérieur&nbsp;: <ulink
        url="&patch-root;/firefox-&firefox-version;-libpng-1.5-1.patch"/></para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Dépendances de Firefox</bridgehead>

    <bridgehead renderas="sect4">Requises</bridgehead>
    <para role="required"><xref linkend="alsa-lib"/>, <xref linkend="gtk2"/>,
    <xref linkend="zip"/> et <xref linkend="unzip"/>.</para>

    <para>Remarque&nbsp;: <application>libjpeg</application> et
    <application>libpng</application> (corrigé pour le support apng) devraient
    avoir été installés avant <application>gdk-pixbuf</application> et exister
    sur votre système. Si, pour une quelconque région bizarre, vous ne les aviez
    pas installés, vous devriez supprimer les options
    <option>--with-system-jpeg</option> et <option>--with-system-png</option> de
    du fichier
    <filename>mozconfig</filename> créé ci-dessous.</para>
 
    <bridgehead renderas="sect4">Recommandées</bridgehead>
    <para role="recommended"><xref linkend="yasm"/>
    ou <xref linkend="libvpx"/> (pour que <application>Firefox</application> 
    lise des vidéos webm).</para>

    <bridgehead renderas="sect4">Facultatives</bridgehead>
    <para role="optional"><xref linkend="dbus-glib"/>,
    <xref linkend="startup-notification"/>, <xref linkend="sqlite"/>,
    <ulink url="http://sourceforge.net/projects/hunspell/">Hunspell</ulink>,
    <xref linkend="doxygen"/>, <xref linkend="gnome-vfs"/> et
    <xref linkend="libgnomeui"/> (pour l'intégration à gnome),
    <xref linkend="libnotify"/>, <xref linkend="nspr"/>, <xref linkend="nss"/>,
    <xref linkend="xulrunner"/> (se lier à un xulrunner installé allège
    Firefox d'environ 32 Mior, ce qui est peu par rapport à la taille de
    xulrunner), <xref linkend="wireless_tools"/>,
    <ulink url="http://www.valgrind.org/">Valgrind</ulink> (seulement pour
    tester le code jemalloc) et <xref linkend="wget"/></para>

    <para condition="html" role="usernotes">Notes utilisateur&nbsp;:
    <ulink url="&blfs-wiki;/firefox"/></para>

  </sect2>

  <sect2 role="installation">
    <title>Installation de Firefox</title>

    <para>La configuration de <application>Firefox</application> se
    fait en créant un fichier <filename>mozconfig</filename> contenant les options de configuration désirées. Un
    <filename>mozconfig</filename> par défaut est créé ci-dessous. Pour voir toute
    la liste des options de configuration disponibles, 
    (et une description abrégée de chacune), faites
    <command>./configure --help</command>. Il se peut que vous vouliez aussi 
    relire tout le fichier et
    décommenter d'autres options souhaitées. Créez le fichier en lançant la
    commande suivante&nbsp;:</para>

<screen><?dbfo keep-together="auto"?><userinput>patch -p1 &lt; ../firefox-&firefox-version;-libpng-1.5-1.patch &amp;&amp;
cat &gt; mozconfig &lt;&lt; "EOF"
# Si vous avez une machine multicoeurs, vous pouvez accélérer la
# construction en lançant plusieurs tâches en même temps, mais si vous n'avez qu'un
# coeur, effacez cette ligne&nbsp;:
mk_add_options MOZ_MAKE_FLAGS="-j4"

# Si vous avez installé Yasm effacez cette option&nbsp;:
ac_add_options --disable-webm

# Si vous avez installé DBus-Glib effacez cette option&nbsp;:
ac_add_options --disable-dbus

# Si vous avez installé wireless-tools effacez cette option&nbsp;:
ac_add_options --disable-necko-wifi

# Si vous avez installé libnotify effacez cette option&nbsp;:
ac_add_options --disable-libnotify

# Si vous avez installé xulrunner, décommentez ces deux lignes&nbsp;:
# ac_add_options --with-system-libxul
# ac_add_options --with-libxul-sdk=/usr/lib/xulrunner-devel-&xulrunner-version;

# Décommentez ceci si vous les avez installé&nbsp;:
# ac_add_options --with-system-nspr
# ac_add_options --with-system-nss
# ac_add_options --enable-system-sqlite
# ac_add_options --with-system-libvpx
# ac_add_options --enable-startup-notification

mk_add_options MOZ_OBJDIR=@TOPSRCDIR@/firefox-build-dir
ac_add_options --enable-official-branding
ac_add_options --prefix=/usr

# Le reste de ces options n'ont pas d'effet si vous construisez contre un
# xulrunner déjà installé&nbsp;:
ac_add_options --enable-official-branding
ac_add_options --enable-pthreads
ac_add_options --enable-system-ffi
ac_add_options --enable-system-jpeg
ac_add_options --enable-system-png
ac_add_options --enable-system-cairo
ac_add_options --enable-system-zlib
ac_add_options --enable-shared
ac_add_options --disable-static
ac_add_options --disable-debug
ac_add_options --disable-crashreporter
ac_add_options --disable-installer
ac_add_options --disable-updater
ac_add_options --disable-tests
EOF</userinput></screen>

    <para>Compilez <application>Firefox</application> en lançant les commandes
    suivantes&nbsp;:</para>

<screen><userinput>sed -i 's# ""##' browser/base/Makefile.in &amp;&amp;
make -f client.mk</userinput></screen>

    <para>Si vous <emphasis>n'avez pas</emphasis> lié
    <application>Firefox</application> à un <application>Xulrunner</application>
     installé&nbsp;:</para>

<screen><userinput>make -C firefox-build-dir/browser/installer</userinput></screen>

    <para>Ce paquet n'est pas fourni avec une suite de tests.</para>

    <para>Maintenant, si vous <emphasis>n'avez pas</emphasis> lié
    <application>Firefox</application> à un <application>Xulrunner</application>
    installé, en tant qu'utilisateur <systemitem class="username">root</systemitem> ,
    installez le paquet&nbsp;:</para>

<screen role="root"><userinput>mkdir /usr/lib/firefox-&firefox-version; &amp;&amp;
tar -xvf firefox-build-dir/dist/firefox-*.tar.bz2 -C /usr/lib/firefox-&firefox-version; --strip-components=1 &amp;&amp;
ln -sfv ../lib/firefox-&firefox-version;/firefox /usr/bin/firefox &amp;&amp;
mkdir -p /usr/lib/mozilla/plugins &amp;&amp;
ln -sv ../mozilla/plugins /usr/lib/firefox-&firefox-version; &amp;&amp;
chown -R -v root:root /usr/lib/firefox-&firefox-version;</userinput></screen>

    <para>Si vous avez installé <application>Firefox</application> lié à
    un <application>Xulrunner</application> déjà installé, en tant qu'utilisateur
    <systemitem class="username">root</systemitem>&nbsp;:</para>

<screen role="root"><userinput>make -C firefox-build-dir install &amp;&amp;
rm /usr/bin/firefox &amp;&amp;
cat &gt; /usr/bin/firefox &lt;&lt; "EOF" &amp;&amp;
#!/bin/bash
/usr/lib/xulrunner-&xulrunner-version;/xulrunner /usr/lib/firefox-&firefox-version;/application.ini "${@}"
EOF
chmod 755 /usr/bin/firefox &amp;&amp;
mkdir -p /usr/lib/mozilla/plugins &amp;&amp;
ln -sv ../mozilla/plugins /usr/lib/firefox-&firefox-version; &amp;&amp;
chown -R -v root:root /usr/lib/firefox-&firefox-version;</userinput></screen>
 
  </sect2>

  <sect2 role="commands">
    <title>Explication des commandes</title>

    <para><command>sed -i 's# ""##' browser/base/Makefile.in</command>&nbsp;: Ceci
    supprime un caractère de contrôle non imprimable de la barre de titre.</para>

    <para><command>make -f client.mk ...</command>&nbsp;: Les produits Mozilla
    sont enpaquetés pour permettre l'utilisation d'un fichier de configuration qui peut
    être utilisé pour passer les paramètres de configuration à la
    commande <command>configure</command>. <command>make</command> utilise le
    fichier <filename>client.mk</filename> pour avoir la configuration initiale
    et régler les paramètres et la configuration de départ.</para>

    <para><command>make -C firefox-build-dir/browser/installer</command>&nbsp;: ceci
    crée une archive tar de <application>Firefox</application>.</para>

    <para><command>tar -xfv firefox-build-dir/dist ...</command>&nbsp;: Ceci
    déballe <application>Firefox</application> dans /usr/lib. L'option
    <option>--strip-components=1</option> supprime le répertoire
    &apos;firefox&apos; des noms de fichier, ce qui nous permet de le déballer
    dans un répertoire versionné. Déballer l'archive (au lieu d'exécuter make install)
    l'empêche d'installer /usr/lib/firefox-devel-&firefox-version;
    qui contient environ 430 Mio de fichiers. Si vous avez besoin d'un de ces
    fichiers, ils se trouvent dans <xref linkend="xulrunner"/></para>

    <para><command>make -C firefox-build-dir install</command>&nbsp;: Ceci
    lance
    <command>make install</command> dans firefox-build-dir.</para>

    <para><command>ln -sfv ... /usr/bin/firefox</command>&nbsp;: ceci met un
    lien symbolique vers l'exécutable <command>firefox</command> dans votre
    ${PATH}.</para>

    <para><command>mkdir -p /usr/lib/mozilla/plugins</command>&nbsp;: ceci
    vérifie que /usr/lib/mozilla/plugins existe.
    (si vous avez installé <xref linkend="librsvg"/>, il était probablement déjà là).</para>

    <para><command>ln -sv ... /usr/lib/firefox-&firefox-version;</command>&nbsp;:
    ceci crée un lien symbolique vers /usr/lib/mozilla/plugins.</para>

  </sect2>

  <sect2 role="configuration">
    <title>Configuration de Firefox</title>

    <para>Si vous avez enlevé l'option <option>--disable-webm</option> de votre
    <filename>mozconfig</filename>, votre <application>Firefox</application>
    lire la plupart des vidéos YouTube sans le plugin flash. Pour activer ceci,
    allez sur <ulink
   url="http://www.youtube.com/html5">http://www.youtube.com/html5</ulink>
    et cliquez sur &apos;Rejoindre le test HTML5&apos; (exige l'activation des
    cookies).</para>

  </sect2>

  <sect2 role="content">
    <title>Contenu</title>

    <segmentedlist>
      <segtitle>Programmes installés</segtitle>
      <segtitle>Bibliothèques installées</segtitle>
      <segtitle>Répertoires installés</segtitle>

      <seglistitem>
        <seg>firefox</seg>
        <seg>De nombreuses bibliothèques, composants du navigateur, des plugins, des
        extensions et des modules d'aide installés dans
        <filename class='directory'>/usr/lib/firefox-&firefox-version;</filename>.</seg>
        <seg>/usr/lib/firefox-&firefox-version;</seg>

      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descriptions courtes</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="firefox-prog">
        <term><command>firefox</command></term>
        <listitem>
          <para>est un navigateur internet gtk2 qui utilise le moteur
          d'affichage gecko.</para>
          <indexterm zone="firefox firefox-prog">
            <primary sortas="b-firefox">firefox</primary>
          </indexterm>
        </listitem>
      </varlistentry>
   </variablelist>

  </sect2>

</sect1>
