<sect2>
<title>Configurer Mozilla</title>

<para>Aucune configuration spécifique n'est requise tant que le binaire
<command>mozilla</command> se trouve dans le path de l'utilisateur. Si
<application>Mozilla</application> est installé dans un répertoire non standard,
alors créez un lien symbolique  vers le binaire <command>mozilla</command> à
partir du répertoire <filename class="directory">/usr/bin</filename>. Ceci
s'applique aussi à <application>Mozilla FireBird</application> et à
<application>Mozilla Thunderbird</application>.</para>

<para>Beaucoup d'applications recherchent <command>netscape</command>
lorsqu'elles ont besoin d'ouvrir un navigateur. Vous pouvez créer un lien
symbolique pour plus de facilité.</para>

<screen><userinput><command>ln -sf mozilla /usr/bin/netscape</command></userinput></screen>

<para>Pour installer les plugins variés de <application>Mozilla</application>,
référez-vous au <ulink url="http://plugindoc.mozdev.org/linux.html">projet
PluginDoc de Mozdev</ulink>.</para>

</sect2>

