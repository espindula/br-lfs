<sect1 id="postlfs-config-logon">
<?dbhtml filename="logon.html" dir="postlfs"?>
<title>/etc/issue (Personnaliser votre connection)</title>

<para>Lorsque vous démarrez pour la première fois votre nouveau système LFS,
l'écran de connection sera joli et texte (comme il doit être dans un système
dépouillé). Néanmoins, beaucoup de gens voudront que leur système affiche
quelques informations dans le message de connexion. Ceci peut se faire en
utilisant le fichier <filename>/etc/issue</filename>.</para>

<para>Le fichier <filename>/etc/issue</filename> est un fichier de texte qui
accepte aussi certaines séquences d'échappement (voir plus bas) pour
insérer des informations sur le système. Il existe aussi le fichier
<filename>issue.net</filename> pouvant être utilisé lors de connexions à
distance. Néanmoins, SSH va seulement l'utiliser si vous avez mis en place
l'option dans les fichiers de configuration et ne va <emphasis>pas</emphasis>
interpréter les séquences d'échappement indiquées ci-dessous.</para>

<para>Une des informations les plus communes que les gens veulent est d'effacer
l'écran à chaque connexion. Le moyen le plus simple pour y arriver est de placer
la séquence d'échappement "clear" dans /etc/issue. Un moyen simple est de faire
<userinput>clear > /etc/issue</userinput>. Ceci insérera le code d'échappement
au début du fichier <filename>/etc/issue</filename>. Notez que si vous faites
ceci, quand vous éditez le fichier, vous devez laisser le caractère ^[c sur la
première ligne.</para>

<para>Les séquences d'échappement suivantes sont reconnues par agetty (le
programme qui analyse habituellement <filename>/etc/issue</filename>). Cette
information provient de <userinput>man agetty</userinput> où vous pouvez
trouver des informations supplémentaires sur le processus de connexion.</para>

<para>Le fichier peut contenir certains codes d'échappement pour afficher des
informations variées. Toutes les séquences d'échappement consistent d'un
backslash (\) immédiatement suivi d'une des lettres expliquées ci-dessous
(donc \d dans <filename>/etc/issue</filename> inséreré la date courante).</para>

<para><screen>b   Insérer la vitesse de la ligne.
d   Insérer la date courante.
s   Insérer le nom du système, le nom du système d'exploitation.
l   Insérer le nom de la ligne tty courante.
m   Insérer l'identifieur de l'architecture de la machine, par exemple i486
n   Insérer le nom du noeud de la machine, aussi connu sous le nom de nom
    d'hote.
o   Insérer le nom de domaine de la machine.
r   Insérer le numéro de version du noyau, par exemple 2.4.16.
t   Insérer la date courante.
u   Insérer le nomre d'utilisateurs connectés en ce moment.
U   Insérer la phrase "1 user" ou "&lt;n&gt; users" où &lt;n&gt; est le nombre
    d'utilisateurs actuellement connectés.
v   Insérer la version de l'OS, par exemple la date de construction.</screen></para>

</sect1>

