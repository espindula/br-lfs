<sect1 id="postlfs-config-inputrc">
<?dbhtml filename="inputrc.html" dir="postlfs"?>
<title>/etc/inputrc</title>

<para>Inputrc réalise la configuration du clavier pour certaines situations. Ce
fichier est le fichier de démarrage utilisé par readlin - la bibliothèque
relative aux entrées utilisée par Bash et la plupart des autres shells.</para>

<para>Pour plus d'informations, voir <filename>info bash</filename> -- Noeud:
Readline Init file mais aussi <filename>info readline</filename>. Beaucoup de
choses peuvent être faite avec ce seul fichier rc.</para>

<para>La suite est un exemple de <filename>/etc/inputrc</filename> avec des
commentaires expliquant ce que font les différentes options.</para>

<para>Notez que les commentaires peuvent ne <emphasis>pas</emphasis> être sur
la même ligne que les commandes dans <filename>inputrc</filename>.</para>

<para><screen># Début /etc/inputrc

# S'assure que nous ne sortons pas tout sur une ligne
set horizontal-scroll-mode Off

# Active l'entrée 8bit
set meta-flag On 
set input-meta On

# Turns off 8th bit stripping
set convert-meta Off

# Conserve le 8ème bit pour l'affichage
set output-meta On

# rien, visible ou audible
set bell-style none

# Toutes les indications ci-dessous font correspondre une séquence d'échappement
# à une fonction readline spécifique

"\eOd": backward-word
"\eOc": forward-word

# pour la console linux
"\e[1~": beginning-of-line
"\e[4~": end-of-line
"\e[5~": beginning-of-history
"\e[6~": end-of-history
"\e[3~": delete-char
"\e[2~": quoted-insert

# pour xterm
"\eOH": beginning-of-line
"\eOF": end-of-line

# Fin /etc/inputrc</screen></para>

<para>Des valeurs globales sont configurées dans
<filename>/etc/inputrc</filename>. Les valeurs spécifiques aux utilisateurs vont
dans <filename>~/.inputrc</filename>. Ce fichier va écraser les valeurs
globales. La page précédente demande à Bash d'utiliser par défaut
<filename>/etc/inputrc</filename>. Si vous voulez que votre système utilise les
deux, il serait une bonne idée de placer un <filename>.inputrc</filename> par
défaut dans le répertoire <filename>/etc/skel</filename> pour qu'il puisse être
utilisé par les nouveaux utilisateurs.</para>

</sect1>
