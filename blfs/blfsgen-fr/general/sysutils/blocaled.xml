<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

<!ENTITY blocaled-download-http "https://github.com/lfs-book/blocaled/releases/download/v&blocaled-version;/blocaled-&blocaled-version;.tar.xz">
  <!ENTITY blocaled-download-ftp  "">
  <!ENTITY blocaled-md5sum        "04d9833d0dfbf3609b0d8f6d276812ff">
  <!ENTITY blocaled-size          "262&nbsp;Ko">
  <!ENTITY blocaled-buildsize     "4,2&nbsp;Mo (avec les tests)">
  <!ENTITY blocaled-time          "moins de 0,1&nbsp;SBU (avec les tests)">
]>

<sect1 id="blocaled" revision="sysv" xreflabel="blocaled-&blocaled-version;">
  <?dbhtml filename="blocaled.html"?>


  <title>blocaled-&blocaled-version;</title>

  <!--Required section-->
<indexterm zone="blocaled">
    <primary sortas="a-blocaled">blocaled</primary>
  </indexterm>

  
  <sect2 role="package">
    <title>Introduction à blocaled</title>

    <para>
      <application>blocaled</application> est une implémentation du protocole
D-Bus <emphasis>org.freedesktop.locale1</emphasis> qui vient normalement
avec <application>systemd</application>. Il est requis par le bureau
<application>GNOME</application>.
    </para>

    &lfs120_checked;

    <bridgehead renderas="sect3">Informations sur le paquet</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Téléchargement (HTTP)&nbsp;: <ulink url="&blocaled-download-http;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Téléchargement (FTP)&nbsp;: <ulink url="&blocaled-download-ftp;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Somme de contrôle MD5&nbsp;: &blocaled-md5sum;
        </para>
      </listitem>
      <listitem>
        <para>
          Taille du téléchargement&nbsp;: &blocaled-size;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimation de l'espace disque requis&nbsp;: &blocaled-buildsize;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimation du temps de construction&nbsp;: &blocaled-time;
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Dépendances de blocaled</bridgehead>

    <bridgehead renderas="sect4">Requises</bridgehead>
    <para role="required">
      <xref linkend="polkit"/> et <xref linkend="libdaemon"/>
    </para>

  </sect2>

  <sect2 role="installation">
    <title>Installation de blocaled</title>

    <para>
      Installez <application>blocaled</application> en exécutant les commandes
suivantes&nbsp;:
    </para>

<screen><userinput>./configure --prefix=/usr --sysconfdir=/etc &amp;&amp;
make</userinput></screen>

    <para>
      Pour tester les résultats lancez&nbsp;: <command>make check</command>.
    </para>

    <para>
      Maintenant, en tant qu'utilisateur <systemitem
class="username">root</systemitem>&nbsp;:
    </para>

<screen role="root"><userinput>make install</userinput></screen>
  </sect2>

  <sect2 role="configuration">
    <title>Configuration de blocaled</title>

    <sect3 id="blocaled-config">
      <title>Fichiers de configuration</title>
      <para>
        <filename>/etc/blocaled.conf</filename>
      </para>

      <indexterm zone="blocaled blocaled-config">
        <primary
        sortas="e-etc-blocaled.conf">/etc/blocaled.conf</primary>
      </indexterm>
    </sect3>

    <sect3><title>Informations sur la configuration</title>

      <para>
        <filename>/etc/blocaled.conf</filename> contient l'emplacement des fichiers
de configuration utilisés par <application>blocaled</application>. Les
valeurs par défaut conviennent pour BLFS. Des informations sur les entrées
se trouvent en commentaires dans le fichier.
      </para>

      <para>
        Le protocole <emphasis>org.freedesktop.locale1</emphasis> est incapable
d'exporter des variables de paramètres régionaux. Les paramètres régionaux
sont stockés par défaut dans <filename>/etc/locale.conf</filename>. Nous
devons les récupérer dans le profil bash. En tant qu'utilisateur <systemitem
class="username">root</systemitem>, lancez&nbsp;:
      </para>

<screen role="root"><userinput>cat &gt; /etc/profile.d/i18n.sh &lt;&lt; "EOF"
<literal># Begin /etc/profile.d/i18n.sh

if [ -r /etc/locale.conf ]; then source /etc/locale.conf; fi

if [ -n "$LANG" ];              then export LANG; fi
if [ -n "$LC_TYPE" ];           then export LC_TYPE; fi
if [ -n "$LC_NUMERIC" ];        then export LC_NUMERIC; fi
if [ -n "$LC_TIME" ];           then export LC_TIME; fi
if [ -n "$LC_COLLATE" ];        then export LC_COLLATE; fi
if [ -n "$LC_MONETARY" ];       then export LC_MONETARY; fi
if [ -n "$LC_MESSAGES" ];       then export LC_MESSAGES; fi
if [ -n "$LC_PAPER" ];          then export LC_PAPER; fi
if [ -n "$LC_NAME" ];           then export LC_NAME; fi
if [ -n "$LC_ADDRESS" ];        then export LC_ADDRESS; fi
if [ -n "$LC_TELEPHONE" ];      then export LC_TELEPHONE; fi
if [ -n "$LC_MEASUREMENT" ];    then export LC_MEASUREMENT; fi
if [ -n "$LC_IDENTIFICATION" ]; then export LC_IDENTIFICATION; fi

# End /etc/profile.d/i18n.sh</literal>
EOF</userinput></screen>

      <para>
        Ensuite le fichier <filename>/etc/locale</filename> doit être généré, en
tant qu'utilisateur <systemitem class="username">root</systemitem>&nbsp;:
      </para>

<screen role="root"><userinput>cat &gt; /etc/locale.conf &lt;&lt; EOF
<literal># Begin /etc/locale.conf

LANG=$LANG

# End /etc/locale.conf</literal>
EOF</userinput></screen>

      <para>
        Si vous voulez lancer un système de fenêtrage X (ou Wayland), vous pourriez
vouloir configurer votre clavier X. La meilleure manière de faire est de
récupérer les paramètres de <filename>/etc/sysconfig/console</filename> et
de les passer au démon <application>blocaled</application>. En tant
qu'utilisateur <systemitem class="username">root</systemitem>&nbsp;:
      </para>

<screen role="root"><userinput>source /etc/sysconfig/console &amp;&amp;
KEYMAP=${KEYMAP:-us}          &amp;&amp;

gdbus call --system                                             \
           --dest org.freedesktop.locale1                       \
           --object-path /org/freedesktop/locale1               \
           --method org.freedesktop.locale1.SetVConsoleKeyboard \
           "$KEYMAP" "$KEYMAP_CORRECTIONS" true true</userinput></screen>

      <para>
        Cela devrait créer ou modifier le fichier de configuration de Xorg (par
défaut <filename>/etc/X11/xorg.conf.d/30-keyboard.conf</filename>) pour
qu'il corresponde aux paramètres de claviers indiqués dans
<envar>KEYMAP</envar>.
      </para>

    </sect3>

  </sect2>

  <sect2 role="content">
    <title>Contents</title>

    <segmentedlist>
      <segtitle>Programme installé</segtitle>
      <segtitle>Bibliothèque installée</segtitle>
      <segtitle>Répertoire installé</segtitle>

      <seglistitem>
        <seg>
          blocaled (dans <filename class="directory">/usr/libexec</filename>)
        </seg>
        <seg>
          None
        </seg>
        <seg>
          /usr/share/blocaled
        </seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descriptions courtes</bridgehead>
      <?dbfo list-presentation="list"?> <?dbhtml list-presentation="table"?>

      <varlistentry id="blocaled-prog">
        <term><command>blocaled</command></term>
        <listitem>
          <para>
            est le démon implémentant le protocole D-Bus
<emphasis>org.freedesktop.locale1</emphasis>
          </para>
          <indexterm zone="blocaled blocaled-prog">
            <primary sortas="b-blocaled">blocaled</primary>
          </indexterm>
        </listitem>
      </varlistentry>
    </variablelist>
  </sect2>

</sect1>
