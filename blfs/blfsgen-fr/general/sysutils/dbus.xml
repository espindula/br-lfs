<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY dbus-download-http "https://dbus.freedesktop.org/releases/dbus/dbus-&dbus-version;.tar.xz">
  <!ENTITY dbus-download-ftp  "">
  <!ENTITY dbus-md5sum        "da42f55aeec51b355587bc3062fc2d41">
  <!ENTITY dbus-size          "1,3&nbsp;Mo">
  <!ENTITY dbus-buildsize     "23&nbsp;Mo (plus 20&nbsp;Mo pour les tests)">
  <!ENTITY dbus-time          "0,2&nbsp;SBU (plus 1,6&nbsp;SBU pour les tests)">
  <!-- The former value was 0.8 SBU, which was far more sensible. -->
  <!-- I got 'real  0m14.355s' for make check at -j1; bdubbs dbus-1.14.2 -->
  <!-- Now 'real 2m51.642ss' for make check at -j1; bdubbs dbus-1.14.6
# TOTAL: 217
# PASS:  217

Sane test resuults for 1.14.8.  -bdubbs
  -->
]>

<sect1 id="dbus" xreflabel="dbus-&dbus-version;">
  <?dbhtml filename="dbus.html"?>


  <title>dbus-&dbus-version;</title>

  <indexterm zone="dbus">
    <primary sortas="a-D-Bus">D-Bus</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introduction à D-Bus</title>

    <para revision="sysv">
      <application>D-Bus</application> est un système de bus de messages, une
manière simple pour les applications de parler entre
elles. <application>D-Bus</application> fournit un démon système (pour des
événements tels que <quote>nouveau périphérique ajouté</quote> ou
<quote>queue de l'imprimante modifiée</quote>) et un démon par session
utilisateur (pour les besoins IPC généraux des applications de
l'utilisateur). Le bus de messages est aussi construit au dessus d'un
environnement de passage de messages biunivoques, qui peut être utilisé par
deux applications pour communiquer directement (sans passer par le démon de
bus de message).
    </para>

    <para revision="systemd">
      Bien que <application>D-Bus</application> a été construit dans LFS, il y a
des fonctionnalités offertes par le paquet dont d'autres paquets de BLFS ont
besoin, mais leur dépendances ne tiennent pas dans LFS.
    </para>

    &lfs120_checked;

    <bridgehead renderas="sect3">Informations sur le paquet</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Téléchargement (HTTP)&nbsp;: <ulink url="&dbus-download-http;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Téléchargement (FTP)&nbsp;: <ulink url="&dbus-download-ftp;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Somme de contrôle MD5&nbsp;: &dbus-md5sum;
        </para>
      </listitem>
      <listitem>
        <para>
          Taille du téléchargement&nbsp;: &dbus-size;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimation de l'espace disque requis&nbsp;: &dbus-buildsize;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimation du temps de construction&nbsp;: &dbus-time;
        </para>
      </listitem>
    </itemizedlist>

    <bridgehead renderas="sect3">Dépendances de D-Bus</bridgehead>

    <bridgehead renderas="sect4">Recommandées</bridgehead>
    <para role="recommended">
      <!-- elogind cannot be used by dbus: dbus only uses the systemd service,
      not the logind one
      <phrase revision="sysv">
and <xref linkend="elogind"/> (These are
      circular dependencies. First build without them, and then again after
      both packages are installed.)</phrase>-->
<xref linkend="xorg7-lib"/> (pour le programme
<command>dbus-launch</command>)
      
    </para>

    <bridgehead renderas="sect4">Facultatives</bridgehead>
    <para role="optional">
      <phrase revision="systemd"><xref role="runtime" linkend="systemd"/> (pour
enregistrer les services lancés par le démon de session D-Bus comme des
services systemd utilisateurs)&nbsp;;</phrase> Pour les tests&nbsp;: <xref
linkend="dbus-python"/>, <xref linkend="pygobject3"/> et <xref
linkend="valgrind"/>&nbsp;; pour la documentation&nbsp;: <xref
linkend="doxygen"/>, <xref linkend="xmlto"/>, <ulink
url="https://pypi.python.org/pypi/mallard-ducktype">Ducktype</ulink> et
<ulink url="&gnome-download-http;/yelp-tools/">Yelp Tools</ulink>
    </para>

    <para condition="html" role="usernotes">Notes des rédacteurs&nbsp;: <ulink url="&blfs-wiki;/dbus"/>
    </para>
  </sect2>

  <sect2 role="installation">
    <title>Installation de D-Bus</title>
    

    <!--
    <para revision="sysv">

      If they do not already exist,
      as the <systemitem class="username">root</systemitem> user, create a
      system user and group to handle the system message bus activity:
    </para>

<screen role="root" revision="sysv"><userinput>groupadd -g 18 messagebus &amp;&amp;
useradd -c "D-Bus Message Daemon User" -d /var/run/dbus \
        -u 18 -g messagebus -s /bin/false messagebus</userinput></screen>
    -->
<para>
      Installez <application>D-Bus</application> en exécutant les commandes
suivantes (vous pouvez d'abord souhaiter observer la sortie de
<command>./configure --help</command> et ajouter des paramètres désirés à la
commande <command>configure</command> affichée ci-dessous)&nbsp;:
    </para>

    
<screen revision="sysv"><!-- Only usefull for systemd: it installs the services files in the
    systemd/user
    - -enable-user-session                \ -->
<userinput>./configure --prefix=/usr                        \
            --sysconfdir=/etc                    \
            --localstatedir=/var                 \
            --runstatedir=/run                   \
            --disable-doxygen-docs               \
            --disable-xml-docs                   \
            --disable-static                     \
            --with-systemduserunitdir=no         \
            --with-systemdsystemunitdir=no       \
            --docdir=/usr/share/doc/dbus-&dbus-version;  \
            --with-system-socket=/run/dbus/system_bus_socket &amp;&amp;
make</userinput></screen>

<screen revision="systemd"><userinput>./configure --prefix=/usr                        \
            --sysconfdir=/etc                    \
            --localstatedir=/var                 \
            --runstatedir=/run                   \
            --enable-user-session                \
            --disable-doxygen-docs               \
            --disable-xml-docs                   \
            --disable-static                     \
            --docdir=/usr/share/doc/dbus-&dbus-version;  \
            --with-system-socket=/run/dbus/system_bus_socket &amp;&amp;
make</userinput></screen>

    <para>
      Voir ci-dessous pour les instructions de test.
    </para>



    <!--
    <warning revision="systemd">

      <para>
        Installing the package will override all files installed by
        <application>D-Bus</application> in LFS. It is critical that
        nothing uses <application>D-Bus</application> libraries or
        programs during the installation. The best way to ensure that these
        libraries are not being used is to run the installation in rescue mode.
        To switch to rescue mode, run the following command as the
        <systemitem class="username">root</systemitem> user (from a TTY):
      </para>

<screen role="root"><userinput>systemctl start rescue.target</userinput></screen>

    </warning>
-->
<para>
      Maintenant, en tant qu'utilisateur <systemitem
class="username">root</systemitem>&nbsp;:
    </para>

<!-- FIXME: No longer needed with usr merge
    <para>

      The shared library needs to be moved to
      <filename class="directory">/lib</filename>, and as a result the
      <filename class="extension">.so</filename> file in
      <filename class="directory">/usr/lib</filename> will need to be
      recreated. Run the following command as the
      <systemitem class="username">root</systemitem> user:
    </para>

<screen role="root"><userinput>mv -v /usr/lib/libdbus-1.so.* /lib &amp;&amp;
ln -sfv ../../lib/$(readlink /usr/lib/libdbus-1.so) /usr/lib/libdbus-1.so</userinput></screen>
-->
<screen role="root"><userinput>make install</userinput></screen>



    <para>
      Si vous utilisez une installation DESTDIR,
<command>dbus-daemon-launch-helper</command> doit être corrigé. Tapez en
tant qu'utilisateur <systemitem class="username">root</systemitem>&nbsp;:
    </para>

<screen role="root"><userinput>chown -v root:messagebus /usr/libexec/dbus-daemon-launch-helper &amp;&amp;
chmod -v      4750       /usr/libexec/dbus-daemon-launch-helper</userinput></screen>

    <para revision="sysv">
      Si vous construisez toujours votre système en chroot ou que vous n'avez pas
encore lancé le démon, mais que vous souhaitez compiler certains paquets
dépendant de <application>D-Bus</application>, générez l'UUID
<application>D-Bus</application> afin d'éviter les avertissements lors de la
compilation de certains paquets à l'aide de la commande suivante en tant
qu'utilisateur <systemitem class="username">root</systemitem>&nbsp;:
    </para>

<screen role="root" revision="sysv"><userinput>dbus-uuidgen --ensure</userinput></screen>

    <para revision="sysv">
      Si vous utilisez <xref linkend="elogind"/>, créez un lien symbolique vers le
fichier <filename>/var/lib/dbus/machine-id</filename>&nbsp;:
    </para>

<!--
    <para revision="systemd">

      If not in chroot, at this point, you should reload the systemd daemon,
      and reenter multi-user mode with the following commands (as the
      <systemitem class="username">root</systemitem> user):
    </para>

<screen role="root" revision="systemd"><userinput>systemctl daemon-reload
systemctl start multi-user.target</userinput></screen>
-->
<!-- Without dbus-python and pygobject3, only 39 tests are run and
         11 out of 39 are skipped.  With dbus-python and pygobject3
         installed, 238 tests are run and 37 out of 238 are skipped.  -->
<screen role="root" revision="sysv"><userinput>ln -sfv /var/lib/dbus/machine-id /etc</userinput></screen>



    

    <para>
      De nombreux tests sont désactivés à moins d'avoir installé à la fois <xref
linkend="dbus-python"/> et <xref linkend="pygobject3"/>. Ils doivent être
lancés en tant qu'utilisateur non privilégié depuis une session locale avec
une adresse de bus. Pour lancer la suite de tests standard, lancez
<command>make check</command>.
    </para>

    
    <!-- With dbus-python and pygobject3 installed and these additional
         debug options, 347 tests are run and 21 out of 347 are skipped.
         Additionally,  217 tests in the "name-test" directory are run
         and none is skipped.  -->
<para>
      Si vous voulez lancer les tests unitaires, des paramètres supplémentaires
doivent être passés à configure qui exposent des fonctionnalités
supplémentaires dans les fichiers binaires mais ne sont pas destinées à être
utilisées dans une construction de production de
<application>D-Bus</application>. Si vous souhaitez lancer les tests, tapez
les commandes suivantes (pour les tests, vous n'avez pas besoin de
construire la doc)&nbsp;:
    </para>

<screen remap="test"><userinput>make distclean                                    &amp;&amp;
PYTHON=python3 ./configure --enable-tests         \
                           --enable-asserts       \
                           --disable-doxygen-docs \
                           --disable-xml-docs     &amp;&amp;
make                                              &amp;&amp;
make check</userinput></screen>

    <para>
      Il y a un signalement indiquant que les tests peuvent échouer s'ils sont
lancés depuis un shell Midnight Commander. Il se peut vous ayez des messages
d'erreur de dépassement de mémoire lors des tests. Ils sont normaux et vous
pouvez sans problème les ignorer.
    </para>

  </sect2>

  <sect2 role="commands">
    <title>Explication des commandes</title>

    <para>
      <parameter>--disable-doxygen-docs</parameter>&nbsp;: Ce paramètre désactive
la construction de la documentation doxygen et son installation, si vous
avez installé <application>doxygen</application>. Si
<application>doxygen</application> est installé et que vous souhaitez
construire et l'installer, effacez ce paramètre.
    </para>

    <para>
      <parameter>--disable-xml-docs</parameter>&nbsp;: Ce paramètre désactive la
construction et l'installation de la documentation html si vous avez
installé <application>xmlto</application>. Supprimez-le, si
<application>xmlto</application> est installé et que vous souhaitiez la
construire et l'installer.
    </para>

    <xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
      href="../../xincludes/static-libraries.xml"/>

    <para revision="systemd">
      <parameter>--enable-user-session</parameter>&nbsp;: Ce paramètre active les
sessions utilisateurs DBus avec <application>systemd</application>.
    </para>

    <para revision="sysv">
      <parameter>--with-systemd{user,system}unitdir=no</parameter>&nbsp;: Ce
paramètre désactive l'installation des unités systemd sur les systèmes basés
sur elogind.
    </para>

    <para>
      <parameter>--with-system-pid-file=/run/dbus/pid</parameter>&nbsp;:  Ce
paramètre spécifie l'endroit pour le fichier PID.
    </para>

    <para>
      <parameter>--with-system-socket=/run/dbus/system_bus_socket</parameter>&nbsp;:
Ce paramètre spécifie l'emplacement du socket du bus système.
    </para>

    <para>
      <parameter>--enable-tests</parameter>&nbsp;: Construit des parties
supplémentaires du code pour tous les tests. N'utilisez pas ce paramètre
pour une construction de production.
    </para>

    <para>
      <option>--enable-embedded-tests</option>&nbsp;: Construit des parties
supplémentaires du code pour prendre en charge seulement les tests
unitaires. N'utilisez pas ce paramètres pour une construction de production.
    </para>

    <para>
      <parameter>--enable-asserts</parameter>&nbsp;: Active le code de débogage
pour exécuter des déclarations d'éléments supposées normalement vrais. Cela
empêche un avertissement selon lequel
'<parameter>--enable-tests</parameter>' n'est utile en lui-même que pour le
profilage et il pourrait ne pas donner de bons résultats pour tous les
tests, mais il ajoute sa propre remarque selon laquelle cela ne devrait pas
être utilisé dans une construction de production.
    </para>

  </sect2>

  <sect2 role="configuration">
    <title>Configuration de D-Bus</title>

    <sect3 id="dbus-config">
      <title>Fichiers de configuration</title>

      <para>
        <filename>/etc/dbus-1/session.conf</filename>,
<filename>/etc/dbus-1/system.conf</filename> et
<filename>/etc/dbus-1/system.d/*</filename>
      </para>

      <indexterm zone="dbus dbus-config">
        <primary sortas="e-etc-dbus-1-session.conf">/etc/dbus-1/session.conf</primary>
      </indexterm>

      <indexterm zone="dbus dbus-config">
        <primary sortas="e-etc-dbus-1/system.conf">/etc/dbus-1/system.conf</primary>
      </indexterm>

      <indexterm zone="dbus dbus-config">
        <primary sortas="e-etc-dbus-1-system.d-star">/etc/dbus-1/system.d/*</primary>
      </indexterm>

    </sect3>

    <sect3 id="dbus-session-config" xreflabel="Répertoire de services personnalisés de D-Bus">
      <title>Informations sur la configuration</title>

      <para>
        On ne devrait probablement pas modifier les fichiers de configuration listés
ci-dessus. Si des modifications sont requises, vous devriez créer
<filename>/etc/dbus-1/session-local.conf</filename> ou
<filename>/etc/dbus-1/system-local.conf</filename> et effectuer les
changements désirés dans ces fichiers.
     </para>

      <para>
        Si un paquet installe un fichier <filename>.service</filename> de
<application>D-Bus</application> hors du répertoire <filename
class="directory">/usr/share/dbus-1/services</filename> standard, vous
devriez ajouter ce répertoire à la configuration de la session locale. Par
exemple, <filename
class="directory">/usr/local/share/dbus-1/services</filename> peut être
ajouté en effectuant les commandes suivantes en tant qu'utilisateur
<systemitem class="username">root</systemitem>&nbsp;:
      </para>

<screen role="root"><userinput>cat &gt; /etc/dbus-1/session-local.conf &lt;&lt; "EOF"
<literal>&lt;!DOCTYPE busconfig PUBLIC
 "-//freedesktop//DTD D-BUS Bus Configuration 1.0//EN"
 "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd"&gt;
&lt;busconfig&gt;

  &lt;!-- Search for .service files in /usr/local --&gt;
  &lt;servicedir&gt;/usr/local/share/dbus-1/services&lt;/servicedir&gt;

&lt;/busconfig&gt;</literal>
EOF</userinput></screen>

    </sect3>

    <sect3  id="dbus-session">
      <title>Démon de Session D-Bus</title>

      <para revision="sysv">
        Pour démarrer automatiquement <command>dbus-daemon</command> quand on
redémarre le système, installez le script de démarrage
<filename>/etc/rc.d/init.d/dbus</filename> depuis le paquet <xref
linkend="bootscripts"/>.
      </para>

      <indexterm zone="dbus dbus-session" revision="sysv">
        <primary sortas="f-dbus">dbus</primary>
      </indexterm>

<screen role="root" revision="sysv"><userinput>make install-dbus</userinput></screen>

      <para revision="sysv">
        Si c'est la première installation de <application>D-Bus</application> sur le
système et que vous ne travaillez pas dans un environnement chroot, vous
pouvez immédiatement démarrer <command>dbus-daemon</command> sans redémarrer
le système&nbsp;:
      </para>

      
      <screen role="nodump" revision="sysv"><!-- nodump because maybe the user is building in chroot -->
<userinput>/etc/init.d/dbus start</userinput></screen>

      <para revision="sysv">
        Remarquez que ce script de démarrage ne démarre que démon
<application>D-Bus</application> de tout le système. Chaque utilisateur
ayant besoin d'un accès aux services de <application>D-Bus</application>
devront exécuter en plus un démon de session. Il y a beaucoup de méthodes
utilisables pour démarrer un démon de session en utilisant la commande
<command>dbus-launch</command>. Revoyez la page de man de
<command>dbus-launch</command> pour des détails sur les paramètres et les
options disponibles. Voici quelques suggestions et exemples&nbsp;:
      </para>

      <para revision="systemd">
        Il y a plusieurs méthodes que vous pouvez utiliser pour démarrer un démon de
session avec la commande <command>dbus-launch</command>. Regardez la page de
manuel de <command>dbus-launch</command> pour les détails à propos des
paramètres disponibles et des options. Voici quelques suggestions et
exemples&nbsp;:
      </para>


      <itemizedlist spacing="compact">
        <listitem>
          <para>
            Ajouter <command>dbus-launch</command> à la ligne dans le fichier
<filename>~/.xinitrc</filename> qui démarre votre environnement graphique.
          </para>
        </listitem>
        <listitem>
          <para>
            Si vous utilisez <command>gdm</command> ou un autre gestionnaire d'affichage
qui appelle le fichier <filename>~/.xsession</filename>, vous pouvez ajouter
<command>dbus-launch</command> à la ligne dans votre fichier
<filename>~/.xsession</filename> qui démarre votre environnement
graphique. La syntaxe ressemblerait à celle de l'exemple dans le fichier
<filename>~/.xinitrc</filename>.
          </para>
        </listitem>
        <listitem>
          <para>
            Les exemples montrés précédemment utilisent <command>dbus-launch</command>
pour spécifier un programme à exécuter. Ceci a le bénéfice (quand on utilise
également le paramètre <parameter>--exit-with-x11</parameter>) de stopper le
démon de la session quand le programme spécifié est stoppé. Vous pouvez
également démarrer le démon de la session dans vos scripts de démarrage du
système ou personnels en ajoutant les lignes suivantes&nbsp;:
          </para>

<screen role="nodump"><userinput><literal># Start the D-Bus session daemon
eval `dbus-launch`
export DBUS_SESSION_BUS_ADDRESS</literal></userinput></screen>

          <para>
            Cette méthode n'arrêtera pas le démon de session quand vous quittez votre
shell, donc vous devriez ajouter la ligne suivante à votre fichier
<filename>~/.bash_logout</filename>&nbsp;:
          </para>

<screen role="nodump"><userinput><literal># Kill the D-Bus session daemon
kill $DBUS_SESSION_BUS_PID</literal></userinput></screen>
        </listitem>

        

      <!-- KDM is no longer usable, SDDM is it's replacement
        <listitem>
          <para>
            A hint has been written that provides ways to start scripts
            using the KDM session manager of KDE. The concepts in this hint could
            possibly be used with other session managers as well. The hint is
            located at <ulink
            url="&hints-root;/downloads/files/execute-session-scripts-using-kdm.txt"/>.
          </para>
        </listitem>
        -->
</itemizedlist>

    </sect3>

  </sect2>

  <sect2 role="content" revision="sysv">
    <title>Contenu</title>

    <segmentedlist>
      <segtitle>Programmes installés</segtitle>
      <segtitle>Bibliothèque installée</segtitle>
      <segtitle>Répertoires installés</segtitle>

      <seglistitem>
        <seg>
          dbus-cleanup-sockets, dbus-daemon, dbus-launch, dbus-monitor,
dbus-run-session, dbus-send, dbus-test-tool,
dbus-update-activation-environment et dbus-uuidgen
        </seg>
        <seg>
           libdbus-1.so
        </seg>
        <seg>
          /etc/dbus-1, /usr/{include,lib}/dbus-1.0, /usr/lib/cmake/DBus1,
/usr/share/dbus-1, /usr/share/xml/dbus-1,
/usr/share/doc/dbus-&dbus-version;, et /var/{lib,run}/dbus
        </seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descriptions courtes</bridgehead>
      <?dbfo list-presentation="list"?> <?dbhtml list-presentation="table"?>

      <varlistentry id="dbus-cleanup-sockets">
        <term><command>dbus-cleanup-sockets</command></term>
        <listitem>
          <para>
            nettoie les sockets superflus dans un répertoire
          </para>
          <indexterm zone="dbus dbus-cleanup-sockets">
            <primary sortas="b-dbus-cleanup-sockets">dbus-cleanup-sockets</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="dbus-daemon">
        <term><command>dbus-daemon</command></term>
        <listitem>
          <para>
            est le démon de bus de messages <application>D-Bus</application>
          </para>
          <indexterm zone="dbus dbus-daemon">
            <primary sortas="b-dbus-daemon">dbus-daemon</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="dbus-launch">
        <term><command>dbus-launch</command></term>
        <listitem>
          <para>
            est utilisé pour démarrer <command>dbus-daemon</command> depuis un script
shell. Il est en principe appelé par des scripts de connexion d'un
utilisateur
          </para>
          <indexterm zone="dbus dbus-launch">
            <primary sortas="b-dbus-launch">dbus-launch</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="dbus-monitor">
        <term><command>dbus-monitor</command></term>
        <listitem>
          <para>
            est utilisé pour surveiller les messages à travers un bus de messages
<application>D-Bus</application>
          </para>
          <indexterm zone="dbus dbus-monitor">
            <primary sortas="b-dbus-monitor">dbus-monitor</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="dbus-run-session">
        <term><command>dbus-run-session</command></term>
        <listitem>
          <para>
            démarre un processus comme une nouvelle session
<application>D-Bus</application>
          </para>
          <indexterm zone="dbus dbus-run-session">
            <primary sortas="b-dbus-run-session">dbus-run-session</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="dbus-send">
        <term><command>dbus-send</command></term>
        <listitem>
          <para>
            est utilisé pour envoyer un message à un bus de messages
<application>D-Bus</application>
          </para>
          <indexterm zone="dbus dbus-send">
            <primary sortas="b-dbus-send">dbus-send</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="dbus-test-tool">
        <term><command>dbus-test-tool</command></term>
        <listitem>
          <para>
            est un générateur de trafic <application>D-Bus</application> et un outil de
tests&nbsp;; c'est un outil multi-usage pour le débogage et le profilage de
<application>D-Bus</application>
          </para>
          <indexterm zone="dbus dbus-test-tool">
            <primary sortas="b-dbus-test-tool">dbus-test-tool</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="dbus-update-activation-environment">
        <term><command>dbus-update-activation-environment</command></term>
        <listitem>
          <para>
            est utilisé pour mettre à jour l'environnement utilisé pour les services de
session de <application>D-Bus</application>&nbsp;; il met à jour la liste
des variables d'environnement utilisées par <command>dbus-daemon
--session</command> quand il active les services de session sans utiliser
systemd
          </para>
          <indexterm zone="dbus dbus-update-activation-environment">
            <primary sortas="b-dbus-update-activation-environment">
            dbus-update-activation-environment</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="dbus-uuidgen">
        <term><command>dbus-uuidgen</command></term>
        <listitem>
          <para>
            est utilisé pour générer ou lire un ID universellement unique
          </para>
          <indexterm zone="dbus dbus-uuidgen">
            <primary sortas="b-dbus-uuidgen">dbus-uuidgen</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="libdbus-1">
        <term><filename class="libraryfile">libdbus-1.so</filename></term>
        <listitem>
          <para>
            contient les fonctions de l'API utilisées par le démon de messages
<application>D-Bus</application>. <application>D-Bus</application> est
d'abord une bibliothèque qui fournit une communication directe entre deux
applications&nbsp;; <command>dbus-daemon</command> est une application qui
utilise cette bibliothèque pour implémenter un démon de bus de message
          </para>
          <indexterm zone="dbus libdbus-1">
            <primary sortas="c-libdbus-1">libdbus-1.so</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

  <sect2 role="content" revision="systemd">
    <title>Contenu</title>

      <para>
        Une liste des fichiers installés, avec leur description courte se trouve sur
<ulink url="&lfs-root;/chapter08/dbus.html#contents-dbus"/>
      </para>

  </sect2>

</sect1>
