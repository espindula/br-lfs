<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!ENTITY fcron-download-http "http://fcron.free.fr/archives/fcron-&fcron-version;.src.tar.gz">
  <!ENTITY fcron-download-ftp "">
<!--  <!ENTITY fcron-download-ftp "ftp://ftp.seul.org/pub/fcron/fcron-&fcron-version;.src.tar.gz">-->
  <!ENTITY fcron-md5sum "bd4996e941a40327d11efc5e3fd1f839">
  <!ENTITY fcron-size "587 Ko">
  <!ENTITY fcron-buildsize "5.1 Mo">
  <!ENTITY fcron-time "0,1&nbsp;SBU">
]>

<sect1 id="fcron" xreflabel="Fcron-&fcron-version;">
  <?dbhtml filename="fcron.html"?>

  <sect1info>
<date>$Date$</date></sect1info>

  <title>Fcron-&fcron-version;</title>

  <indexterm zone="fcron">
    <primary sortas="a-Fcron">Fcron</primary>
  </indexterm>

  <sect2 role="package">
    <title>Introduction à Fcron</title>

    <para>
      Le paquet <application>Fcron</application> contient un lanceur de commande
périodique ayant pour but de remplacer <application>Vixie
Cron</application>.
    </para>

    &lfs112_checked;

    <bridgehead renderas="sect3">Informations sur le paquet</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Téléchargement (HTTP)&nbsp;: <ulink url="&fcron-download-http;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Téléchargement (FTP)&nbsp;: <ulink url="&fcron-download-ftp;"/>
        </para>
      </listitem>
      <listitem>
        <para>
          Somme de contrôle MD5&nbsp;: &fcron-md5sum;
        </para>
      </listitem>
      <listitem>
        <para>
          Taille du téléchargement&nbsp;: &fcron-size;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimation de l'espace disque requis&nbsp;: &fcron-buildsize;
        </para>
      </listitem>
      <listitem>
        <para>
          Estimation du temps de construction&nbsp;: &fcron-time;
        </para>
      </listitem>
    </itemizedlist>

    <!--
    <bridgehead renderas="sect3">
Additional Downloads</bridgehead>
    <itemizedlist spacing="compact">
      <listitem>
        <para>
          Required patch:
          <ulink url="&patch-root;/fcron-&fcron-version;-sendmail_upstream-1.patch"/>
        </para>
      </listitem>
    </itemizedlist>
-->
<bridgehead renderas="sect3">Dépendances de Fcron</bridgehead>

    <bridgehead renderas="sect4">Facultatives</bridgehead>
    <para role="optional">
      Un <xref linkend="server-mail"/>, <ulink
url="../postlfs/editors.html">éditeur de texte</ulink> (par défaut
<command>vi</command> du paquet <xref linkend="vim"/>), <xref
linkend="linux-pam"/> et <xref linkend="docbook-utils"/>
    </para>

    <para condition="html" role="usernotes">Notes utilisateur&nbsp;: <ulink url="&blfs-wiki;/fcron"/></para>

  </sect2>

  <sect2 role="installation">
    <title>Installation de Fcron</title>

    <para id="fcron-syslog" revision="sysv">
      <application>Fcron</application> utilise le service cron de
<command>syslog</command> pour enregistrer tous les messages. Comme LFS ne
paramètre pas ce service dans <filename>/etc/syslog.conf</filename>, on doit
le faire avant d'installer <application>Fcron</application>. Cette commande
enverra la ligne nécessaire au fichier <filename>/etc/syslog.conf</filename>
actuel (exécutez-la en tant qu'utilisateur <systemitem
class='username'>root</systemitem>)&nbsp;:
    </para>

    <indexterm zone="fcron fcron-syslog" revision="sysv">
      <primary sortas="e-etc-syslog.conf">/etc/syslog.conf</primary>
    </indexterm>

<screen role="root" revision="sysv"><userinput>cat &gt;&gt; /etc/syslog.conf &lt;&lt; "EOF"
<literal># Begin fcron addition to /etc/syslog.conf

cron.* -/var/log/cron.log

# End fcron addition</literal>
EOF</userinput></screen>

    <para revision="sysv">
      Le fichier de configuration a été modifié, donc le rechargement du démon
<command>sysklogd</command> activera les modifications (de nouveau en tant
qu'utilisateur <systemitem class='username'>root</systemitem>).
    </para>

<screen role="root" revision="sysv"><userinput>/etc/rc.d/init.d/sysklogd reload</userinput></screen>

    <para>
      Pour des raisons de sécurité, vous devriez créer un utilisateur et un groupe
non privilégiés pour <application>Fcron</application>. (faites en tant
qu'utilisateur <systemitem class='username'>root</systemitem>)&nbsp;:
    </para>

<screen role="root"><userinput>groupadd -g 22 fcron &amp;&amp;
useradd -d /dev/null -c "Fcron User" -g fcron -s /bin/false -u 22 fcron</userinput></screen>

    <para>
      Maintenant corrigez certains emplacement codés en dur dans la
documentation&nbsp;:
    </para>

<screen><userinput>find doc -type f -exec sed -i 's:/usr/local::g' {} \;</userinput></screen>
    <para>
      Installez <application>Fcron</application> en exécutant les commandes
suivantes&nbsp;:
    </para>

<screen revision="sysv"><userinput>./configure --prefix=/usr          \
            --sysconfdir=/etc      \
            --localstatedir=/var   \
            --without-sendmail     \
            --with-boot-install=no \
            --with-systemdsystemunitdir=no &amp;&amp;
make</userinput></screen>

<screen revision="systemd"><userinput>./configure --prefix=/usr          \
            --sysconfdir=/etc      \
            --localstatedir=/var   \
            --without-sendmail     \
            --with-piddir=/run     \
            --with-boot-install=no &amp;&amp;
make</userinput></screen>

    <para>
      Ce paquet n'a pas de suite de tests.
    </para>

    <para>
      Maintenant, en tant qu'utilisateur <systemitem
class="username">root</systemitem>&nbsp;:
    </para>

<screen role="root"><userinput>make install</userinput></screen>

    <para>
      Les installations par DESTDIR doivent se faire en tant qu'utilisateur
<systemitem class="username">root</systemitem>. De plus, si les fichiers de
configuration de PAM doivent être installés dans <filename
class="directory">/etc/pam.d</filename>, vous devez créer ce répertoire dans
le DESTDIR avant d'effectuer l'installation.
    </para>

  </sect2>

  <sect2 role="commands">
    <title>Explication des commandes</title>


    <!--
    <para>

      <command>autoconf</command>: This command is required to rebuild
      <filename>configure</filename> using the fixed input file.
    </para>-->
<para>
      <parameter>--without-sendmail</parameter>&nbsp;: Par défaut,
<application>Fcron</application> essaiera d'utiliser la commande
<command>sendmail</command> d'un paquet <xref linkend="server-mail"/> pour
vous envoyer par messagerie le résultat du script
<command>fcron</command>. Ce paramètre est utilisé pour désactiver la
notification par messagerie par défaut. Ne mettez pas ce paramètre pour
activer l'option par défaut. Vous pouvez sinon utiliser
<option>--with-sendmail=<replaceable>&lt;/chemin/vers/la/commande/MTA&gt;</replaceable></option>
pour utiliser une autre commande de messagerie.
    </para>

    <para>
      <parameter>--with-boot-install=no</parameter>&nbsp;: Ceci empêche
l'installation du script de démarrage inclus avec le paquet.
    </para>

    <para revision="systemd">
      <parameter>--with-piddir=/run</parameter>&nbsp;: cela corrige l'unité
systemd pour qu'elle utilise les bons répertoires pour les fichiers PID pour
que systemctl ne se fige pas et que les unités démarrent correctement.
    </para>

    <para revision="sysv">
      <parameter>--with-systemdsystemunitdir=no</parameter>&nbsp;: Ce paramètre
évite la construction des unités <application>systemd</application>, qui ne
sont pas requises pour un système <application>SYS V</application>.
    </para>

    <para>
      <option>--with-editor=<replaceable>&lt;/path/to/editor&gt;</replaceable></option>&nbsp;:
Ce paramètre vous permet de régler l'éditeur de texte par défaut.
    </para>

    <para>
      <option>--with-dsssl-dir=<replaceable>&lt;/path/to/dsssl-stylesheets&gt;</replaceable></option>&nbsp;:
Peut être utilisé si vous avez installé <xref
linkend="docbook-utils"/>. Actuellement, les feuilles de styles dsssl sont
dans le répertoire <filename
class="directory">/usr/share/sgml/docbook/dsssl-stylesheets-1.79</filename>.
    </para>

  </sect2>

  <sect2 role="configuration">
    <title>Configuration de Fcron</title>

    <sect3 id="fcron-config">
      <title>Fichiers de configuration</title>

      <para>
        <filename>/etc/fcron.conf</filename>, <filename>/etc/fcron.allow</filename>
et <filename>/etc/fcron.deny</filename>
      </para>

      <indexterm zone="fcron fcron-config">
        <primary sortas="e-etc-fcron.conf">/etc/fcron.conf</primary>
      </indexterm>

      <indexterm zone="fcron fcron-config">
        <primary sortas="e-etc-fcron.allow">/etc/fcron.allow</primary>
      </indexterm>

      <indexterm zone="fcron fcron-config">
        <primary sortas="e-etc-fcron.deny">/etc/fcron.deny</primary>
      </indexterm>

    </sect3>

    <sect3 id="run-parts" xreflabel="Informations sur la configuration de Fcron">
      <title>Informations sur la configuration</title>

      <para>
        Aucune modification n'est nécessaire dans un fichier de config. Vous pouvez
trouver des informations de configuration dans la page de man de
<filename>fcron.conf</filename>.
      </para>

      <para>
        Les scripts <command>fcron</command> sont écrits en utilisant
<command>fcrontab</command>. Reportez-vous à la page de manuel de
<command>fcrontab</command> pour les paramètres adaptés à votre situation.
      </para>

      <para id="fcron-pam">
        Si <application>Linux-PAM</application> est installé, deux fichiers de
configuration PAM sont installés dans <filename
class='directory'>etc/pam.d</filename>. Sinon, si <filename
class='directory'>etc/pam.d</filename> n'est pas utilisé, l'installation
enverra deux sections de configuration vers le fichier
<filename>/etc/pam.conf</filename>. Vous devriez vous assurer que les
fichiers correspondent à vos préférences. Modifiez-les comme il faut pour
s'adapter à vos besoins.
      </para>

      <indexterm zone="fcron fcron-pam">
        <primary sortas="e-etc-pam.d">/etc/pam.d/*</primary>
      </indexterm>

      <indexterm zone="fcron fcron-pam">
        <primary sortas="e-etc-pam.conf">/etc/pam.conf</primary>
      </indexterm>

      <bridgehead renderas="sect4">Taches périodiques</bridgehead>

      <para>
        Si vous voulez paramétrer une hiérarchie périodique pour l'utilisateur root,
lancez d'abord les commandes suivantes (en tant qu'utilisateur <systemitem
class="username">root</systemitem>) pour créer le script
<filename>/usr/bin/run-parts</filename>&nbsp;:
      </para>

<screen role="root"><userinput>cat &gt; /usr/bin/run-parts &lt;&lt; "EOF" &amp;&amp;
<literal>#!/bin/sh
# run-parts:  Runs all the scripts found in a directory.
# from Slackware, by Patrick J. Volkerding with ideas borrowed
# from the Red Hat and Debian versions of this utility.

# keep going when something fails
set +e

if [ $# -lt 1 ]; then
  echo "Usage: run-parts &lt;directory&gt;"
  exit 1
fi

if [ ! -d $1 ]; then
  echo "Not a directory: $1"
  echo "Usage: run-parts &lt;directory&gt;"
  exit 1
fi

# There are several types of files that we would like to
# ignore automatically, as they are likely to be backups
# of other scripts:
IGNORE_SUFFIXES="~ ^ , .bak .new .rpmsave .rpmorig .rpmnew .swp"

# Main loop:
for SCRIPT in $1/* ; do
  # If this is not a regular file, skip it:
  if [ ! -f $SCRIPT ]; then
    continue
  fi
  # Determine if this file should be skipped by suffix:
  SKIP=false
  for SUFFIX in $IGNORE_SUFFIXES ; do
    if [ ! "$(basename $SCRIPT $SUFFIX)" = "$(basename $SCRIPT)" ]; then
      SKIP=true
      break
    fi
  done
  if [ "$SKIP" = "true" ]; then
    continue
  fi
  # If we've made it this far, then run the script if it's executable:
  if [ -x $SCRIPT ]; then
    $SCRIPT || echo "$SCRIPT failed."
  fi
done

exit 0</literal>
EOF
chmod -v 755 /usr/bin/run-parts</userinput></screen>

      <para>
        Maintenant, créez la hiérarchie des répertoires pour les taches périodiques
(de nouveau en tant qu'utilisateur <systemitem
class="username">root</systemitem>)&nbsp;:
      </para>

<screen role="root"><userinput>install -vdm754 /etc/cron.{hourly,daily,weekly,monthly}</userinput></screen>

      <para>
        Enfin, ajoutez <command>run-parts</command> au fcrontab du système (toujours
en tant qu'utilisateur <systemitem class='username'>root</systemitem>).
      </para>

<screen role="root"><userinput>cat &gt; /var/spool/fcron/systab.orig &lt;&lt; "EOF"
<literal>&amp;bootrun 01 * * * * root run-parts /etc/cron.hourly
&amp;bootrun 02 4 * * * root run-parts /etc/cron.daily
&amp;bootrun 22 4 * * 0 root run-parts /etc/cron.weekly
&amp;bootrun 42 4 1 * * root run-parts /etc/cron.monthly</literal>
EOF</userinput></screen>

    </sect3>

    <sect3  id="fcron-init">
      <title><phrase revision="sysv">Script de démarrage</phrase> <phrase
revision="systemd">Unité Systemd</phrase></title>

      <para revision="sysv">
        Installez le script d'initialisation
<filename>/etc/rc.d/init.d/fcron</filename> à partir du paquet <xref
linkend="bootscripts"/>.
      </para>

      <para revision="systemd">
        Activez le démarrage de <application>Fcron</application> au démarrage en
utilisant l'unité systemd précédemment installée.
      </para>

      <indexterm zone="fcron fcron-init">
        <primary sortas="f-fcron">fcron</primary>
      </indexterm>

<screen role="root" revision="sysv"><userinput>make install-fcron</userinput></screen>

<screen role="root" revision="systemd"><userinput>systemctl enable fcron</userinput></screen>

      <para>
        Enfin, toujours en tant qu'utilisateur <systemitem
class="username">root</systemitem>, démarrez fcron et générez le fichier
<filename>/var/spool/fcron/systab</filename>&nbsp;:
      </para>

<screen role="root" revision="sysv"><userinput>/etc/rc.d/init.d/fcron start &amp;&amp;
fcrontab -z -u systab</userinput></screen>

<screen role="root" revision="systemd"><userinput>systemctl start fcron &amp;&amp;
fcrontab -z -u systab</userinput></screen>
    </sect3>

  </sect2>

  <sect2 role="content">
    <title>Contenu</title>

    <segmentedlist>
      <segtitle>Programmes installés</segtitle>
      <segtitle>Bibliothèques installées</segtitle>
      <segtitle>Répertoires installés</segtitle>

      <seglistitem>
        <seg>fcron, fcrondyn, fcronsighup et fcrontab</seg>
        <seg>Aucune</seg>
        <seg>/usr/share/doc/fcron-&fcron-version; et /var/spool/fcron</seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descriptions courtes</bridgehead>
      <?dbfo list-presentation="list"?> <?dbhtml list-presentation="table"?>

      <varlistentry id="fcron-prog">
        <term><command>fcron</command></term>
        <listitem>
          <para>
            et le démon de planification
          </para>
          <indexterm zone="fcron fcron-prog">
            <primary sortas="b-fcron">fcron</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="fcrondyn">
        <term><command>fcrondyn</command></term>
        <listitem>
          <para>
            est un outil utilisateur conçu pour interagir avec un démon
<command>fcron</command> existant
          </para>
          <indexterm zone="fcron fcrondyn">
            <primary sortas="b-fcrondyn">fcrondyn</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="fcronsighup">
        <term><command>fcronsighup</command></term>
        <listitem>
          <para>
            demande à <command>fcron</command> de relire les tables
<application>Fcron</application>
          </para>
          <indexterm zone="fcron fcronsighup">
            <primary sortas="b-fcronsighup">fcronsighup</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="fcrontab">
        <term><command>fcrontab</command></term>
        <listitem>
          <para>
            est un programme utilisé pour installer, éditer, lister et supprimer les
tables utilisées par <command>fcron</command>
          </para>
          <indexterm zone="fcron fcrontab">
            <primary sortas="b-fcrontab">fcrontab</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
