<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;
]>

<sect1 id="tex-path" xreflabel="Initialisation du PATH pour TeX Live">
  <?dbhtml filename="tex-path.html"?>


  <title>Initialisation du PATH pour TeX Live</title>

  <para>
    Avant le début de la construction de TeX Live, initialisé votre PATH pour
que le système puisse correctement trouver les fichiers. Si vous initialisez
vos scripts de connexion tel que recommandé dans <xref
linkend="postlfs-config-profile"/>, mettez à jour les chemins nécessaires en
ajoutant le script <filename>texlive.sh</filename>. Les programmes sont
toujours installés dans un sous-répertoire &lt;ARCH&gt;-linux et en 32-bit
x86 c'est toujours i386-linux. Pour x86_64 et i?86 vous pouvez générer cela
en tant que $TEXARCH&nbsp;:
  </para>

  <note>
    <para>
      Si vous mettez à jour un version des années précédentes, vous devriez
modifier manuellement <filename>texlive.sh</filename> pour vous assurer que
la version de cette année que vous voulez utiliser sera la seule
installation de TeX présente (certaines personnes ont besoin de garder
plusieurs versions pour s'assurer qu'il n'y a pas de régression dans leurs
documents).
    </para>
  </note>



   <!-- EOF should NOT be in double quotes, we want it to evaluate $TEXARCH -->
<!-- this is also in texlive.xml -->
<para>Maintenant créez le script texlive.sh en tant que &root;&nbsp;:</para>

<screen role="root"><userinput>TEXARCH=$(uname -m | sed -e 's/i.86/i386/' -e 's/$/-linux/')

cat &gt; /etc/profile.d/texlive.sh &lt;&lt; EOF
<literal># Begin texlive setup
TEXLIVE_PREFIX=/opt/texlive/&texlive-year;
export TEXLIVE_PREFIX

pathappend \$TEXLIVE_PREFIX/texmf-dist/doc/man  MANPATH
pathappend \$TEXLIVE_PREFIX/texmf-dist/doc/info INFOPATH
pathappend \$TEXLIVE_PREFIX/bin/$TEXARCH

# End texlive setup</literal>
EOF

unset TEXARCH</userinput></screen>

  <note>
    <para>
      Les chemin MANPATH et INFOPATH standards sont spécifiés ci-dessus pour
s'assurer qu'ils sont inclus. S'ils sont déjà définis dans la procédure du
script de démarrage, la fonction pathappend s'assurera de supprimer les
doublons, donc les inclure ici ne cause aucun problème.
    </para>
  </note>

  <note>
    <para>
      Les antislashs avant les signes dollar dans le script ci-dessus permettent
de faciliter le copier-coller. Les antislashs ne devraient pas apparaître
dans le script final.
    </para>
  </note>

  <para>
    Les nouveaux chemins peuvent être activés immédiatement en lançant&nbsp;:
  </para>

<screen><userinput>source /etc/profile</userinput></screen>

  <para>
    The shared libraries will be installed into a texlive directory.  As the
<systemitem class="username">root</systemitem> user, add it to your
<filename>/etc/ld.so.conf</filename>:
  </para>

<screen role="root"><userinput>cat &gt;&gt; /etc/ld.so.conf &lt;&lt; EOF
<literal># Begin texlive addition

$TEXLIVE_PREFIX/lib

# End texlive addition</literal>
EOF</userinput></screen>

  <para>
    Vous pouvez maintenant continuer soit par <xref linkend="tl-installer"/>
pour une installation binaire de texlive, ou par <xref linkend="texlive"/>
pour commencer une installation depuis les sources.
  </para>

</sect1>
