<sect2>
<title>Explication des commandes</title>

<para><userinput>-prefix /opt/qt-&qt-version;</userinput>: Cette commande
indique la destination de l'installation.</para>

<para><userinput>-qt-gif</userinput>: Cette commande ajoute le support des
fichiers gif aux bibliothèques.</para>

<para><userinput>-system-libpng -system-libmng -system-zlib
-system-libjpeg</userinput>: Cette commande force la bibliothèque à utiliser
les bibliothèques partagées présentes sur le système au lieu de recréer son
propre ensemble de bibliothèques de support de ces fonctions.</para>

<para><userinput>-no-g++-exceptions</userinput>: Cette commande désactive le
codage des exceptions générées par le compilateur C++.</para>

<para><userinput>-thread</userinput>: Cette commande compile la bibliothèque
avec le support du multi-threading.</para>

<para><userinput>if test -L /opt/qt; then rm /opt/qt; fi</userinput> : Cette
commande élimine le lien si il existe déjà pour que la commande suivante
s'exécute correctement.</para>

<para><userinput>ln -s /opt/qt-&qt-version; /opt/qt</userinput>: Cette commande
normalise l'emplacement des bibliothèques qt dans l'entrée ld.so.conf décrit
ci-dessous.</para>

<para><userinput>cp -r doc/man /opt/qt/doc</userinput>: Cette commande installe
le répertoire man qui est oublié par le make install.</para>

<para><userinput>cp -r examples /opt/qt/doc</userinput>: Cette commande installe
le répertoire d'exemples oublié par make install.</para>

</sect2>
