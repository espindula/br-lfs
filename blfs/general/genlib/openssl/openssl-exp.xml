<sect2>
<title>Explications des commandes</title>

<para><option>no-rc5 no-idea</option> : Une fois ajoutée à la commande
<command>./config</command>, ceci éliminera la construction de ces méthodes de cryptage.
Les licences en question peuvent vous être nécessaire pour que vous puissiez les
utiliser dans vos projets.</para>

<para><command>rm doc/apps/passwd.pod</command> : Cette commande empêche
<application>Open<acronym>SSL</acronym></application> d'écraser la
page man passwd existante par sa propre page man.</para>

<para><command>mv doc/crypto/{,openssl_}threads.pod</command>: This
commands prevents <application>Open<acronym>SSL</acronym></application> from
overwriting an existing man page from <application>Perl</application>.</para>

<para><command>sed -i -e 's/-m486/-march=i486/' Configure</command>:
<application>GCC</application> issues a warning on every
compilation because the <command>Configure</command> 
command uses deprecated <parameter>-m486</parameter> instead of
<parameter>-march=i486</parameter>.</para>

<para><screen><command>make MANDIR=/usr/share/man
make MANDIR=/usr/share/man install</command></screen> :
Ces commandes installent <application>OpenSSL</application> avec les pages man
dans <filename class="directory">/usr/share/man</filename> au lieu de
<filename class="directory">/etc/ssl/man</filename>, par défaut.</para>
 
<para><command>cp -r certs /etc/ssl</command>: The certificates
must be copied manually since the install script
skips this step.</para>
 
<para><command>rmdir /etc/ssl/lib</command> : Il s'agit d'une simple
commande de ménage. Pour une raison inconnue, la routine d'installation
d'<application>OpenSSL</application> crée le répertoire <filename
class="directory">>/etc/ssl/lib</filename> même si les
bibliothèques ont été installées dans <filename
class="directory">/usr/lib</filename>. Nous le supprimons pour garder les choses
jolies et bien rangées!</para>

</sect2>

