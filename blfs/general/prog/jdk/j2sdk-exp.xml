<?xml version="1.0" encoding="ISO-8859-1"?>
<sect2>
<title>Explication des commandes</title>

<para><command>export ALT_BOOTDIR="$JAVA_HOME"</command> : Cette variable
indique l'emplacement du bootstrap <acronym>JDK</acronym>.</para>

<para><command>export ALT_MOZILLA_PATH=$PWD</command> : Cette variable
pointe vers l'emplacement où vous avez décompressé les entêtes de
<application>Mozilla</application>.</para>

<para><command>export ALT_DEVTOOLS_PATH="/usr/bin"</command> : Ceci modifie
l'emplacement où l'outil de construction trouvera les exécutables nécessaires.
</para>

<para><command>export BUILD_NUMBER="blfs-`date +%s`"</command> : Ceci vous
aidera à identifier la version, utilisée lors de la compilation, de
l'environnement d'exécution et de la machine virtuelle en ajoutant cette
information à la version donnée par <command>java -version</command>.</para>

<para><command>export DEV_ONLY=true</command> : Cette commande empêche la
compilation de la documentation et élimine une dépendance pour
<application>rpm</application>.</para>

<para><command>unset JAVA_HOME</command> : Ceci supprime la variable
JAVA_HOME pour prévenir contre un mauvais emplacement lors de la compilation.
</para>

<para><command>unset CLASSPATH</command> : Ceci supprime la variable
<envar>CLASSPATH</envar> pour prévenir contre un mauvais emplacement lors de la
compilation.</para>

<para><command>unset CFLAGS...</command> : Ces variables posent problème
lors de la compilation. Ne jamais les laisser.</para>

<para><command>export INSANE=true</command> : Si vous n'indiquez pas que vous
êtes malade, la construction ne continuera pas. La plateforme certifiée pour la
construction est une RedHat 6.1. La variable ci-dessus vous assure que toutes
les erreurs relatives à la compilation sur une plateforme non certifiée seront
changées en messages d'avertissement.</para>

<para><command>export MAKE_VERBOSE=true</command> : Autorise l'affichage de
l'évolution de la compilation sur la console.</para>

<para><command>export ALT_CACERTS_FILE...</command> : Spécifie le certificat
à utiliser.</para>

</sect2>

