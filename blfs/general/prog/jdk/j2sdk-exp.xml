<sect2>
<title>Explication des commandes</title>

<para><userinput>T=`grep...</userinput> : Détermine l'endroit où l'archive tar
commence dans l'archive auto-extractible.</para>

<para><userinput>export ALT_BOOTDIR="$JAVA_HOME"</userinput> : Cette variable
indique l'emplacement du bootstrap JDK.</para>

<para><userinput>export ALT_MOZILLA_PATH=$PWD</userinput> : Cette variable
pointe vers l'emplacement où vous avez décompressé les entêtes de mozilla.
</para>

<para><userinput>export ALT_DEVTOOLS_PATH="/usr/bin"</userinput> : Ceci modifie
l'emplacement où l'outil de construction trouvera les exécutables nécessaires.
</para>

<para><userinput>export BUILD_NUMBER="blfs-`date +%s`"</userinput> : Ceci vous
aidera à identifier la version, utilisée lors de la compilation, de
l'environnement d'exécution et de la machine virtuelle en ajoutant cette
information à la version donnée par "java -version".</para>

<para><userinput>export DEV_ONLY=true</userinput> : Cette commande empêche la
compilation de la documentation et élimine une dépendance pour rpm.</para>

<para><userinput>unset JAVA_HOME</userinput> : Ceci supprime la variable
JAVA_HOME pour prévenir contre un mauvais emplacement lors de la compilation.
</para>

<para><userinput>unset CLASSPATH</userinput> Ceci supprime la variable CLASSPATH
pour prévenir contre un mauvais emplacement lors de la compilation.</para>

<para><userinput>unset CFLAGS...</userinput> : Ces variables posent problème
lors de la compilation. Ne jamais les laisser.</para>

<para><userinput>export INSANE=true:</userinput> Si vous n'indiquez pas que vous
êtes malade, la construction ne continuera pas. La plateforme certifiée pour la
construction est une RedHat 6.1. La variable ci-dessus vous assure que toutes
les erreurs relatives à la compilation sur une plateforme non certifiée seront
changées en messages d'avertissement.</para>

<para><userinput>export MAKE_VERBOSE=true</userinput> : Autorise l'affichage de
l'évolution de la compilation sur la console.</para>

<para><userinput>export ALT_CACERTS_FILE...</userinput> Spécifie le certificat
à utiliser.</para>

</sect2>

