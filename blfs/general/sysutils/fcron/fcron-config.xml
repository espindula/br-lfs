<sect2>
<title>Configurer fcron</title>

<sect3><title>Script make install</title>

<para>Après que les fichiers soient installés, le script 'make install' entre
dans une routine de configuration. Le premier test concerne un groupe nommé
fcron. Si il n'existe pas, une ligne de commande va vous être proposée à
laquelle vous pourrez répondre oui ('y') ou non ('n'). Ceci arrive de nouveau
pour créer un utilisateur nommé fcron et pour installer un script dans le
répertoire init.d avec les liens appropriés dans les niveaux d'exécution
2, 3, 4 et 5. L'étape finale de configuration est d'arrêter tout processus
fcron et d'en démarrer un nouveau. Un oui ('yes') arrêtera les processus
fcron en cours mais échouera pour démarrer le nouveau fcron. La commande
suivante corrigera ceci:</para>

<para><screen><userinput>cat &gt; /etc/rc.d/init.d/fcron &lt;&lt; "EOF"</userinput>
#!/bin/sh
# Début $rc_base/init.d/fcron

# Basé sur le script sysklogd script de LFS-3.1 et précédents.
# Réécrit par Gerard Beekmans  - gerard@linuxfromscratch.org

source /etc/sysconfig/rc
source $rc_functions

case "$1" in
        start)
                echo "Starting fcron..."
                loadproc fcron
                ;;

        stop)
                echo "Stopping fcron..."
                killproc fcron
                ;;

        restart)
                $0 stop
                sleep 1
                $0 start
                ;;

        status)
                statusproc fcron
                ;;

        *)
                echo "Usage: $0 {start|stop|restart|status}"
                exit 1
                ;;
esac

# Fin $rc_base/init.d/fcron
<userinput>EOF
chmod 755 /etc/rc.d/init.d/fcron &amp;&amp;
/etc/rc.d/init.d/fcron start &amp;&amp;
mv /etc/rc.d/rc0.d/K60fcron /etc/rc.d/rc0.d/K08fcron &amp;&amp;
mv /etc/rc.d/rc6.d/K60fcron /etc/rc.d/rc6.d/K08fcron</userinput></screen></para>
</sect3>

<sect3><title>Fichiers de configuration</title>

<para><userinput>/etc/fcron.conf, /etc/fcron.allow,
/etc/fcron.deny</userinput></para>

<para>Aucune changement n'est requis pour tous ces fichiers.
Des informations de configuration peuvent être trouvées dans
la page man de fcron.conf.</para>

<para>Les scripts fcron sont écrits avec fcrontab. Référez-vous
à la page man de fcrontab pour les bons paramètres dans votre
situation.</para></sect3>

</sect2>

