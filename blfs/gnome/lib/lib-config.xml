<sect1 id="gnome-lib-config">
<?dbhtml filename="lib-config.html" dir="gnome"?>
<title>Configuration de pre-installation</title>

<para>Les packages dans ce chapitre requiert les configurations suivantes:
</para>

<para>Ajoutez à votre système ou votre prfile système:</para>

<para><screen>export PATH=$PATH:/opt/gnome/bin:/opt/gnome2/bin
export PKG_CONFIG_PATH=/opt/gnome/lib/pkgconfig:/opt/gnome2/lib/pkgconfig</screen></para>

<para>Ajoutez à votre /etc/ld.so.conf:</para>

<para><screen><userinput>cat &gt;&gt; /etc/ld.so.conf &lt;&lt; "EOF"</userinput>
# Début gnome addition to /etc/ld.so.conf

/opt/gnome/lib
/opt/gnome2/lib

# Fin gnome addition
<userinput>EOF</userinput></screen></para>

<para>Ajoutez à votre /etc/man.conf:</para>

<para><screen><userinput>cat &gt;&gt; /etc/man.conf &lt;&lt; "EOF"</userinput>
# Début gnome addition to man.conf

MANPATH /opt/gnome/man
MANPATH /opt/gnome2/man

# Fin gnome addition to man.conf
<userinput>EOF</userinput></screen></para>



</sect1>
