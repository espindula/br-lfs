<sect1 id="gnome-lib-config">
<?dbhtml filename="lib-config.html" dir="gnome"?>
<title>Configuration de pre-installation</title>

<para>Mettez une variable d'environnement pour résoudre la destination du
préfixe.</para>

<para>Si Gnome est le bureau de votre choix:
<screen>export GNOME_PREFIX=/usr</screen>
Si vous souhaitez essayer Gnome:
<screen>export GNOME_PREFIX=/opt/gnome-&gnome-version;</screen></para>

<para>Le groupe des testeurs aura aussi besoin de faire tous les changements
suivants:</para>

<para>Ajoutez à votre système ou votre profile système:</para>

<para><screen>export PATH=$PATH:/opt/gnome-&gnome-version;/bin
export PKG_CONFIG_PATH=$PKG_CONFIG_PATH:/opt/gnome-&gnome-version;/lib/pkgconfig
export GNOME_LIBCONFIG_PATH=/usr/lib</screen></para>

<para>Ajoutez à votre <filename>/etc/ld.so.conf</filename>:</para>

<para><screen><userinput>cat &gt;&gt; /etc/ld.so.conf &lt;&lt; "EOF"</userinput>
# Début gnome addition to /etc/ld.so.conf

/opt/gnome-&gnome-version;/lib

# Fin gnome addition
<userinput>EOF</userinput></screen></para>

<para>Ajoutez à votre <filename>/etc/man.conf</filename>:</para>

<para><screen><userinput>cat &gt;&gt; /etc/man.conf &lt;&lt; "EOF"</userinput>
# Début gnome addition to man.conf

MANPATH /opt/gnome-&gnome-version;/man

# Fin gnome addition to man.conf
<userinput>EOF</userinput></screen></para>

</sect1>
