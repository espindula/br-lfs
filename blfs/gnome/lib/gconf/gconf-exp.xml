<sect2>
<title>Explication des commandes</title>

<para><userinput>--libexecdir=$GNOME_PREFIX/sbin</userinput> : Cette commande
place les fichiers libexec dans <filename>$GNOME_PREFIX/sbin</filename> au lieu
de <filename>$GNOME_PREFIX/libexec</filename>.</para>

<para><userinput>--sysconfdir=/etc/gnome</userinput> : Cette commande place les
fichiers de configuration dans <filename>/etc/gnome</filename> au lieu de
<filename>$GNOME_PREFIX/etc</filename>. Cette installation contrôle toutes les
futures installations de schéma, modifications d'emplacemenent, en sachant que
la suppression de cette commande <emphasis>doit</emphasis> être consistant.
</para>

</sect2>

