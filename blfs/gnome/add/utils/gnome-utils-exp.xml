<sect2>
<title>Explication des commandes</title>

<para><userinput>--libexecdir=$GNOME_PREFIX/sbin</userinput> : Cette commande
place les fichiers libexec dans <filename>$GNOME_PREFIX/sbin</filename> au lieu
de <filename>$GNOME_PREFIX/libexec</filename>.</para>

<para><userinput>--localstatedir=/var/lib</userinput> : Cette commande place les
fichiers scrollkeeper dans <filename>/var/lib/scrollkeeper</filename> au lieu
de <filename>$GNOME_PREFIX/var/scrollkeeper</filename>.</para>

<para><userinput>--sysconfdir=/etc/gnome</userinput> : Cette commande place les
fichiers de configuration dans <filename>/etc/gnome</filename> au lieu de
<filename>$GNOME_PREFIX/etc</filename>.</para>

<para><userinput>--with-pam-prefix=/etc/pam.d</userinput> : Cette option met les
fichiers PAM dans <filename>/etc/pam.d</filename> au lieu de
<filename>/etc/gnome</filename>.</para>

</sect2>

