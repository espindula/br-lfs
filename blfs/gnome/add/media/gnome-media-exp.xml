<sect2>
<title>Explication des commandes</title>

<para><parameter>--sysconfdir=/etc/gnome</parameter> : Cette commande place les
fichiers de configuration dans <filename class="directory">/etc/gnome</filename>
au lieu de <filename class="directory">$GNOME_PREFIX/etc</filename>.</para>

<para><parameter>--localstatedir=/var/lib</parameter> : Cette commande place les
fichiers <application>ScrollKeeper</application> dans <filename
class="directory">/var/lib/scrollkeeper</filename> au lieu de <filename
class="directory">$GNOME_PREFIX/var/scrollkeeper</filename>.</para>

<para><parameter>--libexecdir=$GNOME_PREFIX/sbin</parameter> : Cette commande
place les fichiers libexec dans <filename class="directory">$GNOME_PREFIX/sbin</filename>
au lieu de <filename class="directory">$GNOME_PREFIX/libexec</filename>.</para>

</sect2>

