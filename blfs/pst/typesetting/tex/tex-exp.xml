<sect2>
<title>Explications des commandes d'installation</title>

<para><screen><userinput>gzip -dc ../teTeX-texmf-&tex-version;.tar.gz \
		| (umask 0; cd /usr/share/texmf; tar xvf -)</userinput></screen> :
Déballage des polices TeX et des bibliothèques de macro.</para>

<para><userinput>--with-x=no</userinput> : Evite toute dépendance avec
X-Windows. TeX peut être compilé avec le support de X-Windows, principalement
pour xdvi. Si ceci est désiré, supprimez cette option de configure.</para>

<para><userinput>--exec-prefix=/usr --bindir=/usr/bin</userinput> :
Nécessaire pour que les binaires TeX soient installés dans
<filename>/usr/bin</filename>.</para>

<para><userinput>--without-texinfo</userinput> : Une installation LFS par défaut
dispose déjà du package texinfo, nous éviterons de le remplacer avec le package
texinfo inclus.</para> 

<para><userinput>--with-system-ncurses</userinput> : Demande l'utilisation
de la bibliothèque ncurses déjà installée.</para>

<para><userinput>--with-system-zlib</userinput> : Les systèmes LFS, à partir
des versions 4.0, disposent de zlib installé avec la base du système
d'exploitation. Nous devons éviter de le construire ici.</para> 

<para><userinput>texconfig dvips paper letter</userinput> : Configure la taille
du papier par défaut pour TeX.</para>

<para><userinput>texconfig font rw</userinput> : Spécifie les polices
modifiables.</para>

</sect2>
