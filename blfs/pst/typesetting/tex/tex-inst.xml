<sect2>
<title>Installation de TeX</title>

<para>TeX est installé légèrement différemment qu'un package GNU typique; ces
instructions assumeront que /usr/src est le répertoire de travail courant,
contenant les trois archives tar TeX.</para>

<para>Installez TeX en lançant les commandes suivantes:</para>

<para><screen><userinput>
mkdir -p /usr/share/texmf &amp;&amp;
tar zxvf teTeX-src-&tex-version;.tar.gz &amp;&amp;
cd teTeX-&tex-version; &amp;&amp;
gzip -dc ../teTeX-texmf-&tex-version;.tar.gz \
      | (umask 0; cd /usr/share/texmf; tar xvf -)
</userinput></screen></para>

<para>Si l'archive tar du code source optionnel texmf était téléchargé,
déballez-la maintenant:</para>
<para><screen><userinput>      
gzip -dc ../teTeX-texmfsrc-&tex-version;.tar.gz \
      | (umask 0; cd /usr/share/texmf; tar xvf -)
</userinput></screen></para>

<para><screen><userinput>      
./configure --with-x=no --prefix=/usr \
    --without-texinfo   --with-system-ncurses --with-system-zlib \
    --exec-prefix=/usr --bindir=/usr/bin &amp;&amp;
make world &amp;&amp;
texconfig dvips paper letter &amp;&amp;
texconfig font rw
</userinput></screen></para>

<note><para>
Les systèmes LFS avant la version 4.0 n'installent pas zlib par défaut.
Supprimez l'option --with-system-zlib des commandes ci-dessus si vous ne voulez
pas installer zlib.</para></note>
</sect2>
