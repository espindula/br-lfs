<sect2>
<title>Configurer MPlayer</title>

<sect3><title>Fichiers de configuration</title>
<para><filename>/etc/mplayer/*</filename>, <filename>~/.mplayer/*</filename></para>
</sect3>

<sect3><title>Informations de configuration</title>

<para>Si vous souhaitez utiliser une police <application>FreeType</application>, vous avez besoin de lier
un fichier <filename>TTF</filename> à votre répertoire <filename
class="directory">~/.mplayer</filename>. Par exemple:
<screen><userinput><command>ln -sf /usr/X11R6/lib/X11/fonts/TTF/luxisri.ttf /etc/mplayer/subfont.tt</command></userinput></screen>
ou
otherwise
<screen><userinput><command>ln -sf /usr/share/mplayer/font/font-arial-*-iso-8859-1 /etc/mplayer/font</command></userinput></screen>
Vous pouvez choisir la taille parmi 14, 18, 24
et 28.</para>

<para>Configuré l'interface utilisateur avec les commandes suivantes exécutées
à partir de votre répertoire source:</para>

<screen><userinput><command>cat &gt; /etc/mplayer/config &lt;&lt; "EOF"</command>
gui = yes
skin = default
<command>EOF</command></userinput></screen>

<para>La première ligne, <userinput>gui = yes</userinput>, fera que MPlayer se
lancera en mode <acronym>GUI</acronym> automatiquement. Si vous souhaitez
décider s'il doit se lancer en mode <acronym>GUI</acronym> ou non, oubliez cette
ligne et lancez <command>mplayer</command> avec <command>-gui</command> ou
lancez <command>gmplayer</command> (c'est-à-dire lorsque vous le lancez à partir
d'un menu du gestionnaire de fenêtre ou à l'invite). Notez que le fichier de
configuration principal est appelé <filename>config</filename> s'il est placé
dans le répertoire <filename class="directory">~/.mplayer</filename>.</para>

</sect3>

</sect2>

