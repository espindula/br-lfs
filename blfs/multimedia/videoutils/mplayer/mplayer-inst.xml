<?xml version="1.0" encoding="ISO-8859-1"?>
<sect2>
<title>Installation de <application>MPlayer</application></title>

<para>Si vous avez téléchargé des <acronym>CODEC</acronym> propriétaires,
déballez-les sur un emplacement système (comme <filename
class="directory">/usr/lib/mplayer/codecs</filename>) et ajoutez l'option
<parameter>--with-codecsdir=<replaceable>[emplacement]</replaceable></parameter>
au script configure.</para>

<note><para>Les mainteneurs du package recommendent de construire sans
optimisation</para></note>

<para>Installez <application>MPlayer</application> en lançant les commandes
suivantes:</para>

<screen><userinput><command>./configure --prefix=/usr --confdir=/etc/mplayer \
    --enable-largefiles --enable-shared-pp &amp;&amp;
make &amp;&amp;
make install &amp;&amp;
make -C libavcodec/libpostproc install</command></userinput></screen>

<para>Vous aurez besoin de <filename>codecs.conf</filename> uniquement si vous
voulez modifier ses propriétés car le binaire principal en contient une copie
interne&nbsp;:</para>

<screen><userinput><command>cp etc/codecs.conf /etc/mplayer</command></userinput></screen>

<para>La version <acronym>GUI</acronym> de <application>MPlayer</application>
requiert un thème. Déballez-le dans l'emplacement recommendé&nbsp;:</para>

<screen><userinput><command>install -d /usr/share/mplayer/Skin &amp;&amp;
tar xjvf ../default-1.8.tar.bz2 -C /usr/share/mplayer/Skin</command></userinput></screen>

<sect3>
<title>Installation de la lecture de <acronym>DVD</acronym></title>

<para>Si vous voulez lire des <acronym>DVD</acronym> avec
<application>MPlayer</application>, vous avez besoin de faire un
lien partant de votre lecteur de <acronym>DVD</acronym> vers
<filename>/dev/dvd</filename>:</para>

<screen><userinput><command>ln -s /dev/&lt;dvd drive&gt; /dev/dvd</command></userinput></screen>

<para>Remplacez <replaceable>[lecteur dvd]</replaceable> par le périphérique
adéquate, par exemple <filename>/dev/hdc</filename>. Si vous ne savez pas quel
péripérique choisir, tapez:</para>

<para><screen><userinput>dmesg | grep DVD</userinput></screen></para>

<para>Vous devriez obtenir:</para>

<screen><userinput>hdc: Pioneer DVD-ROM ATAPIModel DVD-114 0110,
ATAPI CD/DVD-ROM drive</userinput></screen>

<para>Si vous avez une émulation SCSI activé pour les lecteurs, vous aurez
besoin d'obtenir le bon périphérique SCSI. Chaque lecteur CD/DVD est attribué
dans le même ordre que les périphériques <acronym>IDE</acronym> de cette façon
<filename>/dev/scd0</filename>, <filename>/dev/scd1</filename> et ainsi de
suite.</para>

</sect3>

</sect2>

