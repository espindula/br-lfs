<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;
]>
<sect1 id="kde-pre-install-config">
  <?dbhtml filename="pre-install-config.html"?>
  <sect1info>
    <othername>$LastChangedBy&nbsp;: rthomsen $</othername>
    <date>$Date&nbsp;: 2012-04-15 19:19:58 +0200 (Sun, 15 Apr 2012) $</date>
  </sect1info>
  <title>configuration de la pré-installation de KDE</title>
  <note><para>Si vous n'avez pas installé <application>Xorg</application> dans
  <filename class="directory">/usr</filename>, certain des modules  
  <application>CMake</application> dans <application>KDE</application>
  cherche des paquets dans des chemins codés en durs. Pour résoudre ce problème créé les liens symboliques suivants
  (en tant qu'utilisateur root):</para>
<screen>
<userinput>ln -sv $XORG_PREFIX /usr/X11R6</userinput>
</screen>
  </note>
<sect2><title>Installation dans /usr</title>
    <para>Une option est de mettre <application>KDE</application> dans la hiérarchie
    <filename class='directory'>/usr</filename>.  Cela va créer un setup plus simple
    mais il sera plus difficile d'essayer des versions multiples de
    <application>KDE</application>.</para>
<screen>
<userinput>export &kde-dir-var;=/usr</userinput>
</screen>
  </sect2>
  
  <sect2><title>Installation dans /opt</title>
    <para>Une méthode pour construire des multiples versions est d'installer
    <application>KDE</application> dans la hiérarchie <filename
    class='directory'>/opt</filename>&nbsp;:</para>
<screen>
<userinput>export &kde-dir-var;=/opt/kde</userinput>
</screen>
    <para>Si vous n'avez pas installé <application>KDE</application> dans
    <filename class="directory">/usr</filename>, vous aurez besoin de faire quelques
	changement supplémentaire de la configuration. Une bonne pratique est de les ajouter dans votre
	profil système ou personnel:</para>
<screen>
<userinput>cat &gt; /etc/profile.d/kde.sh &lt;&lt; 'EOF'
<literal># Begin /etc/profile.d/kde.sh
&kde-dir-var;=/opt/kde
KDEDIR=&kde-dir;
pathappend &kde-dir;/bin             PATH
pathappend &kde-dir;/lib/pkgconfig   PKG_CONFIG_PATH
pathappend &kde-dir;/share/pkgconfig PKG_CONFIG_PATH
pathappend &kde-dir;/share           XDG_DATA_DIRS
pathappend /etc/kde/xdg                XDG_CONFIG_DIRS
export &kde-dir-var; KDEDIR
# End /etc/profile.d/kde.sh</literal>
EOF
</userinput>
</screen>
    <para>Ajouter dans votre <filename>/etc/ld.so.conf</filename>&nbsp;:</para>
<screen role="root">
<userinput>cat &gt;&gt; /etc/ld.so.conf &lt;&lt; EOF
<literal># Begin kde addition
/opt/kde/lib
# End kde addition</literal>
EOF</userinput>
</screen>
    <indexterm zone="kde-pre-install-config">
      <primary sortas="e-etc-ld.so.conf">/etc/ld.so.conf</primary>
    </indexterm>
    <para>Quelques paquets KDE installent des fichiers dans les répertoires D-BUS et polkit.
    Quand on installe KDE ailleurs que dans <filename class="directory">/usr</filename>,
    D-Bus et polkit ont besoin de trouver ces fichiers. La façon la plus simple de le faire est de créer
    les liens symboliques suivants (en tant qu'utilisateur <systemitem class="username">root</systemitem> )&nbsp;:</para>
<screen role="root">
<userinput>install -d &kde-dir;/share &amp;&amp;
ln -svf /usr/share/dbus-1 &kde-dir;/share &amp;&amp;
ln -svf /usr/share/polkit-1 &kde-dir;/share</userinput>
</screen>
    
     <tip>
      <para>Parfois, les chemins d'installation sont corrigés dans les fichies installés.
	  C'est la raison pourquoi <filename class="directory">/opt/kde</filename>
      est utilisé comme préfixe d'installation au lieu de <filename class="directory">/opt/kde-&kde-version;</filename>.
      Après l'installation de <application>KDE</application>, vous pouvez considérer de renommer le répertoire
	  et créer un lien symbolique:</para>
<screen role="root">
<userinput>mv /opt/kde{,-&kde-version;} &amp;&amp;
ln -svf kde-&kde-version; /opt/kde</userinput>
</screen>
      <para>Plus tard, vous pouvez vouloir installer d'autres versions de <application>KDE</application>.
      Pour faire cela, il suffit d'effacer le lien symbolique et utiliser <filename class="directory">/opt/kde</filename>
      de nouveau comme préfixe (<application>KDE</application> ne doit pas être démarré). La version de
      <application>KDE</application> que vous utiliserez dépendra uniquement de la cible du lien
	  symbolique. Aucune autre configuration ne sera nécessaire.</para>
    </tip>
  </sect2>
  
</sect1>
