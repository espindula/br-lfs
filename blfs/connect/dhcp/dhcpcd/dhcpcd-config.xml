<?xml version="1.0" encoding="ISO-8859-1"?>
<sect2>
<title>Configurer <application>dhcpcd</application></title>

<sect3><title>Fichiers de configuration</title>
<para><filename>/var/lib/dhcpcd/*</filename></para>
</sect3>

<sect3><title>Informations de configuration</title>

<para>Pour configurer <command>dhcpcd</command>, vous avez tout d'abord besoin
d'installer le script de service réseau,
<filename>/etc/sysconfig/network-devices/services/dhcpcd</filename>
inclus dans le paquetage <xref linkend="intro-important-bootscripts"/>.</para>

<screen><userinput><command>make install-service-dhcpcd</command></userinput></screen>

<para>Maintenant, créez le fichier de configuration
<filename>ifconfig.eth0</filename> en utilisant les commandes suivantes.
Ajustez de manière appropriée pour les interfaces supplémentaires.</para>

<para><emphasis>Note</emphasis>&nbsp;: ceci écrasera tout fichier déjà
existant.</para>

<screen><userinput><command>cat &gt; /etc/sysconfig/network-devices/ifconfig.eth0 &lt;&lt; "EOF"</command>
ONBOOT=yes
DHCP_START="<replaceable>[insérez ici les options de lancement
appropriées]</replaceable>"
DHCP_STOP="-k <replaceable>[insérez ici les options d'arrêt
appropriées]</replaceable>"
SERVICE=dhcpcd
<command>EOF</command></userinput></screen>

<para>Pour plus d'information sur les bonnes valeurs de
<envar>DHCP_START</envar> et <envar>DHCP_STOP</envar>, examinez la page de
manuel pour <command>dhcpcd</command>.</para>

<para>Enfin, si l'interface <acronym>DHCP</acronym> configurée est utilisée
comme passerelle par défaut, vous devez supprimer les variables GATEWAY et
GATEWAY_IF de <filename>/etc/sysconfig/network</filename>&nbsp;:</para>

<screen><userinput><command>sed -i "s/GATEWAY/#&amp;/" /etc/sysconfig/network</command></userinput></screen>

</sect3>

</sect2>

