<sect1 id="connect-dhcp-client">
<?dbhtml filename="dhcpclient.html" dir="connect"?>
<title>dhcp-&dhcp-version;</title>

<para>Le package DHCP fournit à la fois un programme client et un
programme serveur pour utiliser DHCP. Si vous voulez l'installer, les
instructions se trouvent sur cette page <xref linkend="dhcp"/>.
Notez que, si vous n'utilisez que le client, vous n'avez
<emphasis>pas</emphasis> besoin de lancer le serveur, et donc vous n'avez
besoin ni du script de démarrage ni des liens associés au démon du serveur.
Notez que pour utiliser le client, vous devez modifier votre script
<filename>ethnet</filename> comme décrit dans la prochaine section.
Néanmoins, vous avez seulement besoin de lancer le serveur DHCP si vous
proposez ce service sur un réseau, et vous devez savoir si c'est le cas;
dans le cas contraire, ne lancez pas le serveur!</para>

<para>Pour configurer dhclient, vous devez créer les deux fichiers,
<filename>/etc/sysconfig/network-devices/ifconfig.eth0</filename> et
<filename>/etc/dhclient.conf</filename>.</para>

<para>Tout d'abord, créez le fichier <filename>ifconfig.eth0</filename> avec
les commandes suivantes (notez que ceci va écraser tout fichier existant):
</para>

<para><screen><userinput>cd /etc/sysconfig/network-devices &amp;&amp;
cat &gt; ifconfig.eth0 &lt;&lt; "EOF"</userinput>
ONBOOT=yes
DHCP_PROG=/sbin/dhclient
DHCP_START=&lt;appropriate start parameters&gt;
DHCP_STOP=-r
<userinput>EOF</userinput></screen></para>

<para>Pour plus d'informations sur les valeurs appropriées
<userinput>DHCP_START</userinput> et <userinput>DHCP_STOP</userinput>,
examinez la page main pour dhclient.</para>

<para>Ensuite, vous devez créer le fichier
<filename>/etc/dhclient.conf</filename> en utilisant la commande
suivante:</para>

<para><screen><userinput>cat &gt; /etc/dhclient.conf &lt;&lt; "EOF"</userinput>
# dhclient.conf

interface "eth0"{
prepend domain-name-servers 127.0.0.1;
request subnet-mask, broadcast-address, time-offset, routers,
        domain-name, domain-name-servers, host-name;
require subnet-mask, domain-name-servers;
}
# end dhclient.conf
<userinput>EOF</userinput></screen></para>

</sect1>
