<sect2>
<title>Explication des commandes d'installation</title>

<para><command>install -d /var/cache/samba</command>&nbsp;: ce répertoire est
nécessaire pour que les démons <command>smbd</command> et <command>nmbd</command>
fonctionnent correctement.</para>

<para><parameter>--sysconfdir=/etc</parameter>&nbsp;: initialise le répertoire
des fichiers de configuration pour éviter la valeur par défaut, <filename
class="directory">/usr/etc</filename>.</para>

<para><parameter>--localstatedir=/var</parameter>&nbsp;: initialise le répertoire
des données variables pour éviter la valeur par défaut, à savoir
<filename class="directory">/usr/var</filename>.</para>

<para><option>--with-fhs</option>&nbsp;: affecte tous les autres chemins de fichiers
pour une compatibilité avec le <acronym>FHS</acronym>.</para>

<para><option>--with-smbmount</option>&nbsp;: ordonne la création d'un binaire
supplémentaire utilisé par la commande <command>mount</command> de façon à
ce que le montage de partages <acronym>SMB</acronym> (Windows) soit aussi peu
complexe que les partages distants <acronym>NFS</acronym>.</para>

<para><command>install -m755 nsswitch/libnss_win{s,bind}.so /lib</command>&nbsp;:
les bibliothèques nss ne sont pas installées par défaut. Si vous comptez utiliser
winbindd pour l'authentification de domaines et/ou la résolution de noms
<acronym>WINS</acronym>, vous aurez besoin de ces bibliothèques.</para> 

<para><command>ln -sf libnss_winbind.so /lib/libnss_winbind.so.2</command> 
et <command>ln -sf libnss_wins.so /lib/libnss_wins.so.2</command>&nbsp;:
ces liens symboliques sont requis pour que glibc utilise les bibliothèques nss.
</para>

<para><command>cp ../examples/smb.conf.default /etc/samba</command>&nbsp;:
ceci copie un fichier <filename>smb.conf </filename> par défaut dans
<filename>/etc/samba</filename>. Cet exemple de configuration ne fonctionnera pas
tant qu'il ne sera pas édité pour votre site et tant qu'il ne sera pas renommé
<filename>smb.conf</filename>.</para>

</sect2>
