<?xml version="1.0" encoding="ISO-8859-1"?>
<sect2>
<title>Installation de <application>Samba</application></title>

<para>Installez <application>Samba</application> en lançant les commandes
suivantes&nbsp;:</para>

<screen><userinput><command>patch -Np1 -i samba-3.0.4-maintainer.patch &amp;&amp;
cd source &amp;&amp;
install -d /var/cache/samba &amp;&amp;
./configure \
    --prefix=/usr \
    --sysconfdir=/etc \
    --localstatedir=/var \
    --with-piddir=/var/run \
    --with-fhs \
    --with-smbmount &amp;&amp;
make &amp;&amp;
make install &amp;&amp;
install -m755 nsswitch/libnss_win{s,bind}.so /lib &amp;&amp;
ln -sf libnss_winbind.so /lib/libnss_winbind.so.2 &amp;&amp;
ln -sf libnss_wins.so /lib/libnss_wins.so.2 &amp;&amp;
cp ../examples/smb.conf.default /etc/samba</command></userinput></screen>

<note><para>Vous pouvez lancer <command>configure</command> avec le paramètre
<parameter>--help</parameter>. D'autres paramètres peuvent être nécessaires pour
tenir compte des dépendances en option.</para></note>

</sect2>
