<sect2>
<title>Explication de la configuration des commandes</title>
<para><screen><userinput>groupadd -g 200 named
useradd -m -g named -u 200 -s /bin/false named
cd /home/named
mkdir -p dev etc/namedb/slave var/run
mknod /home/named/dev/null c 1 3
mknod /home/named/dev/random c 1 8
chmod 666 /home/named/dev/{null,random}
mkdir /home/named/etc/namedb/pz
cp /etc/localtime /home/named/etc : </userinput></screen>

Créez l'utilisateur non privilégié et le groupe named, avec les fichiers
périphériques que named accédera de l'intérieur de la prison chroot.</para>

<para><userinput>
cat &gt; /home/named/etc/named.conf &lt;&lt; "EOF" :</userinput>
Créez le fichier de configuration BOND, d'où named lira l'emplacement des
fichiers zone, noms de serveur root et clés DNS sécurisés.</para>
<para><userinput>
cat &gt; /home/named/etc/namedb/pz/127.0.0 &lt;&lt; "EOF" :</userinput>
Créez un seul fichier zone.</para>
<para><userinput>
cat &gt; /home/named/etc/namedb/root.hints &lt;&lt; "EOF" :</userinput>
Le fichier root.hints est une liste des noms de serveurs root. Ce fichier doit
être mis à jour périodiquement avec l'utilitaire dig. Consultez le manuel de
référence de l'administrateur BIND 9 pour plus de détails.</para>
<para><userinput>
cat &gt; /etc/rndc.conf &lt;&lt; "EOF" :</userinput>
Le fichier rndc.conf contient l'information de contrôle les opérations de named
avec l'utilitaire rndc.</para>

<para><userinput>
cat &gt; /etc/resolv.conf &lt;&lt; "EOF" :</userinput>
Le fichier resolv.conf spécifiera l'hôte local (127.0.0.1) en tant que serveur
de noms.</para>

<para><userinput>
cat &gt; /etc/rc.d/init.d/bind &lt;&lt; "EOF" :</userinput>
Créez le script de démarrage pour BOND 9, utilisé pour lancer et arrêter le
démon de serveur de noms, named.</para>

</sect2>

