<?xml version="1.0" encoding="ISO-8859-1"?>
<sect2>
<title>Installation de <application>Courier</application></title>

<para>Avant de compiler le programme, vous devez créer les utilisateurs et
groupes qui seront attendues lors de l'exécution du script d'installation.
Ajoutez les utilisateurs et groupes avec les commandes suivantes:</para>

<screen><userinput><command>groupadd courier &amp;&amp;
useradd -c 'Courier Mail Server' -d /dev/null -g courier -s /bin/false courier</command></userinput></screen>

<para>Vous avez aussi besoin de créer un utilisateur bin. Si vous avez déjà
cet utilisateur, cette étape peut être ignorée.</para>

<screen><userinput><command>useradd -c 'bin' -d /dev/null -g bin -u 1 bin</command></userinput></screen>

<para>Ceci crée un répertoire
<filename class="directory">/var/run/courier</filename> où tous les fichiers
pid du <acronym>MTA</acronym> <application>Courier</application> existeront.
Ceci permettra la création de <filename>courierfilter.pid</filename>. Sans
l'exécution de <command>courierfilter</command>, le MTA
<application>Courier</application> ne fonctionnera pas. Lancez les commandes
suivantes pour créer
<filename class="directory">/var/run/courier</filename>&nbsp;:</para>

<screen><userinput><command>mkdir /var/run/courier &amp;&amp;
chown courier:courier /var/run/courier</command></userinput></screen>

<para>Installez <application>courier</application> en lançant les commandes
suivantes&nbsp;:</para>

<screen><userinput><command>./configure --prefix=/usr --libexecdir=/usr/lib/courier \
    --datadir=/usr/share/courier --sysconfdir=/etc/courier \
    --localstatedir=/var/lib/courier --with-piddir=/var/run/courier \
    --disable-root-check --with-mailuser=courier --with-mailgroup=courier \
    --with-paranoid-smtpext --disable-autorenamesent \
    --enable-workarounds-for-imap-client-bugs --with-db=gdbm &amp;&amp;
make &amp;&amp;
make install &amp;&amp;
make install-configure</command></userinput></screen>


</sect2>

