<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-partitioning-aboutlfs">
  <?dbhtml filename="aboutlfs.html"?>

  <title>Configurando a Variável $LFS e o Umask</title>

  <para>Ao longo deste livro, a variável de ambiente <envar>LFS</envar> será usada
muitas vezes. Você deveria se assegurar de que essa variável sempre está
definida no decorrer do processo de construção do LFS. Ela deveria ser
configurada para o nome do diretório onde você estará construindo seu
sistema LFS - nós usaremos <filename class="directory">/mnt/lfs</filename>
como um exemplo, porém você possivelmente escolha qualquer nome de diretório
que queira. Se você está construindo o LFS em uma partição separada, esse
diretório será o ponto de montagem para a partição. Escolha um local de
diretório e configure a variável com o seguinte comando:</para>

<screen role="nodump"><userinput>export LFS=<replaceable>/mnt/lfs</replaceable></userinput></screen>

  <para>Ter essa variável configurada é benéfico naqueles comandos, tais como
<command>mkdir -v $LFS/tools</command>, que podem ser digitados
literalmente. O shell automaticamente substituirá <quote>$LFS</quote> por
<quote>/mnt/lfs</quote> (ou para o que a variável foi configurada) quando
processar a linha de comando.</para>

  <para>Agora configure a máscara de criação do modo de arquivo (umask) para
<literal>022</literal> caso a distribuição anfitriã use um padrão diferente:</para>

<screen role="nodump"><userinput>umask 022</userinput></screen>

  <para>Configurar o umask como 022 garante que os arquivos e diretórios
recém-criados sejam escrevíveis somente pelos(as) donos(as) deles, mas sejam
legíveis e pesquisáveis (somente para diretórios) por qualquer pessoa
(assumindo que os modos padrão são usados pela chamada de sistema <ulink
role='man' url='&man;open.2'>open(2)</ulink>, novos arquivos acabarão com o
modo de permissão 644 e diretórios com o modo 755). Um padrão excessivamente
permissivo pode deixar buracos de segurança no sistema LFS, e um padrão
excessivamente restritivo pode causar problemas estranhos ao construir ou
usar o sistema LFS.</para>

  <caution>
  <para>Não se esqueça de verificar se <envar>LFS</envar> está configurada e se o
umask está configurado como <literal>022</literal> sempre que você sair e
reentrar no ambiente de trabalho atual (como ao fazer um
<command>su</command> para <systemitem class="username">root</systemitem> ou
outro(a) usante). Verifique se a variável <envar>LFS</envar> está
configurada corretamente com:</para>

<screen role="nodump"><userinput>echo $LFS</userinput></screen>

  <para>Certifique-se de que a saída gerada mostre o caminho para o local de
construção do teu sistema LFS, que é <filename
class="directory">/mnt/lfs</filename> se o exemplo fornecido foi seguido.</para>

  <para>Verifique se o umask está configurado corretamente com:</para>

  <screen role="nodump"><userinput>umask</userinput></screen>

  <para>A saída gerada possivelmente seja <computeroutput>0022</computeroutput> ou
<computeroutput>022</computeroutput> (o número de zeros à esquerda depende
da distribuição anfitriã).</para>

  <para>Se qualquer saída gerada desses dois comandos estiver incorreta, use o
comando dado anteriormente nesta página para configurar <envar>$LFS</envar>
como o nome correto de diretório e configurar umask como
<literal>022</literal>.</para></caution>

  <note><para>Uma maneira de garantir que a variável <envar>LFS</envar> e o umask estejam
sempre configuradas corretamente é a de editar o arquivo
<filename>.bash_profile</filename> em teu diretório lar pessoal e em
<filename>/root/.bash_profile</filename> e inserir os comandos
<command>export</command> e <command>umask</command> acima. Além disso, o
shell especificado no arquivo <filename>/etc/passwd</filename> para
todos(as) os(as) usantes que precisarem da variável <envar>LFS</envar>
precisa ser bash para garantir que o arquivo
<filename>.bash_profile</filename> seja incorporado como parte do processo
de login.</para>

  <para>Outra consideração é o método que é usado para logar no sistema
anfitrião. Se logar por intermédio de um gerenciador gráfico de exibição, o
<filename>.bash_profile</filename> do(a) usante normalmente não é usado
quando um terminal virtual for iniciado. Nesse caso, adicione os comandos ao
arquivo <filename>.bashrc</filename> para o(a) usante e <systemitem
class="username">root</systemitem>. Adicionalmente, algumas distribuições
usam um teste "if" e não executam as instruções restantes do
<filename>.bashrc</filename> para uma invocação não interativa do
bash. Certifique-se de colocar os comandos antes do teste para uso não
interativo.</para>

  </note>

</sect1>
