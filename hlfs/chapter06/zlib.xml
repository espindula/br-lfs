<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-system-zlib" role="wrap">
  <?dbhtml filename="zlib.html"?>

  <title>Zlib-&zlib-version;</title>

  <indexterm zone="ch-system-zlib">
    <primary sortas="a-Zlib">Zlib</primary>
  </indexterm>

  <sect2 role="package">
    <title/>

    <para>Le paquet Zlib contient des routines de compression et
		décompression utilisées par certains programmes.</para>

    <para>Notes utilisateur&nbsp;: <ulink url="&hlfs-wiki;/zlib"/></para>

  </sect2>

  <sect2 role="installation">
    <title>Installation de Zlib</title>

    <note>
      <para>Zlib est connu pour mal construire sa bibliothèque partagée si
			<envar>CFLAGS</envar> fait partie de l'environnement. En initialisant une
			variable <envar>CFLAGS</envar>, assurez-vous d'ajouter la directive
			<parameter>-fPIC</parameter> à la variable <envar>CFLAGS</envar> pour la 
			durée de la commande configure ci-dessous puis de la 
			supprimer après coup. Ceci n'est un problème que si vous n'avez pas
			ajouté la modification ASLR (-fPIC) aux specs de GCC</para>
    </note>

    <para role="warnings">Nous pouvons ajouter des avertissements de 
		compilateur à Zlib&nbsp;:</para>

<screen role="warnings"><userinput>sed '0,/^# udpate Makefile/s//CFLAGS=&quot;$CFLAGS \\\
    -Wall -Wformat-security -Wwrite-strings -Wpointer-arith \\\
    -Wstrict-prototypes -Wmissing-prototypes -Werror -Wfatal-errors&quot;\n&amp;/' \
    -i.orig configure</userinput></screen>

    <para>Préparez la compilation de Zlib&nbsp;:</para>

<screen><userinput>./configure --prefix=/usr --shared --libdir=/lib</userinput></screen>

    <para>Compilez le paquet&nbsp;:</para>

<screen><userinput>make</userinput></screen>

    <para>Pour tester les résultats, lancez&nbsp;:
    <userinput>make check</userinput>.</para>

    <para>Installez la bibliothèque partagée&nbsp;:</para>

<screen><userinput>make install</userinput></screen>

    <para>La commande précédente a installé un lien symbolique dans
    <filename class="directory">/lib</filename>. Nous allons le supprimer et le
    lier à nouveau vers 
    <filename class="directory">/usr/lib</filename>&nbsp;:</para>

<screen><userinput>rm -v /lib/libz.so
ln -vsf ../../lib/libz.so.&zlib-version; /usr/lib/libz.so</userinput></screen>

  </sect2>

  <sect2 id="contents-zlib" role="content">
    <title>Contenu de Zlib</title>

    <segmentedlist>
      <segtitle>Bibliothèques installées</segtitle>

      <seglistitem>
        <seg>libz.so</seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descriptions courtes</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="libz">
        <term><filename class="libraryfile">libz</filename></term>
        <listitem>
          <para>Contient des fonctions de compression et décompression utilisées
					par certains programmes</para>
          <indexterm zone="ch-system-zlib libz">
            <primary sortas="c-libz">libz</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
