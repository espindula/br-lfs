<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;
]>

<sect1 id="ch-system-zlib" role="wrap">
  <?dbhtml filename="zlib.html"?>

  <title>Zlib-&zlib-version;</title>

  <indexterm zone="ch-system-zlib">
    <primary sortas="a-Zlib">Zlib</primary>
  </indexterm>

  <sect2 role="package">
    <title/>

    <para>Le paquet Zlib contient des routines de compression et
    décompression utilisées par quelques programmes.</para>

  </sect2>

  <sect2 role="installation">
    <title>Installation de Zlib</title>

    <para os="p1">Ce correctif ajoutera -fPIC à notre construction et va nous permettre de construire une bibliothèque
    à la fois statique et partagée&nbsp;:</para>

<screen os="p2"><userinput>patch -Np1 -i ../&zlib-fpic-patch;</userinput></screen>

    <para os="a">Préparez la compilation de Zlib&nbsp;:</para>

<screen os="b"><userinput>CC="gcc -isystem /usr/include" CXX="g++ -isystem /usr/include" \
    LDFLAGS="-Wl,-rpath-link,/lib" ./configure --prefix=/usr --shared</userinput></screen>


    <para os="c">Compilez le paquet&nbsp;:</para>

<screen os="d"><userinput>make</userinput></screen>

    <para os="e">Pour tester les résultats, exécutez&nbsp;:
    <userinput>make check</userinput>.</para>

    <para os="f">Installez le paquet&nbsp;:</para>

<screen os="g"><userinput>make install</userinput></screen>

    <para os="h">La commande a installé deux fichiers <filename class="extension">.so</filename> dans
    <filename class="directory">/usr/lib</filename>. Nous allons les déplacer dans
    <filename class="directory">/lib</filename> p1is le lier à nouveau à
    <filename class="directory">/usr/lib</filename>&nbsp;:</para>

<screen os="i"><userinput>mv -v /usr/lib/libz.so.* /lib
ln -svf ../../lib/libz.so.1 /usr/lib/libz.so</userinput></screen>

    <para os="j">Maintenant, nous corrigeons les droits sur la bibliothèque statique&nbsp;:</para>

<screen os="k"><userinput>chmod -v 644 /usr/lib/libz.a</userinput></screen>

  </sect2>

  <sect2 id="contents-zlib" role="content">
    <title>Contenu de Zlib</title>

    <segmentedlist>
      <segtitle>Bibliothèques installées</segtitle>

      <seglistitem>
        <seg>libz.[a,so]</seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descriptions courtes</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="libz">
        <term><filename class="libraryfile">libz</filename></term>
        <listitem>
          <para>Contient des fonctions de compression et décompression utilisées par
          quelques programmes</para>
          <indexterm zone="ch-system-zlib libz">
            <primary sortas="c-libz">libz</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>

