<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;
]>

<sect1 id="ch-system-readline" role="wrap">
  <?dbhtml filename="readline.html"?>

  <title>Readline-&readline-version;</title>

  <indexterm zone="ch-system-readline">
    <primary sortas="a-Readline">Readline</primary>
  </indexterm>

  <sect2 role="package">
    <title/>

    <para>Le paquet Readline est un ensemble de bibliothèques qui
    offrent des fonctionnalités d'édition de la ligne de commande et
    d'historique.</para>

  </sect2>

  <sect2 role="installation">
    <title>Installation de Readline</title>

    <para os="p1">Le correctif suivant contient des mises à jour issues du mainteneur. Le mainteneur de Readline ne fait ces
    correctifs que pour corriger des problèmes sérieux&nbsp;:</para>

<screen os="p2"><userinput>patch -Np1 -i ../&readline-fixes-patch;</userinput></screen>

    <para os="a">Préparez la compilation de Readline:</para>

<screen os="b"><userinput>./configure --prefix=/usr --libdir=/lib</userinput></screen>

    <para os="c">Compilez le paquet&nbsp:</para>

<screen os="d"><userinput>make SHLIB_XLDFLAGS=-lncurses</userinput></screen>

    <variablelist os="e">
      <title>Voici la signification de l'option de make&nbsp;:</title>

      <varlistentry>
        <term><parameter>SHLIB_XLDFLAGS=-lncurses</parameter></term>
        <listitem>
          <para>Cette option force Readline à se lier à la bibliothèque
          <filename class="libraryfile">libncurses</filename>.</para>
        </listitem>
      </varlistentry>

    </variablelist>

    <para os="f">Ce paquet ne fournit pas de suite de tests.</para>

    <para os="g">Installez le paquet&nbsp:</para>

<screen os="h"><userinput>make install</userinput></screen>

    <para os="i">Donnez aux bibliothèques dynamiques de Readline des droits plus appropriés&nbsp;:</para>

<screen os="j"><userinput>chmod -v 755 /lib/lib{readline,history}.so*</userinput></screen>

    <para os="k">Maintenant, déplacez les bibliothèques statiques à un endroit pl1s adapté&nbsp;:</para>

<screen os="l"><userinput>mv -v /lib/lib{readline,history}.a /usr/lib</userinput></screen>

    <para os="m">Ensuite, supprimez les fichiers <filename class="extension">.so</filename>
    dans <filename class="directory">/lib</filename> et liez-les à nouveau à
    <filename class="directory">/usr/lib</filename>.</para>

<screen os="n"><userinput>rm -v /lib/lib{readline,history}.so
ln -svf ../../lib/libreadline.so.5 /usr/lib/libreadline.so
ln -svf ../../lib/libhistory.so.5 /usr/lib/libhistory.so</userinput></screen>

  </sect2>

  <sect2 id="contents-readline" role="content">
    <title>Contenu de Readline</title>

    <segmentedlist>
      <segtitle>Bibliothèques installées</segtitle>

      <seglistitem>
        <seg>libhistory.[a,so] et libreadline.[a,so]</seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descriptions courtes</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="libhistory">
        <term><filename class="libraryfile">libhistory</filename></term>
        <listitem>
          <para>Fournit une interface utilisateur cohérente pour rappeler des lignes
          dans l'historique</para>
          <indexterm zone="ch-system-readline libhistory">
            <primary sortas="c-libhistory">libhistory</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="libreadline">
        <term><filename class="libraryfile">libreadline</filename></term>
        <listitem>
          <para>Aide à une cohérence dans l'interface utilisateur pour des programmes
          discrets qui ont besoin d'une interface en ligne de commande</para>
          <indexterm zone="ch-system-readline libreadline">
            <primary sortas="c-libreadline">libreadline</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>

