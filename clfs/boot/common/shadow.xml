<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;
]>

<sect1 id="ch-boot-shadow" role="wrap">
  <?dbhtml filename="shadow.html"?>

  <title>Shadow-&shadow-version;</title>

  <indexterm zone="ch-boot-shadow">
    <primary sortas="a-Shadow">Shadow</primary>
    <secondary>boot</secondary>
  </indexterm>

 <xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
  href="../../final-system/common/shadow.xml"
  xpointer="xpointer(//*[@role='package'])"/>

  <sect2 role="installation">
    <title>Installation de Shadow</title>

    <para os="b">Désactivez l'installation du programme <command>groups</command>
    et de ses pages de man, car Coreutils offre une meilleure version&nbsp;:</para>

<screen os="c"><userinput>sed -i 's/groups$(EXEEXT) //' src/Makefile.in
find man -name Makefile.in -exec sed -i '/groups\.1\.xml/d' '{}' \;
find man -name Makefile.in -exec sed -i 's/groups\.1 / /' {} \;</userinput></screen>

    <para os="t1">Les entrées de cache suivantes définissent les valeurs des
    tests qui ne se lancent pas dans une compilation croisée&nbsp;:</para>

<screen os="t2"><userinput>echo "ac_cv_func_setpgrp_void=yes" &gt; config.cache</userinput></screen>

    <para os="d">Préparez la compilation de Shadow&nbsp;:</para>

<screen os="e"><userinput>./configure --prefix=/tools \
    --build=${CLFS_HOST} --host=${CLFS_TARGET} --sysconfdir=/etc \
    --cache-file=config.cache</userinput></screen>

<para os="f">Voici la signification des options de configure&nbsp;:</para>

  <variablelist os="g">
    <varlistentry>
      <term><parameter>--sysconfdir=/etc</parameter></term>
      <listitem><para>Dit à Shadow d'installer ses fichiers de configuration 
      dans <filename class="directory">/etc</filename> et non dans
      <filename class="directory">/tools/etc</filename>.</para></listitem>
    </varlistentry>
    
  </variablelist>


    <para os="h">Compilez le paquet&nbsp;:</para>

<screen os="i"><userinput>make</userinput></screen>

    <para os="j">Ce paquet n'est pas fourni avec une suite de tests.</para>

    <para os="k">Installez le paquet&nbsp;:</para>

<screen os="l"><userinput>make DESTDIR=${CLFS} install</userinput></screen>

    <para os="u">Liez <command>login</command> et <command>passwd</command> à
    /bin&nbsp;:</para>

<screen os="v"><userinput>ln -sv /tools/bin/{login,passwd} ${CLFS}/bin</userinput></screen>

  </sect2>

  <sect2 role="content">
    <title/>

    <para>Les détails sur ce paquet sont situÃ©s dans <xref
    linkend="contents-shadow" role="."/></para>
  </sect2>

</sect1>
