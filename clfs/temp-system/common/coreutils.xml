<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;
]>

<sect1 id="ch-temp-system-coreutils" role="wrap">
  <?dbhtml filename="coreutils.html"?>

  <title>Coreutils-&coreutils-version;</title>

  <indexterm zone="ch-temp-system-coreutils">
    <primary sortas="a-Coreutils">Coreutils</primary>
    <secondary>système temporaire</secondary>
  </indexterm>

  <xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
  href="../../final-system/common/coreutils.xml"
  xpointer="xpointer(//*[@role='package'])"/>

<sect2 role="installation">
    <title>Installation de Coreutils</title>

    <para os="aa">La commande suivante met à jour les temps indiqués sur les pages de man
    d'uname et de hostname afin que Makefile ne s'attende pas à les
    regénérer&nbsp;:</para>

<screen os="ab"><userinput>touch man/uname.1 man/hostname.1</userinput></screen>

    <para os="ba">Configure ne peut pas déterminer correctement comment obtenir de l'espace libre
    lors de la compilation croisée, il en résulte que le programme <command>df</command> ne sera pas construit. Ajoutez 
    les entrées suivantes dans
    <filename>config.cache</filename> pour corriger cela et corrigez
    divers problèmes de compilation croisée&nbsp;:</para>

<screen os="bb"><userinput>cat &gt; config.cache &lt;&lt; EOF
fu_cv_sys_stat_statfs2_bsize=yes
gl_cv_func_working_mkstemp=yes
EOF</userinput></screen>


    <para os="bd">Préparez la compilation de Coreutils&nbsp;:</para>

<screen os="be"><userinput>./configure --prefix=/tools \
    --build=${CLFS_HOST} --host=${CLFS_TARGET} \
    --enable-install-program=hostname --cache-file=config.cache</userinput></screen>

    <para os="h1">Coreutils ne construit pas make-prime-list correctement
    et il se peut que l'hôte n'exécute pas le binaire cible. Construisez-le en utilisant le
    compilateur de l'hôte pour qu'on puisse l'exécuter pour la génération des
    données nécessaires à la construction.</para>

<screen os="h2"><userinput>cp -v Makefile{,.orig}
sed '/src_make_prime_list/d' Makefile.orig > Makefile
depbase=`echo src/make-prime-list.o | sed 's|[^/]*$|.deps/&amp;|;s|\.o$||'`;\
    gcc -std=gnu99  -I. -I./lib  -Ilib -I./lib -Isrc -I./src  \
    -fdiagnostics-show-option -funit-at-a-time -g -O2 -MT \
    src/make-prime-list.o -MD -MP -MF $depbase.Tpo -c -o src/make-prime-list.o \
    src/make-prime-list.c &amp;&amp;
mv -f $depbase.Tpo $depbase.Po
gcc -std=gnu99 -fdiagnostics-show-option -funit-at-a-time -g -O2 \
        -Wl,--as-needed  -o src/make-prime-list src/make-prime-list.o</userinput></screen>

    <para os="h3">Supprimez la construction de la page de man de hostname car
    elle est modifiée par les commandes précédentes.</para>

<screen os="h4"><userinput>cp -v Makefile{,.bak}
sed -e '/hostname.1/d' Makefile.bak > Makefile</userinput></screen>

   <para os="bf">Compilez le paquet&nbsp;:</para>

<screen os="bg"><userinput>make</userinput></screen>

    <para os="bh">Installez le paquet&nbsp;:</para>

<screen os="bi"><userinput>make install</userinput></screen>

  </sect2>

  <sect2 role="content">
    <title/>

    <para>Les détails sur ce paquet sont disponibles dans <xref
    linkend="contents-coreutils" role="."/></para>

  </sect2>

</sect1>
