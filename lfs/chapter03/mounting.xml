<sect1 id="ch04-mounting">
<title>Monter la nouvelle partition</title>
<?dbhtml filename="mounting.html" dir="chapter03"?>

<para>Maintenant que nous avons créé un système de fichiers, nous voulons être
capable d'accéder à la partition. Pour cela, nous avons besoin de la monter et
nous devons choisir un point de montage. Dans ce livre, nous assumons que le
système de fichiers est monté sous <filename>/mnt/lfs</filename>, mais peu
importe le répertoire que vous avez choisi.</para>

<para>Choisissez un point de montage et assignez-le à la variable
d'environnement LFS en lançant:</para>

<para><screen><userinput>export LFS=/mnt/lfs</userinput></screen></para>

<para>Maintenant, créez le point de montage et montez le système de fichiers
LFS en lançant:</para>

<para><screen><userinput>mkdir -p $LFS &amp;&amp;
mount /dev/xxx $LFS</userinput></screen></para>

<para>Remplacez <filename>xxx</filename> avec la désignation de la partition
LFS.</para>

<para>Si vous avez décidez d'utiliser de multiples partitions pour LFS (disons
une pour <filename>/</filename> et une autre pour <filename>/usr</filename>),
montez-les ainsi:</para>

<para><screen><userinput>mkdir -p $LFS &amp;&amp;
mount /dev/xxx $LFS &amp;&amp;
mkdir $LFS/usr &amp;&amp;
mount /dev/yyy $LFS/usr</userinput></screen></para>

<para>Bien sûr, remplacez <filename>xxx</filename> et <filename>yyy</filename>
avec les noms de partitions appropriés.</para>

<para>Maintenant que nous nous sommes créés l'endroit où nous allons travailler,
nous sommes prêt à commencer l'assemblage des différents outils au chapitre
suivante.</para>

</sect1>

