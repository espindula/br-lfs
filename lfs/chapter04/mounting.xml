<sect1 id="ch04-mounting">
<title>Monter la nouvelle partition</title>
<?dbhtml filename="mounting.html" dir="chapter04"?>

<para>Maintenant que le système de fichiers a été créé, il est prêt à être
utilisé. Tout ce que nous avons à faire pour accéder à la partition
(c'est-à-dire pour pouvoir y lire ou écrire des données), c'est de la monter.
Si vous l'avez monté sous /mnt/lfs, vous pouvez accéder à cette partition en
allant dans le répertoire /mnt/lfs. Nous considérerons dans ce livre que vous
l'avez montée dans /mnt/lfs. Peu importe le répertoire que vous choisirez, du
moment que vous vous en souvenez.</para>

<para>Créez le répertoire /mnt/lfs en exécutant :</para>

<para><screen><userinput>mkdir -p /mnt/lfs</userinput></screen></para>

<para>Maintenant, montez la partition LFS en exécutant:</para>

<para><screen><userinput>mount /dev/xxx /mnt/lfs</userinput></screen></para>

<para>Remplacez <quote>xxx</quote> par le nom de votre partition (tel que
hda11).</para>

<para>Ce répertoire (/mnt/lfs) est la variable $LFS dont nous avons déjà parlé
précédemment durant le chapitre 2. Désormais si vous voulez utiliser la
variable d'environnement $LFS, il vous faudra exécuter
<userinput>export LFS=/mnt/lfs</userinput> maintenant.</para>

<para>Si vous décidez de créer plusieurs partitions pour LFS (disons $LFS et
$LFS/usr), montez les ainsi:</para>

<para><screen><userinput>mkdir -p /mnt/lfs &amp;&amp;
mount /dev/xxx /mnt/lfs &amp;&amp;
mkdir /mnt/lfs/usr &amp;&amp;
mount /dev/yyy /mnt/lfs/usr</userinput></screen></para>

<para>Bien sur, remplacez /dev/xxx et /dev/yyy avec les désignations
appropriées des partitions.</para>

</sect1>

