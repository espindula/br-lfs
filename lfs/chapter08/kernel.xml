<sect1 id="ch08-kernel">
<title>Installer Linux-&kernel-version;</title>
<?dbhtml filename="kernel.html" dir="chapter08"?>

<screen>Temps de construction estimé:           &kernel-time;
Espace disque nécessaire estimé:  &kernel-compsize;</screen>

<para>Construire un noyau implique quelques étapes: le configurer et
le compiler. Il y a plusieurs façons de configurer un noyau. Si la manière
décrite dans ce livre ne vous convient pas, lisez le fichier
<filename>README</filename> qui est inclus dans l'arborescence des sources du
noyau, et cherchez quelles sont les autres options.</para>

<para>Une chose que vous pourriez faire, est de prendre le fichier
<filename>.config</filename> des sources du noyau de votre distribution hôte et
de le copier dans
<filename class="directory">$LFS/usr/src/linux-&kernel-version;</filename>.
De cette façon vous n'avez pas à configurer la totalité du noyau depuis le
début et pouvez utiliser vos valeurs actuelles. Si vous choisissez de le faire,
commencez par lancer la commande <userinput>make mrproper</userinput>, ensuite
copiez le fichier <filename>.config</filename> par dessus, et enfin lancez
<userinput>make menuconfig</userinput> (<userinput>make oldconfig</userinput>
peut être préférable dans certaines situations. Voir le fichier
<filename>README</filename> pour plus de détails sur l'utilisation du
<userinput>make oldconfig</userinput>).</para>

<para>Si vous avez l'intention d'utiliser des modules pour le noyau,
vous aurez besoin d'un fichier <filename>/etc/modules.conf</filename>. Les
informations pertinentes pour le noyau et pour la configuration du noyau en
général peuvent être trouvées dans la documentation du noyau, qui est stockée
dans <filename>/usr/src/linux-&kernel-version;/Documentation</filename>. La
page de manuel de modules.conf et le HOWTO concernant le noyau, disponible à
l'adresse <ulink url="&tldp-root;HOWTO/Kernel-HOWTO.html"/>, peuvent
aussi vous être utile.</para>

<para>Les commandes suivantes sont utilisées pour construire le noyau:</para>

<para><screen><userinput>make mrproper &amp;&amp;
make menuconfig &amp;&amp;
make dep &amp;&amp;
make bzImage &amp;&amp;
make modules &amp;&amp;
make modules_install &amp;&amp;
cp arch/i386/boot/bzImage /boot/lfskernel &amp;&amp;
cp System.map /boot</userinput></screen></para>

<para>Note: le chemin arch/i386/boot/bzImage peut varier suivant la plateforme.</para>

&aa-kernel-dep;

</sect1>
