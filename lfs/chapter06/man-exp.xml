<sect2>
<title>Explication des commandes</title>

<para><userinput>PATH=$PATH:/usr/bin:/bin ./configure -default</userinput>:
Le chemin de certains programmes sont écrits dans les fichiers de manuel.
Malheureusement, le script configure récupère le dernier emplacement dans
PATH plutôt que le premier où ce programme est trouvé. En ajoutant en suffixe
/usr/bin:/bin au PATH pour la commande ./configure, nous nous assurons que
man n'utilise pas les versions /static de nos programmes.</para>

<para><userinput>patch...:</userinput>
Ces trois correctifs mettent en commentaire une des lignes dans le fichier
<filename>man.conf</filename> (MANPATH /usr/man) car il créera des résultats
redondants si on l'utilise avec des programmes comme
<userinput>whatis</userinput>. Il ajoute aussi l'option <emphasis>-R</emphasis>
à la variable <emphasis>PAGER</emphasis> de manière à ce que les pages man
soient affichées correctement. Dernièrement, ce correctif s'occupe aussi du
problème des pages man non formatés pour les écrans de plus de 80 colonnes en
utilisation avec les versions récentes de groff.</para>

</sect2>

