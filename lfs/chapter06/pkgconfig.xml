<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-system-pkgconfig" role="wrap">
  <?dbhtml filename="pkg-config.html"?>

  <sect1info condition="script">
    <productname>pkgconfig</productname>
    <productnumber>&pkgconfig-version;</productnumber>
    <address>&pkgconfig-url;</address>
  </sect1info>

  <title>Pkg-config-&pkgconfig-version;</title>

  <indexterm zone="ch-system-pkgconfig">
    <primary sortas="a-pkgconfig">pkgconfig</primary>
  </indexterm>

  <sect2 role="package">
    <title/>

    <para>Le paquet pkg-config contient un outil pour passer le chemin include
    et/ou les chemins des bibliothèques afin de construire les outils au moment
    de l'exécution de configure et de make file.</para>

    <segmentedlist>
      <segtitle>&buildtime;</segtitle>
      <segtitle>&diskspace;</segtitle>

      <seglistitem>
        <seg>&pkgconfig-ch6-sbu;</seg>
        <seg>&pkgconfig-ch6-du;</seg>
      </seglistitem>
    </segmentedlist>

  </sect2>

  <sect2 role="installation">
    <title>Installation de Pkg-config</title>

    <para>Préparez la compilation de Pkg-config&nbsp;:</para>

<screen><userinput remap="configure">./configure --prefix=/usr         \
            --with-installed-popt \
            --with-internal-glib  \
            --docdir=/usr/share/doc/pkg-config-&pkgconfig-version;</userinput></screen>

    <para>Compilez le paquet&nbsp;:</para>

<screen><userinput remap="make">make</userinput></screen>

    <para>Pour tester les résultats, lancez&nbsp;</para>

<screen><userinput remap="test">make check</userinput></screen>

    <para>Installez le paquet&nbsp;:</para>

<screen><userinput remap="install">make install</userinput></screen>

  </sect2>


  <sect2 id="contents-pkgconfig" role="content">
    <title>Contenu de Pkg-config</title>

    <segmentedlist>
      <segtitle>Programme installé</segtitle>
      <segtitle>Répertoire installé</segtitle>

      <seglistitem>
        <seg>pkg-config</seg>
        <seg>/usr/share/doc/pkg-config-0.26</seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descriptions courtes</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="pkgconfig">
         <!-- Don't remove the extra space, it prevents a FOP warning. -->
        <term><command>pkg-config </command></term>
        <listitem>
          <para>retourne des méta informations sur la bibliothèque ou le paquet
          spécifié.
          </para>
          <indexterm zone="ch-system-pkgconfig pkgconfig">
            <primary sortas="b-pkgconfig">pkg-config</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
