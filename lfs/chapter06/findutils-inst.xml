<sect2>
<title>Installer Findutils</title>

<para>Avant que Findutils ne soit installé, il faut que le correctif de
findutils soit déballé. Assurez-vous qu'il soit déballé avant de lancer les
commandes d'installation.</para>

<para>Installez Findutils en exécutant les commandes suivantes:</para>

<para><screen><userinput>patch -Np1 -i ../findutils-&findutils-patch-version;.patch &amp;&amp;
patch -Np1 -i ../findutils-&findutils-segfault-patch-version;-segfault.patch &amp;&amp;
./configure --prefix=/usr &amp;&amp;
make libexecdir=/usr/bin &amp;&amp;
make libexecdir=/usr/bin install</userinput></screen></para>

</sect2>

<sect2>
<title>Note à propose de la conformité FHS</title>

<para>Par défaut, l'emplacement de la base de données de updatedb est dans
/usr/var. Si vous préférez être conforme avec FHS, vous devrez utiliser un
autre emplacement. La commande suivante utilise le fichier de base de données
<filename>/var/lib/misc/locatedb</filename> qui est compatible FHS.</para>

<para><screen><userinput>patch -Np1 -i ../findutils-&findutils-patch-version;.patch &amp;&amp;
patch -Np1 -i ../findutils-&findutils-segfault-patch-version;-segfault.patch &amp;&amp;
./configure --prefix=/usr &amp;&amp;
make localstatedir=/var/lib/misc libexecdir=/usr/bin &amp;&amp;
make localstatedir=/var/lib/misc libexecdir=/usr/bin install</userinput></screen></para>

</sect2>
