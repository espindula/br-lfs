<sect2><title>&nbsp;</title><para>&nbsp;</para></sect2>

<sect2><title>Configurer Glibc</title>

<para>Nous avons besoin de créer le fichier /etc/nsswitch.conf. Bien que glibc
puisse fournir un paramétrage par défault lorsque ce fichier manque ou est
corrompu, celle-ci ne fonctionne pas correctement avec les réseaux. Nous
traiterons ceci dans un prochain chapitre. Notre zone horaire a également besoin
d'être configurée.</para>

<para>Créez un nouveau fichier<filename>/etc/nsswitch.conf</filename> en
exécutant ce qui suit:</para>

<para><screen><userinput>cat &gt; /etc/nsswitch.conf &lt;&lt; "EOF"</userinput>
# Debut de /etc/nsswitch.conf

passwd: files
group: files
shadow: files

publickey: files

hosts: files dns
networks: files

protocols: db files
services: db files
ethers: db files
rpc: db files

netgroup: db files

# Fin de /etc/nsswitch.conf
<userinput>EOF</userinput></screen></para>

<para>Le script <userinput>tzselect</userinput> doit être lancé et vous devez
répondre aux questions correspondant à votre zone horaire. Lorsque vous l'aurez
fait, le script vous donnera l'adresse du fichier de zone horaire nécessaire.
</para>

<para>Créez le lien symbolique <filename
class="directory">/etc/localtime</filename> en lançant:</para>

<para><screen><userinput>ln -sf ../usr/share/zoneinfo/&lt;tzselect's output&gt; /etc/localtime</userinput></screen></para>

<para>Les sorties de tzselect peuvent être du style <emphasis>EST5EDT</emphasis>
ou <emphasis>Canada/Eastern</emphasis>.</para>

<para>Le lien symbolique que vous avez créé serait:</para>

<para><screen><userinput>ln -sf ../usr/share/zoneinfo/EST5EDT /etc/localtime</userinput></screen></para>

<para>Ou:</para>

<para><screen><userinput>ln -sf ../usr/share/zoneinfo/Canada/Eastern /etc/localtime </userinput></screen></para>

</sect2>

<sect2><title>&nbsp;</title><para>&nbsp;</para></sect2>

<sect2>
<title>Configuring Dynamic Loader</title>

<para>By default, the dynamic loader
(<filename>/lib/ld-linux.so.2</filename>) searches through <filename
class="directory">/lib</filename> and <filename
class="directory">/usr/lib</filename> for dynamic libraries that are needed
by programs when you run them. However, if there are libraries in
directories other than <filename class="directory">/lib</filename> and
<filename class="directory">/usr/lib</filename>, you need to add them to
the <filename>/etc/ld.so.conf</filename> file for the dynamic
loader to find them. Two directories that are commonly known to contain
additional libraries are <filename
class="directory">/usr/local/lib</filename> and <filename
class="directory">/opt/lib</filename>, so we add those directories to the
dynamic loader's search path.</para>

<para>Create a new file <filename>/etc/ld.so.conf</filename> by running the
following:</para>

<para><screen><userinput>cat &gt; /etc/ld.so.conf &lt;&lt; "EOF"</userinput>
# Begin /etc/ld.so.conf

/usr/local/lib
/opt/lib

# End /etc/ld.so.conf
<userinput>EOF</userinput></screen></para>

</sect2>
