<sect2>
<title>Explication des commandes</title>

<para><userinput>ln -s /static/bin/pwd /bin/pwd</userinput>: Les sources
du noyau enregistrent en dur le chemin vers la commande<filename>pwd</filename>,
c'est-à-dire <filename>/bin/pwd</filename> donc nous créons un lien symbolique
temporaire pour gérer cela.</para>

<para><userinput>make mrproper:</userinput> Ceci s'assure que l'arborescence
du noyau est parfaitement propre. Nous faisons ceci parce que l'équipe de
développement du noyau recommande que ceci soit fait avant
<emphasis>chaque</emphasis> compilation du noyau, et que nous ne devons pas
supposer que l'arborescence des sources soit propre après l'avoir déballé.
</para>

<para><userinput>make include/linux/version.h</userinput> et
<userinput>make symlinks</userinput>: Ceci crée le fichier
<filename>include/linux/version.h</filename>,  ainsi que le lien symbolique
spécifique à la plateforme <filename class="symlink">include/asm</filename>.
</para>

<para><userinput>mkdir /usr/include/asm</userinput>
<userinput>cp include/asm/* /usr/include/asm</userinput> et
<userinput>cp -R include/asm-generic /usr/include</userinput>:
Ces commandes copient les fichiers d'entête assembleur du kernel dans 
<filename class="directory">/usr/include/asm</filename> et
<filename class="directory">/usr/include/asm-generic</filename>.</para>

<para><userinput>cp -R include/linux /usr/include</userinput>:
Cette commande copie les fichiers d'entête pour la cross-compilation du kernel
dans <filename>/usr/include</filename>.</para>

<para><userinput>touch /usr/include/linux/autoconf.h</userinput>:
Quelque fichiers d'entête du kernel incluent le fichier
<filename>autoconf.h</filename>, mais en dehors de la hiérarchie des fichiers
source du noyau Linux, ce fichier n'apporte rien donc nous en créons un vide
de manière à ne pas avoir d'erreurs de compilation lorsqu'il est une dépendance
pour un autre fichier d'entête du noyau.</para>

</sect2>
