<sect2>
<title>Explication des commandes</title>

<para><userinput>patch -Np1 -i ../glibc-&glibc-patch-version;.patch : </userinput>
Ce correctif convertit toutes les occurences de <filename>$(PERL)</filename> en
<filename>/usr/bin/perl</filename> dans le fichier
<filename>malloc/Makefile</filename>. Ceci est fait parce que Glibc ne peut
pas détecter automatiquement l'emplacement de perl, celui-ci n'ayant pas
encore été installé. Ce correctif remplace aussi toutes les occurences du mot
<emphasis>root</emphasis> par <emphasis>0</emphasis> dans le fichier
<filename>login/Makefile</filename>. Ceci est fait parce que Glibc elle-même
n'est pas encore installée et donc, la résolution des noms d'utilisateurs en
identifiants ne fonctionne pas pour l'instant. Un
<userinput>chown root fichier</userinput> échouerait, mais fonctionnerait
en utilisant directement les numéros d'identifiant.</para>

<para>Dernièrement, le correctif résout aussi un problème qui faisait que les
programmes liés statiquement 'plantaient' si ils étaient liés avec des
bibliothèques Glibc-2.2. Ce correctif est seulement nécessaire temporairement
parce que nous avons des programmes statiques dans <filename
class="directory">/static/bin</filename> qui peuvent être liés avec une ancienne
version de Glibc (celle de la distribution hôte). Nous installerons Glibc une
nouvelle fois à la fin de ce chapitre pour supprimer ce correctif et avoir une
Glibc telle que les développeurs le souhaitaient.</para>

<para><userinput>touch /etc/ld.so.conf : </userinput>Une des étapes finales de
l'installation de Glibc est de lancer ldconfig pour mettre à jour le cache du
chargeurdynamique. Si ce fichier n'existe pas, l'installation s'interrompera
avec uneerreur de lecture du fichier, donc nous créons un fichier vide (le
fichier videcontiendra les valeurs par défaut pour Glibc en utilisant /lib et
/usr/lib,ce qui est suffisant).</para>

<para><userinput>--disable-profile : </userinput>Ceci désactive la construction
des bibliothèques avec des informations pour l'évaluation de performances.
Cette commande peut être omise si vous souhaitez travailler sur l'évaluation
de performances.</para>

<para><userinput>--enable-add-ons : </userinput>Ceci active le complément que
nous installons avec Glibc, linuxthreads.</para>

<para><userinput>--libexecdir=/usr/bin : </userinput>Cela force l'installation
du programme pt_chown dans le répertoire /usr/bin.</para>

<para><userinput>echo "cross-compiling = no" &gt; configparms : </userinput>
Nous faisons ceci parce que nous créons seulement notre propre système.
La cross-compilation est utilisée, par exemple, pour construire un package pour
un ApplePower PC sur un système Intel. La raison pour laquelle Glibc pense que
nousfaisons de la cross-compilation est qu'il ne peut compiler un programme de
testpour s'en apercevoir, donc il est par défaut en cross-compilation.
Compiler le programme de test échoue car Glibc n'a pas encore été installée.
</para>

<para><userinput>exec /static/bin/bash --login : </userinput>Cette commande va
démarrer un nouveau shell bash qui va remplacer l'actuel. Cela supprime le
message "I have no name!" de l'invite de commande qui est dû à l'incapacité de
bash à faire correspondre un ID utilisateur avec un nom utilisateur (qui est
dû au fait que Glibc n'est pas installée).</para>

</sect2>
