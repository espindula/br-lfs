<sect2>
<title>Installation du noyau Linux</title>

<para>Nous ne compilerons pas encore un nouveau noyau -- nous le ferons quand
nous aurons fini l'installation de tous les packages. Mais comme certains
packages ont besoin des fichiers d'entête du noyau, nous allons déballer
l'archive du noyau maintenant, le configurer et copier les fichiers d'entête
où ils pourront être trouvés par ces packages.</para>

<para>Il est important de noter que les fichiers du répertoire source du noyau
n'appartiennent pas à root. A chaque fois que vous déballez un package en tant
qu'utilisateur root (ce qui est notre cas lorsque nous sommes dans le chroot),
les fichiers appartiennent aux identifiant utilisateur et groupe de
l'utilisateur ayant créé l'archive. Ceci n'est habituellement pas un problème
pour tout package que vous installez parce que vous supprimez les sources à la
fin de l'installation. Mais les sources du noyau Linux sont souvent conservés
longtemps, donc il existe une chance pour, quelque soit l'uid utilisé, celui-ci
sera assigné à quelqu'un sur la machine, et cette personne disposera des droits
d'écriture sur les sources du noyau.</para>

<para>A la lumière de ceci, vous pourriez vouloir lancer <userinput>chown
0.0</userinput> sur le répertoire linux-&kernel-version; pour vous assurer que
tous les fichiers appartiennent bien à l'utilisateur <emphasis>root</emphasis>.
</para>

<para>Les entêtes du noyau sont installés en lançant les commandes suivantes:
</para>

<para><screen><userinput>ln -s /static/bin/pwd /bin/pwd &amp;&amp;
make mrproper &amp;&amp;
make include/linux/version.h &amp;&amp;
make symlinks &amp;&amp;
cp -HR include/asm /usr/include &amp;&amp;
cp -R include/asm-generic /usr/include &amp;&amp;
cp -R include/linux /usr/include &amp;&amp;
touch /usr/include/linux/autoconf.h &amp;&amp;
rm /bin/pwd</userinput></screen></para>

</sect2>
