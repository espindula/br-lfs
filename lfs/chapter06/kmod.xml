<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-system-kmod" role="wrap">
  <?dbhtml filename="kmod.html"?>

  <sect1info condition="script">
    <productname>kmod</productname>
    <productnumber>&kmod-version;</productnumber>
    <address>&kmod-url;</address>
  </sect1info>

  <title>Kmod-&kmod-version;</title>

  <indexterm zone="ch-system-kmod">
    <primary sortas="a-Kmod">Kmod</primary>
  </indexterm>

  <sect2 role="package">
    <title/>

    <para>Le paquet Kmod contient des bibliothèques et des outils pour charger
    des modules du noyau</para>

    <segmentedlist>
      <segtitle>&buildtime;</segtitle>
      <segtitle>&diskspace;</segtitle>

      <seglistitem>
        <seg>&kmod-ch6-sbu;</seg>
        <seg>&kmod-ch6-du;</seg>
      </seglistitem>
    </segmentedlist>

  </sect2>

  <sect2 role="installation">
    <title>Installation de Kmod</title>

    <para>Préparez la compilation de Kmod&nbsp;:</para>

<screen><userinput remap="configure">./configure --prefix=/usr          \
            --bindir=/bin          \
            --sysconfdir=/etc      \
            --with-rootlibdir=/lib \
            --disable-manpages     \
            --with-xz              \
            --with-zlib</userinput></screen>

    <variablelist>
      <title>Voici la signification des options de configure&nbsp;:</title>

      <varlistentry>
        <term><parameter>--with-xz, --with-zlib</parameter></term>
        <listitem>
          <para>Ces options permettent à Kmod de gérer des modules noyau compressés.</para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><parameter>--disable-manpages</parameter></term>
        <listitem>
          <para>Cette option empêche les pages de manuel de se construire, vu
          qu'elles s'appuient sur libxslt, qui n'est pas disponible dans 
          l'environnement chroot.</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><parameter>--with-rootlibdir=/lib</parameter></term>
        <listitem>
          <para>Cette option garantit que la bibliothèque et les fichiers liés
          seront au bon endroit.</para>
        </listitem>
      </varlistentry>

    </variablelist>

    <para>Compilez le paquet&nbsp;:</para>

<screen><userinput remap="make">make</userinput></screen>

    <para>Pour tester les résultats, lancez&nbsp;:</para>

<screen><userinput remap="test">make check</userinput></screen>

    <para>Installez le paquet et créez des liens symboliques à des
    fins de compatibilité avec Module-Init-Tools, (le paquet qui gérait 
    auparavant les modules du noyau Linux), et ajoutez manuellement 
    les pages de manuel. De plus, assurez-vous que toutes les 
    bibliothèques sont dans le bon répertoire&nbsp;:</para>

<screen><userinput remap="install">make install
for target in depmod insmod modinfo modprobe rmmod; do
  ln -sv ../bin/kmod /sbin/$target
done

ln -sv kmod /bin/lsmod
cp -v man/*.5 /usr/share/man/man5
cp -v man/*.8 /usr/share/man/man8</userinput></screen>

  </sect2>

  <sect2 id="contents-kmod" role="content">
    <title>Contenu de Kmod</title>

    <segmentedlist>
      <segtitle>Programmes installés</segtitle>
      <segtitle>Bibliothèques installées</segtitle>

      <seglistitem>
        <seg>depmod (lien vers kmod), insmod (lien vers kmod), kmod,  kmod-nolib,
        lsmod (lien vers kmod), modinfo (liien vers kmod), modprobe (lien vers kmod),
        et rmmod (liien vers kmod)</seg>
        <seg>libkmod.so</seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descriptions courtes</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="depmod">
        <term><command>depmod</command></term>
        <listitem>
          <para>Crée un fichier de dépendances basé sur les symboles qu'il
          trouve dans l'ensemble de modules existant&nbsp;; ce fichier de
          dépendance est utilisé par
          <command>modprobe</command> pour charger automatiquement les
          modules requis</para>
          <indexterm zone="ch-system-kmod depmod">
            <primary sortas="b-depmod">depmod</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="insmod">
        <term><command>insmod</command></term>
        <listitem>
          <para>Installe un module chargeable dans le noyau en cours d'exécution</para>
          <indexterm zone="ch-system-kmod insmod">
            <primary sortas="b-insmod">insmod</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="kmod">
        <term><command>kmod</command></term>
        <listitem>
          <para>Charge et décharge des modules du noyau</para>
          <indexterm zone="ch-system-kmod kmod">
            <primary sortas="b-kmod">kmod</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="libkmod">
        <term><filename class="libraryfile">libkmod</filename></term>
        <listitem>
          <para>Cette bibliothèque est utilisée par  d'autres programmes pour
          charger et décharger des modules noyau</para>
          <indexterm zone="ch-system-kmod">
            <primary sortas="c-libkmod">libkmod</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="lsmod">
        <term><command>lsmod</command></term>
        <listitem>
          <para>Liste les modules actuellement chargés</para>
          <indexterm zone="ch-system-kmod lsmod">
            <primary sortas="b-lsmod">lsmod</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="modinfo">
        <term><command>modinfo</command></term>
        <listitem>
          <para>Examine un fichier objet associé à un module noyau et affiche des
          informations qu'il peut en tirer</para>
          <indexterm zone="ch-system-kmod modinfo">
            <primary sortas="b-modinfo">modinfo</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="modprobe">
        <term><command>modprobe</command></term>
        <listitem>
          <para>Utilise un fichier de dépendance créé par
          <command>depmod</command>, pour charger automatiquement les modules adéquats</para>
          <indexterm zone="ch-system-kmod modprobe">
            <primary sortas="b-modprobe">modprobe</primary>
          </indexterm>
        </listitem>
      </varlistentry>

      <varlistentry id="rmmod">
        <term><command>rmmod</command></term>
        <listitem>
          <para>Décharge des modules du noyau en cours d'exécution</para>
          <indexterm zone="ch-system-kmod rmmod">
            <primary sortas="b-rmmod">rmmod</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>

