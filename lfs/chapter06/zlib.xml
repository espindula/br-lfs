<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-system-zlib" role="wrap">
  <?dbhtml filename="zlib.html"?>

  <title>Zlib-&zlib-version;</title>

  <indexterm zone="ch-system-zlib">
    <primary sortas="a-Zlib">Zlib</primary>
  </indexterm>

  <sect2 role="package">
    <title/>

    <para>Le paquet Zlib contient des routines de compression et
    décompression utilisées par quelques programmes.</para>

    <segmentedlist>
      <segtitle>&buildtime;</segtitle>
      <segtitle>&diskspace;</segtitle>

      <seglistitem>
        <seg>&zlib-ch6-sbu;</seg>
        <seg>&zlib-ch6-du;</seg>
      </seglistitem>
    </segmentedlist>

  </sect2>

  <sect2 role="installation">
    <title>Installation de Zlib</title>

    <note>
      <para>Zlib est connu pour mal construire sa bibliothèque partagée si
      <envar>CFLAGS</envar> fait partie de l'environnement. En initialisant une
      variable <envar>CFLAGS</envar>, assurez-vous d'ajouter la directive
      <parameter>-fPIC</parameter> à la variable <envar>CFLAGS</envar> pour la durée
      de la commande <command>configure</command> ci-dessous puis de la supprimer
      après coup.</para>
    </note>

    <para>Préparez la compilation de Zlib&nbsp;:</para>

<screen><userinput>./configure --prefix=/usr --shared --libdir=/lib</userinput></screen>

    <para>Compilez le paquet&nbsp;:</para>

<screen><userinput>make</userinput></screen>

    <para>Pour tester les résultats, lancez&nbsp;:

    <userinput>make check</userinput>.</para>

    <para>Installez la bibliothèque partagée&nbsp;:</para>

<screen><userinput>make install</userinput></screen>

    <para>La commande précédente a installé un fichier

    <filename class="extension">.so</filename> dans

    <filename class="directory">/lib</filename>. Nous le supprimerons et
    créerons de nouveau un lien vers

    <filename class="directory">/usr/lib</filename>:</para>

<screen><userinput>rm -v /lib/libz.so
ln -sfv ../../lib/libz.so.&zlib-version; /usr/lib/libz.so</userinput></screen>

    <para>Construisez la bibliothèque statique&nbsp;:</para>

<screen><userinput>make clean
./configure --prefix=/usr
make</userinput></screen>

    <para>Pour tester de nouveau les résultats, lancez&nbsp;:
    <userinput>make check</userinput>.</para>

    <para>Installez la bibliothèque statique&nbsp;:</para>

<screen><userinput>make install</userinput></screen>

    <para>Corrigez les droits sur la bibliothèque statique&nbsp;:</para>

<screen><userinput>chmod -v 644 /usr/lib/libz.a</userinput></screen>

  </sect2>

  <sect2 id="contents-zlib" role="content">
    <title>Contenu de Zlib</title>

    <segmentedlist>
      <segtitle>Bibliothèques installées</segtitle>

      <seglistitem>
        <seg>libz.{a,so}</seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descriptions courtes</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="libz">
        <term><filename class="libraryfile">libz</filename></term>
        <listitem>
          <para>Contient des fonctions de compression et décompression utilisées par
          quelques programmes</para>
          <indexterm zone="ch-system-zlib libz">
            <primary sortas="c-libz">libz</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
