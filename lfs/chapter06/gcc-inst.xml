<sect2>
<title>Installation de GCC</title>

<para>Ce package nécessite que son correctif soit appliqué avant que vous
puissiez l'installer. Assurez-vous qu'il a bien été déballé avant de lancer
les commandes d'installation.</para>

<para>Ce package est connu pour avoir un mauvais fonctionnement si vous avez
changé ses paramètres d'optimisation par défaut (incluant les options -march
et -mcpu). GCC se comporte mieux sans. Donc, si vous avez défini des variables
d'environnement qui écrasent les optimisations par défaut, comme CFLAGS et
CXXFLAGS, nous vous recommendons de les annuler ou de les modifier lors de la
construction de GCC. Vous avez été prévenu.</para>

<para>Installer GCC en exécutant les commandes suivantes. Ces commandes
construiront les compilateurs C et C++. D'autres compilateurs sont disponibles
dans le package gcc. Si vous voulez également construire tous les autres
compilateurs disponibles, enlevez l'option --enable-languages=c,c++ dans
la commande de configuration. Consultez la documentation de GCC pour plus de
détails sur lescompilateurs disponibles.</para>

<para>Note : la construction des autres compilateurs n'est pas testée par les
personnes qui travaillent sur LFS.</para>

<para><screen><userinput>patch -Np1 -i ../gcc-&gcc-patch-version;.patch &amp;&amp;
mkdir ../gcc-build &amp;&amp;
cd ../gcc-build &amp;&amp;
../gcc-&gcc-version;/configure --prefix=/usr --enable-shared \
&nbsp;&nbsp;&nbsp;&nbsp;--enable-languages=c,c++ --enable-threads=posix \
&nbsp;&nbsp;&nbsp;&nbsp;--with-slibdir=/lib --enable-_cxa-atexit \
&nbsp;&nbsp;&nbsp;&nbsp;--enable-clocale=gnu &amp;&amp;
make bootstrap &amp;&amp;
make install &amp;&amp;
ln -s ../usr/bin/cpp /lib &amp;&amp;
ln -s ../bin/cpp /usr/lib &amp;&amp;
ln -s gcc /usr/bin/cc</userinput></screen></para>

</sect2>
