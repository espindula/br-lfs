<sect2>
<title>Installation de Shadow Password Suite</title>

<para>Before you install this package, you may want to have a look at
the Shadow hint. It discusses how you can make your system more secure
regarding passwords, such as how to enable the more secure MD5 passwords,
and how to get the most out of this Shadow package. The Shadow hint can
be found at <ulink url="&hints-root;shadowpasswd_plus.txt"/>.</para>

<para>Avant d'installer ce package, vous devriez jeter un oeil à l'astuce LFS
sur Shadow. Elle examine la façon de rendre son système plus sûr en ce qui
concerne les mots de passe, par exemple en activant des mots de passe MD5 plus
sécurisés, et comment tirer le meilleur parti de ce package. Cette astuce est
disponible à l'adresse suivante: <ulink url="&hints-root;shadowpasswd_plus.txt" />.
</para>

<para>Installez la Shadow Password Suite en lançant les commandes suivantes:</para>

<para><screen><userinput>./configure --prefix=/usr --enable-shared &amp;&amp;
make &amp;&amp;
make install &amp;&amp;
cp etc/{limits,login.access} /etc &amp;&amp;
sed 's%/var/spool/mail%/var/mail%' \
&nbsp;&nbsp;&nbsp;&nbsp;etc/login.defs.linux &gt; /etc/login.defs &amp;&amp;
ln -s vipw /usr/bin/vigr &amp;&amp;
rm /bin/vipw &amp;&amp;
mv /bin/sg /usr/bin &amp;&amp;
mv /lib/{libmisc.*a,libshadow.*a} /usr/lib &amp;&amp;
ln -s ../../lib/libshadow.so /usr/lib</userinput></screen></para>

<para>Sh-utils et Shadow Password Suite installent tous les deux un
programme <filename>groups</filename>. Si vous voulez, vous pouvez
supprimer le programme installé par la Shadow Password Suite en
lançant la commande suivante:</para>

<para><screen><userinput>rm /bin/groups</userinput></screen></para>

</sect2>