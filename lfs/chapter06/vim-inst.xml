<sect2>
<title>Installation de Vim</title>

<para>Ce package nécessite que son patch lui soit appliqué vant
de pouvoir l'installer. Soyez sûr qu'il a été déballé avant de
lancer les commandes d'installation.</para>

<para>Si vous ne voulez pas de vim comme éditeur sur votre système LFS,
vous devez télécharger et installer l'éditeur que vous préférez.
Il y a quelques astuces sur la manière d'installer différents éditeurs
disponibles à <ulink url="&hints-index;#editors"/>.
Les astuces actuellement disponibles concernent Emacs, Joe et nano.</para>

<para>Installez Vim en lançant les commandes suivantes:</para>

<para><screen><userinput>patch -Np1 -i ../vim-&vim-patch-version;.patch &amp;&amp;
./configure --prefix=/usr &amp;&amp;
make CPPFLAGS=-DSYS_VIMRC_FILE=\\\"/etc/vimrc\\\" &amp;&amp;
make install &amp;&amp;
ln -sf vim /usr/bin/vi</userinput></screen></para>

<para>Si vous pensez installer le système X Window sur votre système LFS,
vous voudrez peut-être re-compiler Vim après avoir installé X. Vim est
livré avec une jolie version GUI de l'éditeur qui requiert que X et quelques
autres librairies soient installées. Pour plus d'informations, consultez la
documentation de Vim.</para>

</sect2>

<sect2>
<title>Notes sur la conformité FHS</title>

<para>Le FHS dit que les éditeurs comme vim devrainet utiliser /var/lib/&lt;editor&gt;
pour leur fichiers temporaires, comme les sauvegardes temporaires par exemple.
Si vous voulez que Vim respecte le FHS, vous devriez utiliser les
commandes suivantes à la place des précedentes.</para>

<para><screen><userinput>patch -Np1 -i ../vim-&vim-patch-version;.patch &amp;&amp;
./configure --prefix=/usr --localstatedir=/var/lib/vim &amp;&amp;
make CPPFLAGS=-DSYS_VIMRC_FILE=\\\"/etc/vimrc\\\" &amp;&amp;
make install &amp;&amp;
ln -sf vim /usr/bin/vi</userinput></screen></para>

</sect2>