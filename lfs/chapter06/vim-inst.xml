<sect2>
<title>Installation de Vim</title>

<para>Ce package nécessite que son correctif lui soit appliqué avant de pouvoir
l'installer. Soyez sûr qu'il a été déballé avant de lancer les commandes
d'installation.</para>

<para>Si vous ne souhaitez pas installer vim, des instructions de
constructions pour d'autres éditeurs sont disponibles sur
<ulink url="&blfs-root;view/cvs/postlfs/editors.html"/>.
Actuellement, il existe des instructions pour Emacs, nano et joe.
</para>

<para>Installez Vim en lançant les commandes suivantes:</para>

<para><screen><userinput>patch -Np1 -i ../vim-&vim-patch-version;.patch &amp;&amp;
./configure --prefix=/usr &amp;&amp;
make CPPFLAGS=-DSYS_VIMRC_FILE=\\\"/etc/vimrc\\\" &amp;&amp;
make install &amp;&amp;
ln -s vim /usr/bin/vi</userinput></screen></para>

<para>Si vous pensez installer le système X Window sur votre système LFS,
vous voudrez peut-être re-compiler Vim après avoir installé X. Vim est
livré avec une jolie version GUI de l'éditeur qui requiert que X et quelques
autres bibliothèques soient installées. Pour plus d'informations, consultez la
documentation de Vim.</para>

</sect2>
