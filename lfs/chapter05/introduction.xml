<sect1 id="ch05-introduction">
<title>Introduction</title>
<?dbhtml filename="introduction.html" dir="chapter05"?>

<para>Dans ce chapitre, nous allons compiler et installer un système
Linux de base. Ce système contiendra tous les outils nécessaires pour
commencer la construction du système LFS final dans le prochain
chapitre.</para>

<para>Les logiciels dans ce chapitre seront liés statiquement, parce
qu'il est possible que votre système Linux hôte utilise une version
différente de la bibliothèque C que celle que vous venez de télécharger
pour utiliser dans votre système LFS. Si nous devions lier les
programmes avec l'ancien bibliothèque C dans ce chapitre, ces programmes
ne fonctionneraient pas correctement, ou pas du tout, quand nous
passerons au système LFS.</para>

<para>Les fichiers compilés dans ce chapitre vont être installés sous le
répertoire <filename class="directory">$LFS/static</filename>, de façon
à les garder séparés des fichiers installés dans le chapitre suivant. Tout
ce que est fait ici est temporaire, donc nous ne voulons pas polluer le futur
système LFS.</para>

<para>La clef pour apprendre ce qui fait fonctionner un système Linux est de
savoir exactement quelle est l'utilité de chaque package et en quoi un
utilisateur ou le système en a besoin. Pour cela, une courte description du
contenu de chaque package est fournie juste après les instructions
d'installation.</para>

<para>Beaucoup de nos packages doivent être patchés avant d'être compilés.
Nous appliquons des patchs que là où c'est nécessaire et quand c'est nécessaire.
Donc, n'hésitez pas si il semble que des instructions pour un patch sont
manquantes.</para>

<para>Pendant l'installation de certains packages, vous verrez très
probablement des messages d'alerte du compilateur défilant sur
votre écran. Ceci est normal et peut être ignoré sans danger. Il s'agit
seulement de messages d'avertissement -- principalement au sujet d'utilisation
incorrecte, mais pas illégale, de la syntaxe de C ou de C++. C'est simplement
que, souvent, les standards du langage C ont changé, et quelques packages
utilisent encore le vieux standard, ce qui n'est pas un problème).
</para>

<para>Avant de commencer, assurez-vous que vous avez positionné correctement
la variable LFS, si vous avez décidé de l'utiliser. Exécutez ce qui suit:
</para>

<para><screen><userinput>echo $LFS</userinput></screen></para>

<para>Vérifiez que la sortie contient le bon répertoire vers le point de
montage de la partition LFS (/mnt/lfs par exemple).</para>

<para><screen><userinput>echo $LFS</userinput></screen></para>

</sect1>
