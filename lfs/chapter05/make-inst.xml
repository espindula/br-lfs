<sect2>
<title>Installation de Make</title> 
 
<para>Préparez la compilation de Make avec:</para>
 
<para><screen><userinput>LDFLAGS="-static" ./configure --prefix=$LFS/static --disable-nls</userinput></screen></para>

<para>Continuez la compilation de ce package:</para>

<para><screen><userinput>make</userinput></screen></para>

<para>Et finissez l'installation du package:</para>

<para><screen><userinput>make install</userinput></screen></para> 

<para>Lors de la phase du 'make install', vous verrez ce message
d'avertissement :</para>

<blockquote><screen>chgrp: changing group of `/mnt/lfs/static/bin/make':
Operation not permitted /mnt/lfs/static/bin/make needs to be owned by
group kmem and setgid; otherwise the `-l' option will probably not work.
You may need special privileges to complete the installation of
/mnt/lfs/static/bin/make.</screen></blockquote>
 
<para>Vous pouvez en toute sécurité ignorer ce message.
<filename>make</filename> n'a pas besoin d'appartenir au groupe kmem et
d'utiliser setgid pour que l'option <emphasis>-l</emphasis> fonctionne. (Cette
option demande à <filename>make</filename> de ne démarrer aucun nouveau travail
quand une certaine charge sur le système a été atteinte.)</para>

</sect2>
