<sect2>
<title>Détail de la commande</title>

<para><userinput>--enable-static-link:</userinput> Cette option permet à Bash
d'être lié statiquement.</para>

<para><userinput>--prefix=$LFS/static:</userinput> Cette option installe tous
les fichiers bash dans le répertoire $LFS/static, qui devient le
répertoire /static lors d'un chroot ou d'un reboot dans LFS.</para>

<para><userinput>--with-curses:</userinput> Ceci permet à bash d'être lié à la 
bibliothèque curses plutôt qu'à la bibliothèque termcap par défaut, laquelle
est devenue obsolète.</para>

<para>Il n'est pas nécessaire au sens strict que le bash statique soit lié à 
libncurses (il peut être lié à la bibliothèque statique termcap dans un premier
temps car nous réinstallerons Bash dans le chapitre 6 de toute façon, où
nous utiliserons libncurses), cependant c'est un bon test pour être sûr que le
package ncurses a été correctement installé. S'il ne l'a pas été, vous aurez
quelques problèmes plus loin dans ce chapitre lorsqu'il s'agira d'installer le
package Texinfo. Ce package, qui requiert ncurses et termcap, ne peut plus
alors être utilisé.</para>

<para>Les caractères <userinput>&amp;&amp;</userinput> à la fin de chaque ligne 
permettent de n'exécuter la prochaine commande que si la commande précédente
se termine en retournant 0, indiquant ainsi qu'elle s'est exécutée sans erreur.
Dans le cas où toutes les commandes sont copiées et collées dans le shell, il
est important de s'assurer que si ./configure échoue, make ne soit pas exécuté
et, de même, que si make échoue, make install ne soit pas exécuté, et ainsi de
suite.</para>

</sect2>
