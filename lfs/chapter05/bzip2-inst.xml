<sect2>
<title>Installation de Bzip2</title>

<para>Installez Bzip2 en lançant les commandes suivantes:</para>

<para><screen><userinput>make CC="gcc -static -s" &amp;&amp;
make PREFIX=$LFS/static install</userinput></screen></para>

<para>Bien que cela ne fasse pas partie du système LFS de base, il est utile de
mentionner qu'un correctif pour Tar peut être téléchargé pour permettre
à celui-ci d'utiliser facilement bzip2/bunzip2 pour les compressions et
décompressions. Avec un tar de base, vous devez utiliser des commandes
comme <userinput>bzcat fichier.tar.bz | tar -xv</userinput> ou
<userinput>tar --use-compress-prog=bunzip2 -xvf fichier.tar.bz2</userinput>
pour utiliser bzip2 et bunzip2 avec tar. Ce correctif apporte l'option
<userinput>-j</userinput> pour décompresser une archive bzip2 avec la commande
<userinput>tar -xvfj fichier.tar.bz2</userinput>. Appliquer ce correctif sera
décrit plus tard quand le package tar sera installé.</para>

</sect2>

