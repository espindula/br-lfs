<sect2>
<title>Installation de Tar</title> 
 
<para>Un correctif peut être appliqué pour donner à tar le support direct des
fichiers bzip2. Ce correctif ajoute l'option <emphasis>-j</emphasis> à tar,
option similaire au <emphasis>-z</emphasis> pour les fichiers gzip.</para>

<para>Appliquer le correctif en lançant la commande suivante:</para>

<para><screen><userinput>patch -Np1 -i ../tar-&tar-patch-version;.patch</userinput></screen></para>

<para>Préparez la compilation de Tar:</para>

<para><screen><userinput>LDFLAGS="-static" ./configure --prefix=$LFS/static --disable-nls</userinput></screen></para>

<para>Continuez avec la compilation du package:</para>

<para><screen><userinput>make</userinput></screen></para>

<para>Finissez l'installation du package:</para>

<para><screen><userinput>make install</userinput></screen></para>

</sect2> 
 
