<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-tools-libstdcpp-pass2" role="wrap">
  <?dbhtml filename="gcc-libstdc++-pass2.html"?>

  <sect1info condition="script">
<productname>gcc-libstdc++</productname>
<productnumber>&gcc-version;</productnumber> <address>&gcc-url;</address></sect1info>

  <title>Libstdc++ de GCC-&gcc-version;, Passe 2</title>

  <indexterm zone="ch-tools-libstdcpp-pass2">
    <primary sortas="a-GCC">GCC</primary>
    <secondary>tools, libstdc++ passe 2</secondary>
  </indexterm>

  <sect2 role="package">
    <title/>

    <para>Lors de la construction de <xref linkend="ch-tools-gcc-pass2"/> nous avons
repoussé l'installation de la bibliothèque standard C++ parce qu'aucun
compilateur convenable n'était disponible pour la construire. Nous ne
pouvons pas utiliser le compilateur construit dans cette section parce que
c'est un compilateur natif et qu'il ne doit pas être utilisé en dehors du
chroot ou il risquerait de polluer les bibliothèques avec des composants de
l'hôte.</para>

    <segmentedlist>
      <segtitle>&buildtime;</segtitle>
      <segtitle>&diskspace;</segtitle>

      <seglistitem>
        <seg>&libstdcpp-tmpp2-sbu;</seg>
        <seg>&libstdcpp-tmpp2-du;</seg>
      </seglistitem>
    </segmentedlist>

  </sect2>

  <sect2 role="installation">
    <title>Installation de Target Libstdc++</title>

    <note>
      <para><application>Libstdc++</application> fait partie des sources de GCC. Vous
devez d'abord déballer l'archive de GCC et vous déplacer dans le répertoire
<filename>gcc-&gcc-version;</filename>.</para>
    </note>

    <para>Créez un lien qui existe lors de la construction de libstdc++ dans
l'arborescence des sources de gcc&nbsp;:</para>

<screen><userinput remap="pre">ln -s gthr-posix.h libgcc/gthr-default.h</userinput></screen>

    <para>Créez un répertoire de construction séparé pour libstdc++ et entrez
dedans&nbsp;:</para>

<screen><userinput remap="pre">mkdir -v build
cd       build</userinput></screen>

    <para>Préparez la compilation de libstdc++&nbsp;:</para>

<screen><userinput remap="configure">../libstdc++-v3/configure            \
    CXXFLAGS="-g -O2 -D_GNU_SOURCE"  \
    --prefix=/usr                    \
    --disable-multilib               \
    --disable-nls                    \
    --host=$(uname -m)-lfs-linux-gnu \
    --disable-libstdcxx-pch</userinput></screen>

    <variablelist>
      <title>Voici la signification des options de configure&nbsp;:</title>

      <varlistentry>
        <term><parameter>CXXFLAGS="-g -O2 -D_GNU_SOURCE"</parameter></term>
        <listitem>
          <para>Ces drapeaux sont passés par le Makefile de plus haut niveau lors de la
construction complète de GCC.</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><parameter>--host=$(uname -m)-lfs-linux-gnu</parameter></term>
        <listitem>
          <para>Nous devons mimer ce qui arriverait si ce paquet était construit avec la
construction complète du compilateur. Ce drapeau serait passé au configure
par la machinerie de construction de GCC.</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><parameter>--disable-libstdcxx-pch</parameter></term>
        <listitem>
          <para>Ce paramètre évite l'installation des fichiers d'en-tête pré-compilés, qui
ne sont pas requis pour le moment.</para>
        </listitem>
      </varlistentry>

    </variablelist>

    <para>Compilez libstdc++ en lançant&nbsp;:</para>

<screen><userinput remap="make">make</userinput></screen>

    <para>Installez la bibliothèque&nbsp;:</para>

<screen><userinput remap="install">make install</userinput></screen>

  </sect2>

  <sect2 role="content">
    <title/>

    <para>Les détails de ce paquet se trouvent dans <xref linkend="contents-gcc"
role="."/></para>

  </sect2>

</sect1>
