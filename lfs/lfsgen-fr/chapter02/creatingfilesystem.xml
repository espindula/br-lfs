<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-partitioning-creatingfilesystem">
  <?dbhtml filename="creatingfilesystem.html"?>

  <title>Créer un système de fichiers sur la partition</title>

  <para>Maintenant qu'une partition vierge est prête, le système de fichiers peut
être créé. LFS peut utiliser n'importe quel système de fichiers reconnu par
le noyau Linux, mais les types les plus classiques sont ext3 et ext4. Le
choix d'un système de fichiers peut être complexe et il dépend des
caractéristiques des fichiers et de la taille de la partition. Par
exemple&nbsp;:</para>

  <variablelist>
    <varlistentry>
      <term>ext2</term>
      <listitem><para>convient aux petites partitions rarement renouvelées telles que /boot.</para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>ext3</term>
      <listitem><para>mise à jour de l'ext2 comprenant un journal aidant à récupérer l'état de la
partition en cas d'arrêt brutal. On l'utilise en général dans une
perspective généraliste.
      </para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>ext4</term>
      <listitem><para>est la dernière version des systèmes de fichiers de la famille ext. Il offre
de nouvelles possibilités, notamment l'horodatage à la nanoseconde, la
création et l'utilisation de très gros fichiers (16&nbsp;To), et des
améliorations de vitesse.</para>
      </listitem>
    </varlistentry>
  </variablelist>

  <para>D'autres systèmes de fichiers comme FAT32, NTFS, ReiserFS, JFS et XFS
servent à des fins plus spécifiques. Vous pouvez trouver plus d'informations
sur ces systèmes de fichiers sur <ulink
url="https://fr.wikipedia.org/wiki/Liste_des_syst%C3%A8mes_de_fichiers"/>.</para>

  <para>LFS suppose que le système de fichiers racine (/) est de type ext4. Pour
créer un système de fichiers <systemitem
class="filesystem">ext4</systemitem> sur la partition LFS, lancez ce qui
suit&nbsp;:</para>

<screen role="nodump"><userinput>mkfs -v -t ext4 /dev/<replaceable>&lt;xxx&gt;</replaceable></userinput></screen>

  <para>Remplacez <replaceable>&lt;xxx&gt;</replaceable> par le nom de la partition
LFS.</para>

  <para>Si vous utilisez une partition de <systemitem class="filesystem">swap
</systemitem> existante, il n'est pas nécessaire de la formater. Si vous
avez créé une nouvelle partition <systemitem
class="filesystem">swap</systemitem>, elle devra être initialisée, pour
pouvoir être utilisée, en exécutant la commande&nbsp;:</para>

<screen role="nodump"><userinput>mkswap /dev/<replaceable>&lt;yyy&gt;</replaceable></userinput></screen>

  <para>Remplacez <replaceable>&lt;yyy&gt;</replaceable> par le nom de la partition
de <systemitem class="filesystem">swap</systemitem>.</para>

</sect1>
