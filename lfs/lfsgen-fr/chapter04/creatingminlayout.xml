<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-tools-creatingminlayout">
  <?dbhtml filename="creatingminlayout.html"?>

  <title>Créer un ensemble limité de répertoire dans le système de fichiers LFS</title>

  <para>La première chose à faire dans la partition LFS est de créer une hiérarchie
de répertoires limitée pour que les programmes compilés dans le <xref
linkend="chapter-temporary-tools"/> (ainsi que glibc et libstdc++ dans le
<xref linkend="chapter-cross-tools"/>) puissent être installés à leur
emplacement final. Cela est requis pour que ces programmes temporaires
puissent être remplacés lorsqu'on les reconstruira dans le <xref
linkend="chapter-building-system"/>.</para>

  <para>Créez la disposition requise des répertoires en lançant ce qui suit en tant
que <systemitem class="username">root</systemitem>&nbsp;:</para>

<screen><userinput>mkdir -pv $LFS/{etc,var} $LFS/usr/{bin,lib,sbin}

for i in bin lib sbin; do
  ln -sv usr/$i $LFS/$i
done

case $(uname -m) in
  x86_64) mkdir -pv $LFS/lib64 ;;
esac</userinput></screen>

    <note>
      <para>
        La commande ci-dessus est correcte. La commande <command>ln</command> a
quelques variations syntaxiques, vérifiez <command>info coreutils
ln</command> et <filename>ln(1)</filename> avant de signaler ce que vous
pensez être une erreur.
	  </para>
    </note>

  <para>Les programmes du <xref linkend="chapter-temporary-tools"/> seront compilés
avec un compilateur croisé (plus de détails dans la section <xref
linkend="ch-tools-toolchaintechnotes"/>). Pour séparer ce compilateur croisé
des autres programmes, il sera installé dans un répertoire spécial. Créez ce
répertoire avec&nbsp;:</para>

<screen><userinput>mkdir -pv $LFS/tools</userinput></screen>

</sect1>
