<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE part PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-config-shells" xreflabel="Créaction du fichier /etc/shells">
  <?dbhtml filename="etcshells.html"?>

  <title>Créaction du fichier /etc/shells</title>

  <indexterm zone="ch-config-shells">
    <primary sortas="e-etc-shells">/etc/shells</primary>
  </indexterm>

  <para>Le fichier <filename>shells</filename> contient une liste des shells de
connexion présents sur le système. Les applications utilisent ce fichier
pour déterminer si un shell est valide. Pour chaque shell, une seule ligne
doit être présente, contenant l'emplacement du shell relativement à la
racine (/).</para>

  <para>Par exemple, ce fichier est consulté par <command>chsh</command> pour
déterminer si un utilisateur non privilégié peut modifier le shell de
connexion de son compte. Si le nom de la commande n'est pas listé,
l'utilisateur n'aura pas le droit d'en changer.</para>

  <para>C'est nécessaire pour des applications telles que
<application>GDM</application> qui ne peuplent pas le navigateur d'interface
s'il ne peut pas trouver <filename>/etc/shells</filename>, ou les démons FTP
qui interdisent traditionnellement aux utilisateurs l'accès avec des shells
qui ne sont pas inclus dans ce fichier.</para>

<screen role="root"><userinput>cat &gt; /etc/shells &lt;&lt; "EOF"
<literal># Begin /etc/shells

/bin/sh
/bin/bash

# End /etc/shells</literal>
EOF</userinput></screen>

</sect1>
