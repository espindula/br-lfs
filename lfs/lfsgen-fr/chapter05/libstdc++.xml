<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-tools-libstdcpp-pass1" role="wrap">
  <?dbhtml filename="gcc-libstdc++-pass1.html"?>

  <sect1info condition="script">
<productname>gcc-libstdc++</productname>
<productnumber>&gcc-version;</productnumber> <address>&gcc-url;</address></sect1info>

  <title>Libstdc++ de GCC-&gcc-version;, passe 1</title>

  <indexterm zone="ch-tools-libstdcpp-pass1">
    <primary sortas="a-GCC">GCC</primary>
    <secondary>tools, libstdc++ passe 1</secondary>
  </indexterm>

  <sect2 role="package">
    <title/>

    <para>Libstdc++ est la bibliothèque standard du C++. Elle est utilisée pour
compiler du code C++ (une partie de GCC est écrit en C++) mais nous avons dû
retarder son installation lorsqu'on a construit <xref
linkend="ch-tools-gcc-pass1"/>, car elle dépend de glibc, qui n'était pas
encore disponible dans le répertoire cible.
    </para>

    <segmentedlist>
      <segtitle>&buildtime;</segtitle>
      <segtitle>&diskspace;</segtitle>

      <seglistitem>
        <seg>&libstdcpp-tmpp1-sbu;</seg>
        <seg>&libstdcpp-tmpp1-du;</seg>
      </seglistitem>
    </segmentedlist>

  </sect2>

  <sect2 role="installation">
    <title>Installation de Libstdc++ Cible</title>

    <note>
      <para><application>Libstdc++</application> fait partie des sources de GCC.  Vous
devriez d'abord déballer l'archive tar de GCC et vous rendre dans le
répertoire <filename>gcc-&gcc-version;</filename>.</para>
    </note>

    <para>Créez un répertoire de construction séparé pour libstdc++ et
rentrez-y&nbsp;:</para>

<screen><userinput remap="pre">mkdir -v build
cd build</userinput></screen>

    <para>Préparez la compilation de libstdc++&nbsp;:</para>

<screen><userinput remap="configure">../libstdc++-v3/configure           \
    --host=$LFS_TGT                 \
    --build=$(../config.guess)      \
    --prefix=/usr                   \
    --disable-multilib              \
    --disable-nls                   \
    --disable-libstdcxx-pch         \
    --with-gxx-include-dir=/tools/$LFS_TGT/include/c++/&gcc-version;</userinput></screen>

    <variablelist>
      <title>Voici la signification des options de configure&nbsp;:</title>

      <varlistentry>
        <term><parameter>--host=...</parameter></term>
        <listitem>
          <para>Indique d'utiliser le compilateur croisé que nous venons tout juste de
construire à la place de celui de <filename>/usr/bin</filename>.</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><parameter>--disable-libstdcxx-pch</parameter></term>
        <listitem>
          <para>Ce paramètre empêche l'installation des fichiers inclus pré-compilés, qui ne
sont pas nécessaires pour l'instant.</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><parameter>--with-gxx-include-dir=/tools/$LFS_TGT/include/c++/&gcc-version;</parameter></term>
        <listitem>
          <para>C'est l'emplacement où le compilateur C++ recherche les fichiers d'en-tête
standards. Dans une construction normale, ces informations sont passées
automatiquement aux options <command>configure</command> de libstdc++ à
partir du premier niveau de répertoire. Dans notre cas, il faut donner
explicitement ces informations.</para>
        </listitem>
      </varlistentry>

    </variablelist>

    <para>Compilez libstdc++ en lançant&nbsp;:</para>

<screen><userinput remap="make">make</userinput></screen>

    <para>Installez la bibliothèque&nbsp;:</para>

<screen><userinput remap="install">make DESTDIR=$LFS install</userinput></screen>

  </sect2>

  <sect2 role="content">
    <title/>

    <para>Les détails sur ce paquet sont situés dans <xref linkend="contents-gcc"
role="."/></para>

  </sect2>

</sect1>
