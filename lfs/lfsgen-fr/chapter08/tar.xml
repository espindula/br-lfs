<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>

<sect1 id="ch-system-tar" role="wrap">
  <?dbhtml filename="tar.html"?>

  <sect1info condition="script">
<productname>tar</productname> <productnumber>&tar-version;</productnumber>
<address>&tar-url;</address></sect1info>

  <title>Tar-&tar-version;</title>

  <indexterm zone="ch-system-tar">
    <primary sortas="a-Tar">Tar</primary>
  </indexterm>

  <sect2 role="package">
    <title/>

    <para>Le paquet Tar fournit la possibilité de créer des archives tar et effectuer
diverses manipulations d'archives. Tar peut être utilisé sur des archives
précédemment créées pour extraire des fichiers, ajouter des fichiers
supplémentaires, mettre à jour ou lister les fichiers qui étaient déjà
stockés. </para>

    <segmentedlist>
      <segtitle>&buildtime;</segtitle>
      <segtitle>&diskspace;</segtitle>

      <seglistitem>
        <seg>&tar-fin-sbu;</seg>
        <seg>&tar-fin-du;</seg>
      </seglistitem>
    </segmentedlist>

  </sect2>

  <sect2 role="installation">
    <title>Installation de Tar</title>

    <para>Préparez la compilation de Tar&nbsp;:</para>

<screen><userinput remap="configure">FORCE_UNSAFE_CONFIGURE=1  \
./configure --prefix=/usr</userinput></screen>

    <variablelist>
      <title>Voici la signification de l'option de configure&nbsp;:</title>

      <varlistentry>
        <term><envar>FORCE_UNSAFE_CONFIGURE=1</envar></term>
        <listitem>
          <para>Ceci oblige le test de <function>mknod</function> à se lancer en tant que
<systemitem class="username">root</systemitem>. On considère généralement
que lancer ce test en tant qu'utilisateur <systemitem
class="username">root</systemitem> est dangereux, mais comme on ne l'exécute
que sur un système qui n'a été construit que partiellement, ce remplacement
est acceptable.</para>
        </listitem>
      </varlistentry>
    </variablelist>

    <para>Compilez le paquet&nbsp;:</para>

<screen><userinput remap="make">make</userinput></screen>

    <para>To test the results, issue:</para>

<screen><userinput remap="test">make check</userinput></screen>

    <para>Un test, capabiblities: binary store/restore, est connu pour échouer s'il
est lancé (LFS n'a pas selinux), mais sera passé si le noyau hôte ne prend
pas en charge les attributs étendus sur le système de fichiers utilisé pour
la construction de LFS.</para>

    <para>Installez le paquet&nbsp;:</para>

<screen><userinput remap="install">make install
make -C doc install-html docdir=/usr/share/doc/tar-&tar-version;</userinput></screen>

  </sect2>


  <sect2 id="contents-tar" role="content">
    <title>Contenu de Tar</title>

    <segmentedlist>
      <segtitle>Programmes installés</segtitle>
      <segtitle>Répertoire installé</segtitle>

      <seglistitem>
        <seg>tar</seg>
        <seg>/usr/share/doc/tar-&tar-version;</seg>
      </seglistitem>
    </segmentedlist>

    <variablelist>
      <bridgehead renderas="sect3">Descriptions courtes</bridgehead>
      <?dbfo list-presentation="list"?>
      <?dbhtml list-presentation="table"?>

      <varlistentry id="tar">
        <term><command>tar</command></term>
        <listitem>
          <para>Crée, extrait des fichiers à partir d'archives et liste le contenu
d'archives, connues sous le nom d'archives tar</para>
          <indexterm zone="ch-system-tar tar">
            <primary sortas="b-tar">tar</primary>
          </indexterm>
        </listitem>
      </varlistentry>

    </variablelist>

  </sect2>

</sect1>
