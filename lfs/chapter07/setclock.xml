<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.3//EN"
"http://www.oasis-open.org/docbook/xml/4.3/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>
<sect1 id="ch-scripts-setclock">
<title>Configuration du script setclock</title>
<?dbhtml filename="setclock.html"?>

<indexterm zone="ch-scripts-setclock">
<primary sortas="d-setclock">setclock</primary>
<secondary>configuration</secondary></indexterm>

<para>Le script setclock lit l'heure à partir de l'horloge matérielle,
aussi connu en tant qu'horloge BIOS ou CMOS (<foreignphrase>(Complementry
Metal-Oxide Semiconductor</foreignphrase>) et soit convertit cette heure
en temps local en utilisant le fichier <filename>/etc/localtime</filename>
(si l'horloge matérielle est réglée sur GMT), soit ne le fait pas (si votre
horloge matérielle est déjà réglé sur l'heure locale).
Il n'existe pas de moyens pour détecter automatiquement si votre horloge
est réglée sur l'heure GMT ou locale, donc nous avons besoin de le configurer
nous-même.</para>

<para>Changer la valeur de la variable <emphasis>UTC</emphasis> ci-dessous
en indiquant <emphasis>0</emphasis> (zéro) si votre horloge matérielle
n'utilise pas l'heure GMT.</para>

<para>Créez un nouveau fichier <filename>/etc/sysconfig/clock</filename> en
lançant la commande suivante&nbsp;:</para>

<screen><userinput>cat &gt; /etc/sysconfig/clock &lt;&lt; "EOF"</userinput>
# Début /etc/sysconfig/clock

UTC=1

# Fin /etc/sysconfig/clock
<userinput>EOF</userinput></screen>

<para>Maintenant, vous voudrez peut-être jeter un &oelig;il sur cette excellente
astuce expliquant comment gérer le temps avec LFS&nbsp;: <ulink 
url="&hints-root;time.txt" />. Il explique des points tels que les fuseaux
horaires, UTC, et la variable d'environnement TZ.</para>

</sect1>

