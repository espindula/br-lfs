<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.3//EN" "http://www.oasis-open.org/docbook/xml/4.3/docbookx.dtd" [
   <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>
<sect1 id="ch-scripts-inputrc">
<title>Créer le fichier /etc/inputrc</title>
<?dbhtml filename="inputrc.html"?>

<indexterm zone="ch-scripts-inputrc"><primary sortas="e-/etc/inputrc">/etc/inputrc</primary></indexterm>

<para>Le fichier <filename>inputrc</filename> gère les fichiers de correspondance
du clavier pour les situations spécifiques. Ce fichier est le fichier de démarrage
utilisé par Readline &mdash; la bibliothèque relative aux entrées &mdash; utilisée
par Bash et la plupart des autres shells.</para>

<para>La plupart des personnes n'ont pas besoin de fichiers de correspondance
spécifiques, donc la commande ci-dessous crée un fichier
<filename>/etc/inputrc</filename> utilisé par tous ceux qui se connectent. Si
vous décidez plus tard que vous avez besoin de surcharger les valeurs par défaut
utilisateur par utilisateur, vous pouvez créer un fichier
<filename>.inputrc</filename> dans le répertoire personnel de l'utilisateur avec
les correspondances modifiées.</para>

<para>Pour plus d'informations sur l'édition du fichier <filename>inputrc</filename>,
voir la section <emphasis>Readline Init File</emphasis> dans <command>info
bash</command>. <command>info readline</command> est aussi une bonne source
d'informations.</para>

<para>Ci-dessous se trouve un fichier <filename>inputrc</filename> générique
avec des commentaires expliquant l'utilité des différentes options. Notez que
les commentaires ne peuvent pas être sur la même ligne que les commandes. Créez
le fichier en utilisant la commande suivante&nbsp;:</para>

<screen><userinput>cat &gt; /etc/inputrc &lt;&lt; "EOF"
<literal># Début /etc/inputrc
# Modifié par Chris Lynn &lt;roryo@roryo.dynup.net&gt;

# Autorise l'invite de commande de passer à la ligne suivant
set horizontal-scroll-mode Off

# Active la saisie 8bit
set meta-flag On 
set input-meta On

# Désactive la suppression du 8ème bit
set convert-meta Off

# Conserve le 8ème bit pour l'affichage
set output-meta On

# aucun, visible ou audible
set bell-style none

# Tout ce qui suit fait correspondre la séquence d'échappement
# de la valeur contenue à l'intérieur du premier argument aux
# fonctions spécifiques de readline

"\eOd": backward-word
"\eOc": forward-word

# pour la console linux
"\e[1~": beginning-of-line
"\e[4~": end-of-line
"\e[5~": beginning-of-history
"\e[6~": end-of-history
"\e[3~": delete-char
"\e[2~": quoted-insert

# pour les xterm
"\eOH": beginning-of-line
"\eOF": end-of-line

# pour Konsole
"\e[H": beginning-of-line
"\e[F": end-of-line

# Fin /etc/inputrc</literal>
EOF</userinput></screen>

</sect1>

