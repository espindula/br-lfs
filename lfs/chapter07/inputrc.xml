<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.3//EN" "http://www.oasis-open.org/docbook/xml/4.3/docbookx.dtd" [
   <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>
<sect1 id="ch-scripts-inputrc">
<title>Créer le fichier /etc/inputrc</title>
<?dbhtml filename="inputrc.html"?>

<indexterm zone="ch-scripts-inputrc"><primary sortas="e-/etc/inputrc">/etc/inputrc</primary></indexterm>

<para>Le fichier <filename>/etc/inputrc</filename> gère la correspondance du
clavier pour des situations spécifiques. Ce fichier est le fichier de démarrage
utilisé par <application>Readline</application>, la bibliothèque relative aux
saisies utilisée par <application>Bash</application> et par la plupart des
autres shells.</para>

<para>Pour plus d'informations, voir la page info de bash, section
<emphasis>Readline Init File</emphasis>. La page info de readline est aussi une
bonne source d'informations.</para>

<para>Les valeurs globales sont initialisées dans
<filename>/etc/inputrc</filename>. Les valeurs personnelles de l'utilisateur
sont configurées dans <filename>~/.inputrc</filename>. Le fichier
<filename>~/.inputrc</filename> surchargera les paramètres globaux. Une page
plus loin configure Bash pour utiliser <filename>/etc/inputrc</filename> s'il
n'y a pas de <filename>.inputrc</filename> pour un utilisateur quand
<filename>/etc/profile</filename> est lu (habituellement au moment de la
connexion). Pour que le système utilise les deux ou pour inverser la gestion
globale du clavier, c'est une bonne idée de placer un fichier
<filename>.inputrc</filename> par défaut dans le répertoire
<filename class="directory">/etc/skel</filename> pour une utilisation par les
nouveaux utilisateurs.</para>

<para>Ci-dessous se trouve un <filename>/etc/inputrc</filename>, avec des
commentaires pour expliquer ce que font les différentes options. Notez que les
commentaires ne peuvent pas être sur la même ligne que les commandes.</para>

<para>Pour créer le fichier <filename>.inputrc</filename> dans
<filename class="directory">/etc/skel</filename> en utilisant la commande
ci-dessous, modifiez la sortie de la commandepar <filename
class="directory">/etc/skel/.inputrc</filename> et assurez-vous de
vérifier/configurer les droits après coup. Copiez ce fichier dans
<filename>/etc/inputrc</filename> et dans le répertoire personnel de chaque
utilisateur existant déjà sur le système, ceci incluant
<emphasis>root</emphasis>, qui a besoin d'une version privée du fichier.
Assurez-vous d'utiliser le paramètre <parameter>-p</parameter> de
<command>cp</command> pour maintenir les droits et assurez-vous aussi de
modifier le propriétaire et le groupe de façon approprié.</para>

<screen><userinput>cat &gt; /etc/inputrc &lt;&lt; "EOF"
<literal># Début /etc/inputrc
# Modifié par Chris Lynn &lt;roryo@roryo.dynup.net&gt;

# Assurez-vous que vous n'affichez pas tout sur la première ligne
set horizontal-scroll-mode Off

# Active la saisie 8bit
set meta-flag On 
set input-meta On

# Désactive la suppression du 8ème bit
set convert-meta Off

# Conserve le 8ème bit pour l'affichage
set output-meta On

# aucun, visible ou audible
set bell-style none

# Tout ce qui suit fait correspondre la séquence d'échappement de la valeur
# contenue à l'intérieur du premier argument aux fonctions spécifiques de
# readline

"\eOd": backward-word
"\eOc": forward-word

# pour la console linux
"\e[1~": beginning-of-line
"\e[4~": end-of-line
"\e[5~": beginning-of-history
"\e[6~": end-of-history
"\e[3~": delete-char
"\e[2~": quoted-insert

# pour les xterm
"\eOH": beginning-of-line
"\eOF": end-of-line

# pour Konsole
"\e[H": beginning-of-line
"\e[F": end-of-line

# Fin /etc/inputrc</literal>
EOF</userinput></screen>

</sect1>

