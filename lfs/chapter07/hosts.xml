<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.3//EN"
"http://www.oasis-open.org/docbook/xml/4.3/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  %general-entities;
]>
<sect1 id="ch-scripts-hosts">
<title>Créer le fichier /etc/hosts</title>
<?dbhtml filename="hosts.html"?>

<indexterm zone="ch-scripts-hosts"><primary
sortas="e-/etc/hosts">/etc/hosts</primary></indexterm>

<indexterm zone="ch-scripts-hosts">
<primary sortas="d-localnet">localnet</primary>
<secondary>/etc/hosts</secondary></indexterm>

<indexterm zone="ch-scripts-hosts">
<primary sortas="d-network">réseau</primary>
<secondary>/etc/hosts</secondary></indexterm>

<para>Si une carte réseau doit être configurée, choisissez l'adresse 
IP, le nom de domaine pleinement qualifié et les alias possibles à déclarer
dans le fichier <filename>/etc/hosts</filename>. La syntaxe est la
suivante&nbsp;:</para>

<screen>&lt;adresse IP&gt; mon-hôte.mon-domaine.org aliases</screen>

<para>À moins que votre ordinateur ne doit être visible à partir d'Internet
(c'est-à-dire que vous avez enregistré un domaine et un bloc valide
d'adresses IP qui vous est affecté, la plupart des utilisateurs n'a pas
ceci), vous devez vous assurer que l'adresse IP se trouve dans la plage
d'adresses réservée aux réseaux privés. Les plages valides sont&nbsp;:</para>

<screen>	Classes Réseaux
	A     10.0.0.0
	B     172.16.0.0 à 172.31.0.255
	C     192.168.0.0 à 192.168.255.255</screen>

<para>Une adresse IP valide pourrait être 192.168.1.1. Un nom de domaine 
pleinement qualifié pour cette adresse IP pourrait être www.linuxfromscratch.org
(ceci n'est pas recommandé car il s'agit d'un nom de domaine valide enregistré
et que cela pourrait causer des problèmes à votre serveur).</para>

<para>Même si vous ne possédez pas de carte réseau, un nom de domaine pleinement
qualifié est toujours requis. Certains programmes en ont besoin pour fonctionner
correctement.</para>

<para>Créez le fichier <filename>/etc/hosts</filename> en lançant la
commande&nbsp;:</para>

<screen><userinput>cat &gt; /etc/hosts &lt;&lt; "EOF"</userinput>
<literal># Début de /etc/hosts (version avec carte réseau)

127.0.0.1 localhost
<replaceable>[192.168.1.1]</replaceable> <replaceable>[&lt;nom d'hôte&gt;.exemple.org]</replaceable> <replaceable>[nom d'hôte]</replaceable>

# Fin de /etc/hosts (version avec carte réseau)</literal>
<userinput>EOF</userinput></screen>

<para>Les valeurs <replaceable>[192.168.1.1]</replaceable> et
<replaceable>[&lt;nom d'hôte&gt;.exemple.org]</replaceable> doivent être
remplacées suivant les contraintes/besoins des utilisateurs (si la machine se
voit affectée une adresse IP par un administrateur réseau/système et que cette
machine est connectée à un réseau existant).</para>

<para>Si vous n'avez pas de carte réseau, créez le fichier
<filename>/etc/hosts</filename> en lançant la commande&nbsp;:</para>

<screen role="nodump"><userinput>cat &gt; /etc/hosts &lt;&lt; "EOF"</userinput>
<literal># Début /etc/hosts (version sans carte réseau)

127.0.0.1 <replaceable>[&lt;nom d'hôte&gt;.exemple.org]</replaceable> <replaceable>[nom d'hôte]</replaceable> localhost

# Fin /etc/hosts (version sans carte réseau)</literal>
<userinput>EOF</userinput></screen>

</sect1>
